[
 {
  "name": "semantic_code_review/review/git.py:@@ -167,35 +168,51",
  "moved": true,
  "old_start": 167,
  "old_count": 35,
  "new_start": 168,
  "new_count": 51,
  "header": "@@ -167,35 +168,51 @@ def _diff_single_ref(",
  "body": "     if no_unstaged:\n         # <ref> vs (HEAD + staged). That's `git diff --cached <ref>` inverted:\n         # actually `git diff <ref> --cached` gives ref vs index.\n-        raw = _git(cwd, \"diff\", \"--cached\", base_sha)\n-        head_committed = _rev_parse(cwd, \"HEAD\")\n+        raw = _safe_diff(cwd, \"--cached\", base_sha)\n+        head_committed = _safe_rev_parse(cwd, \"HEAD\")\n         head_sha = _synthesise_head_sha(head_committed, raw, tag=\"staged\")\n         return raw, base_sha, head_sha, \"ref-working-no-unstaged\", True\n \n     # Default: <ref> vs current working state (includes staged + unstaged).\n-    raw = _git(cwd, \"diff\", base_sha)\n-    head_committed = _rev_parse(cwd, \"HEAD\")\n-    is_dirty = bool(_git(cwd, \"status\", \"--porcelain\").strip())\n+    raw = _safe_diff(cwd, base_sha)\n+    head_committed = _safe_rev_parse(cwd, \"HEAD\")\n+    is_dirty = bool(_safe_status(cwd).strip())\n     head_sha = _synthesise_head_sha(head_committed, raw) if is_dirty else head_committed\n     mode = \"ref-working\" if is_dirty else \"ref..HEAD\"\n     return raw, base_sha, head_sha, mode, True\n \n \n-def _rev_parse(cwd: Path, ref: str) -> str:\n-    return _git(cwd, \"rev-parse\", \"--verify\", f\"{ref}^{{commit}}\").strip()\n+# Thin shims that re-raise GitError as LocalDiffError so callers of\n+# build_local_diff catch a single domain exception. Keeping them as\n+# small named helpers (rather than inlined try/except blocks) keeps\n+# the call-site flow readable.\n \n+def _safe_rev_parse(cwd: Path, ref: str) -> str:\n+    try:\n+        return git_ops.rev_parse(cwd, ref)\n+    except git_ops.GitError as e:\n+        raise LocalDiffError(str(e)) from e\n \n-def _merge_base(cwd: Path, a: str, b: str) -> str:\n-    return _git(cwd, \"merge-base\", a, b).strip()\n \n+def _safe_merge_base(cwd: Path, a: str, b: str) -> str:\n+    try:\n+        return git_ops.merge_base(cwd, a, b)\n+    except git_ops.GitError as e:\n+        raise LocalDiffError(str(e)) from e\n \n-def _git(cwd: Path, *args: str) -> str:\n-    result = subprocess.run(\n-        [\"git\", *args], cwd=cwd, capture_output=True, text=True, check=False\n-    )\n-    if result.returncode != 0:\n-        raise LocalDiffError(f\"git {' '.join(args)} failed: {result.stderr.strip()}\")\n-    return result.stdout\n+\n+def _safe_diff(cwd: Path, *args: str) -> str:\n+    try:\n+        return git_ops.diff(cwd, *args)\n+    except git_ops.GitError as e:\n+        raise LocalDiffError(str(e)) from e\n+\n+\n+def _safe_status(cwd: Path) -> str:\n+    try:\n+        return git_ops.status_porcelain(cwd)\n+    except git_ops.GitError as e:\n+        raise LocalDiffError(str(e)) from e\n \n \n def _concat_diffs(*parts: str) -> str:\n",
  "head_spans": [],
  "base_spans": [
   {
    "start_line": 137,
    "end_line": 181,
    "kind": "function",
    "qualified_name": "_diff_single_ref",
    "depth": 0
   },
   {
    "start_line": 184,
    "end_line": 185,
    "kind": "function",
    "qualified_name": "_rev_parse",
    "depth": 0
   },
   {
    "start_line": 188,
    "end_line": 189,
    "kind": "function",
    "qualified_name": "_merge_base",
    "depth": 0
   },
   {
    "start_line": 192,
    "end_line": 198,
    "kind": "function",
    "qualified_name": "_git",
    "depth": 0
   },
   {
    "start_line": 201,
    "end_line": 202,
    "kind": "function",
    "qualified_name": "_concat_diffs",
    "depth": 0
   },
   {
    "start_line": 205,
    "end_line": 224,
    "kind": "function",
    "qualified_name": "_changed_files",
    "depth": 0
   }
  ],
  "expect_runs": [
   {
    "kind": "ins",
    "start_idx": 19,
    "texts": [
     "# small named helpers (rather than inlined try/except blocks) keeps",
     "# the call-site flow readable."
    ]
   },
   {
    "kind": "ins",
    "start_idx": 22,
    "texts": [
     "def _safe_rev_parse(cwd: Path, ref: str) -> str:",
     "    try:",
     "        return git_ops.rev_parse(cwd, ref)",
     "    except git_ops.GitError as e:",
     "        raise LocalDiffError(str(e)) from e"
    ]
   },
   {
    "kind": "del",
    "start_idx": 28,
    "texts": [
     "def _merge_base(cwd: Path, a: str, b: str) -> str:",
     "    return _git(cwd, \"merge-base\", a, b).strip()"
    ]
   },
   {
    "kind": "ins",
    "start_idx": 31,
    "texts": [
     "def _safe_merge_base(cwd: Path, a: str, b: str) -> str:",
     "    try:",
     "        return git_ops.merge_base(cwd, a, b)",
     "    except git_ops.GitError as e:",
     "        raise LocalDiffError(str(e)) from e"
    ]
   },
   {
    "kind": "ins",
    "start_idx": 45,
    "texts": [
     "def _safe_status(cwd: Path) -> str:",
     "    try:",
     "        return git_ops.status_porcelain(cwd)",
     "    except git_ops.GitError as e:",
     "        raise LocalDiffError(str(e)) from e",
     ""
    ]
   }
  ]
 },
 {
  "name": "semantic_code_review/cli.py:@@ -113,311 +115,18",
  "moved": false,
  "old_start": 113,
  "old_count": 311,
  "new_start": 115,
  "new_count": 18,
  "header": "@@ -113,311 +115,18 @@ def _configure_logging(verbose: bool) -> None:",
  "body": "         h.setLevel(level)\n \n \n-_DEFAULT_GEMINI_API_MODEL = \"gemini-2.5-pro\"\n-\n-\n-def _backend_choices() -> str:\n-    return \", \".join(sorted([\"auto\", *_CONFIG.backends.keys()]))\n-\n-\n def _select_client(backend: str, *, model: str):\n-    \"\"\"Pick a backend handle based on env + explicit choice.\n-\n-    Returns a `Backend` regardless of the path: SDK backends carry a\n-    pydantic-ai model id string, CLI backends carry a `Model` subclass\n-    that wraps the `claude -p` / `gemini -p` subprocess client. The\n-    pipeline calls `make_*_agent(backend.model)` either way.\n+    \"\"\"Resolve a backend name to a `Client` for the augment pipeline.\n \n     `backend` is \"auto\" or any name in `_CONFIG.backends` (builtins +\n-    user-defined `[backends.<name>]` entries). \"auto\" picks claude-api\n-    if `ANTHROPIC_API_KEY` is set, else claude-cli if `claude` is on\n-    PATH, else raises. Both Gemini backends are opt-in only.\n-    \"\"\"\n-    if backend == \"auto\":\n-        backend = _resolve_auto_backend()\n-\n-    bdef = _CONFIG.backends.get(backend)\n-    if bdef is None:\n-        raise typer.BadParameter(\n-            f\"unknown backend {backend!r}; expected one of: {_backend_choices()}.\"\n-        )\n-\n-    btype = bdef.type\n-    if btype is BackendType.ANTHROPIC_SDK:\n-        return _make_anthropic_sdk_backend(backend, model, bdef)\n-    if btype is BackendType.CLAUDE_CLI:\n-        return _make_claude_cli_backend(backend, model)\n-    if btype is BackendType.GOOGLE_SDK:\n-        return _make_google_sdk_backend(backend, model, bdef)\n-    if btype is BackendType.GEMINI_CLI:\n-        return _make_gemini_cli_backend(backend, model, bdef.default_model)\n-    if btype is BackendType.OPENAI_COMPAT:\n-        return _make_openai_compat_backend(backend, model, bdef)\n-    raise typer.BadParameter(f\"backend {backend!r} has unknown type {btype!r}\")\n-\n-\n-def _resolve_auto_backend() -> str:\n-    import shutil as _shutil\n-\n-    bdef = _CONFIG.backends.get(\"claude-api\")\n-    if bdef is not None and (bdef.api_key_env or bdef.api_key_command):\n-        try:\n-            _resolve_api_key(\"claude-api\", bdef)\n-        except typer.BadParameter:\n-            pass\n-        else:\n-            return \"claude-api\"\n-    if _shutil.which(\"claude\"):\n-        return \"claude-cli\"\n-    raise typer.BadParameter(\n-        \"No Anthropic credentials available: set ANTHROPIC_API_KEY \"\n-        \"(or ANTHROPIC_API_TOKEN in .env), install the `claude` CLI \"\n-        \"for subscription-based fallback, or pass --backend=gemini-cli \"\n-        \"(CLI subprocess) / --backend=gemini-api (Google SDK) to opt \"\n-        \"into a Gemini backend.\"\n-    )\n-\n-\n-def _make_anthropic_sdk_backend(name: str, model: str, bdef):\n-    from .augment.agents import Backend\n-\n-    _resolve_and_inject_key(name, bdef, sdk_env=\"ANTHROPIC_API_KEY\")\n-    return Backend(model=f\"anthropic:{model}\")\n-\n-\n-def _make_claude_cli_backend(name: str, model: str):\n-    import shutil as _shutil\n-\n-    from .augment.agents import Backend\n-    from .augment.cli_models import ClaudeCLIModel\n-\n-    if not _shutil.which(\"claude\"):\n-        raise typer.BadParameter(\n-            f\"--backend={name} but `claude` is not on PATH \"\n-            \"(install Claude Code CLI or set ANTHROPIC_API_KEY).\"\n-        )\n-    _warn_cli_fallback()\n-    return Backend(model=ClaudeCLIModel(model=model), is_subprocess_backend=True)\n-\n-\n-def _make_google_sdk_backend(name: str, model: str, bdef):\n-    from .augment.agents import Backend\n-\n-    gem_model = _coerce_gemini_model(model, bdef.default_model)\n-    # GOOGLE_CLOUD_PROJECT short-circuits to Vertex via ADC and skips\n-    # API-key resolution entirely — Vertex doesn't use a bearer token,\n-    # ADC does. Only when there's no GCP project do we need a key.\n-    if os.environ.get(\"GOOGLE_CLOUD_PROJECT\"):\n-        return Backend(model=f\"google-vertex:{gem_model}\")\n-\n-    if bdef.api_key_env or bdef.api_key_command:\n-        _resolve_and_inject_key(name, bdef, sdk_env=\"GEMINI_API_KEY\")\n-    elif not (os.environ.get(\"GEMINI_API_KEY\") or os.environ.get(\"GOOGLE_API_KEY\")):\n-        raise typer.BadParameter(\n-            f\"--backend={name} but no Gemini credentials found. \"\n-            \"Set GEMINI_API_KEY (AI Studio), GOOGLE_API_KEY, or \"\n-            \"GOOGLE_CLOUD_PROJECT (Vertex via ADC).\"\n-        )\n-    return Backend(model=f\"google-gla:{gem_model}\")\n-\n-\n-def _make_gemini_cli_backend(name: str, model: str, default_model: str | None):\n-    import shutil as _shutil\n-\n-    from .augment.agents import Backend\n-    from .augment.cli_models import GeminiCLIModel\n-\n-    if not _shutil.which(\"gemini\"):\n-        raise typer.BadParameter(\n-            f\"--backend={name} but `gemini` is not on PATH \"\n-            \"(install via `npm install -g @google/gemini-cli`).\"\n-        )\n-    if not (\n-        os.environ.get(\"GEMINI_API_KEY\")\n-        or os.environ.get(\"GOOGLE_API_KEY\")\n-        or (Path.home() / \".gemini\" / \"oauth_creds.json\").exists()\n-    ):\n-        raise typer.BadParameter(\n-            f\"--backend={name} but no Gemini credentials found. Set \"\n-            \"GEMINI_API_KEY (AI Studio) or GOOGLE_API_KEY (Vertex), \"\n-            \"or run `gemini` once interactively to complete the \"\n-            \"OAuth flow.\"\n-        )\n-    _warn_gemini_fallback()\n-    gem_model = _coerce_gemini_model(model, default_model)\n-    return Backend(\n-        model=GeminiCLIModel(model=gem_model),\n-        is_subprocess_backend=True,\n-    )\n-\n-\n-def _make_openai_compat_backend(name: str, model: str, bdef):\n-    from .augment.agents import Backend\n-    from .config import BackendDef\n-\n-    assert isinstance(bdef, BackendDef)\n-    if not bdef.base_url:\n-        raise typer.BadParameter(\n-            f\"--backend={name} (type=openai-compat) has no base_url; \"\n-            f\"set [backends.{name}] base_url in config.\"\n-        )\n-    if bdef.api_key_env or bdef.api_key_command:\n-        api_key = _resolve_api_key(name, bdef)\n-    else:\n-        # Local servers (Ollama, llama.cpp) typically require *some*\n-        # non-empty bearer; use a sentinel that's clearly not a key.\n-        api_key = \"not-needed\"\n-\n-    from pydantic_ai.models.openai import OpenAIChatModel\n-    from pydantic_ai.providers.openai import OpenAIProvider\n-\n-    return Backend(\n-        model=OpenAIChatModel(\n-            model_name=model,\n-            provider=OpenAIProvider(base_url=bdef.base_url, api_key=api_key),\n-        ),\n-        # Not a CLI subprocess — this stays False so the in-loop repo\n-        # tools and full pydantic-ai message flow remain enabled.\n-        is_subprocess_backend=False,\n-    )\n-\n-\n-def _resolve_and_inject_key(name: str, bdef, *, sdk_env: str) -> None:\n-    \"\"\"Resolve via env-or-command, then write into the env var the SDK reads.\n-\n-    SDK backends (Anthropic, Google SDK) read their credential from a\n-    fixed env var on construction; pydantic-ai's `anthropic:<model>` /\n-    `google-gla:<model>` strings give us no way to inject a key\n-    explicitly, so we set the env var for them. When `bdef.api_key_env`\n-    matches `sdk_env` (the common case, since the builtins set them\n-    to the same name), the write is idempotent.\n-\n-    Raises if neither env nor command is configured AND `sdk_env` is\n-    not already set in the environment — same precondition the legacy\n-    inline code enforced.\n-    \"\"\"\n-    if not (bdef.api_key_env or bdef.api_key_command):\n-        if not os.environ.get(sdk_env):\n-            raise typer.BadParameter(\n-                f\"--backend={name} but ${sdk_env} is not set \"\n-                \"(load a .env or export the variable).\"\n-            )\n-        return\n-    key = _resolve_api_key(name, bdef)\n-    target = bdef.api_key_env or sdk_env\n-    os.environ[target] = key\n-    # Also mirror to the canonical env var the SDK reads, in case the\n-    # user pointed api_key_env at a custom name.\n-    if target != sdk_env:\n-        os.environ[sdk_env] = key\n-\n-\n-def _resolve_api_key(name: str, bdef) -> str:\n-    \"\"\"Resolve a backend's bearer credential.\n-\n-    Order: `api_key_env` (if set and non-empty) → `api_key_command`\n-    stdout (if set and exits 0 with non-empty output) → BadParameter\n-    explaining what's missing. The command runs without shell\n-    interpretation (argv list) so commands like `gh auth token` and\n-    `gcloud secrets versions access ...` are safe to embed in config.\n-    \"\"\"\n-    import subprocess as _sp\n-\n-    from .config import BackendDef\n-\n-    assert isinstance(bdef, BackendDef)\n-\n-    if bdef.api_key_env:\n-        v = os.environ.get(bdef.api_key_env)\n-        if v:\n-            return v\n-\n-    if bdef.api_key_command:\n-        cmd_str = \" \".join(bdef.api_key_command)\n-        fallback_hint = (\n-            f\" (or set ${bdef.api_key_env} directly)\" if bdef.api_key_env else \"\"\n-        )\n-        try:\n-            proc = _sp.run(\n-                list(bdef.api_key_command),\n-                capture_output=True,\n-                text=True,\n-                timeout=10,\n-                check=False,\n-            )\n-        except FileNotFoundError:\n-            raise typer.BadParameter(\n-                f\"--backend={name}: api_key_command not on PATH: \"\n-                f\"{bdef.api_key_command[0]}{fallback_hint}\"\n-            ) from None\n-        except _sp.TimeoutExpired:\n-            raise typer.BadParameter(\n-                f\"--backend={name}: api_key_command timed out: {cmd_str}\"\n-            ) from None\n-        if proc.returncode != 0:\n-            stderr = (proc.stderr or \"\").strip()\n-            tail = f\"\\n{stderr}\" if stderr else \"\"\n-            raise typer.BadParameter(\n-                f\"--backend={name}: api_key_command exited \"\n-                f\"{proc.returncode}: {cmd_str}{tail}{fallback_hint}\"\n-            )\n-        key = (proc.stdout or \"\").strip()\n-        if not key:\n-            raise typer.BadParameter(\n-                f\"--backend={name}: api_key_command produced empty \"\n-                f\"output: {cmd_str}{fallback_hint}\"\n-            )\n-        return key\n-\n-    # Reached only when api_key_env was set but the var was empty and\n-    # there's no api_key_command fallback (callers gate on\n-    # `api_key_env or api_key_command` before calling, so the\n-    # \"neither configured\" form is unreachable).\n-    raise typer.BadParameter(\n-        f\"--backend={name} but ${bdef.api_key_env} is not set.\"\n-    )\n-\n-\n-def _coerce_gemini_model(model: str, default_model: str | None) -> str:\n-    \"\"\"If a Gemini backend gets a Claude model id (because `[model] default`\n-    was set globally for an Anthropic config), substitute the backend's\n-    own default. Avoids surprising the user with a 404 on the wrong vendor.\n+    user-defined `[backends.<name>]` entries). All dispatch lives in\n+    `semantic_code_review.backends`; this is the CLI's only entry point.\n     \"\"\"\n-    if model.startswith(\"claude\"):\n-        return default_model or _DEFAULT_GEMINI_API_MODEL\n-    return model\n-\n-\n-_FALLBACK_WARNED = False\n-\n-\n-def _warn_cli_fallback() -> None:\n-    global _FALLBACK_WARNED\n-    if _FALLBACK_WARNED:\n-        return\n-    _FALLBACK_WARNED = True\n-    sys.stderr.write(\n-        \"scr: no ANTHROPIC_API_KEY; falling back to `claude -p` subprocess. \"\n-        \"Note: no prompt caching, reduced concurrency, no in-loop repo tools \"\n-        \"(annotation quality will be lower).\\n\"\n-    )\n-    sys.stderr.flush()\n-\n-\n-_GEMINI_WARNED = False\n-\n+    from . import backends as _backends\n \n-def _warn_gemini_fallback() -> None:\n-    global _GEMINI_WARNED\n-    if _GEMINI_WARNED:\n-        return\n-    _GEMINI_WARNED = True\n-    sys.stderr.write(\n-        \"scr: using `gemini -p` subprocess backend. Note: no prompt caching, \"\n-        \"no JSON-schema-constrained output (we validate client-side and retry \"\n-        \"once on failure), reduced concurrency.\\n\"\n-    )\n-    sys.stderr.flush()\n+    if backend == \"auto\":\n+        backend = _backends.resolve_auto(config=_CONFIG)\n+    return _backends.get(backend, config=_CONFIG).resolve(model=model)\n \n \n @app.command()\n",
  "head_spans": [],
  "base_spans": [
   {
    "start_line": 93,
    "end_line": 113,
    "kind": "function",
    "qualified_name": "_configure_logging",
    "depth": 0
   },
   {
    "start_line": 116,
    "end_line": 116,
    "kind": "constant",
    "qualified_name": "_DEFAULT_GEMINI_API_MODEL",
    "depth": 0
   },
   {
    "start_line": 119,
    "end_line": 120,
    "kind": "function",
    "qualified_name": "_backend_choices",
    "depth": 0
   },
   {
    "start_line": 123,
    "end_line": 156,
    "kind": "function",
    "qualified_name": "_select_client",
    "depth": 0
   },
   {
    "start_line": 159,
    "end_line": 178,
    "kind": "function",
    "qualified_name": "_resolve_auto_backend",
    "depth": 0
   },
   {
    "start_line": 181,
    "end_line": 185,
    "kind": "function",
    "qualified_name": "_make_anthropic_sdk_backend",
    "depth": 0
   },
   {
    "start_line": 188,
    "end_line": 200,
    "kind": "function",
    "qualified_name": "_make_claude_cli_backend",
    "depth": 0
   },
   {
    "start_line": 203,
    "end_line": 221,
    "kind": "function",
    "qualified_name": "_make_google_sdk_backend",
    "depth": 0
   },
   {
    "start_line": 224,
    "end_line": 251,
    "kind": "function",
    "qualified_name": "_make_gemini_cli_backend",
    "depth": 0
   },
   {
    "start_line": 254,
    "end_line": 282,
    "kind": "function",
    "qualified_name": "_make_openai_compat_backend",
    "depth": 0
   },
   {
    "start_line": 285,
    "end_line": 312,
    "kind": "function",
    "qualified_name": "_resolve_and_inject_key",
    "depth": 0
   },
   {
    "start_line": 315,
    "end_line": 378,
    "kind": "function",
    "qualified_name": "_resolve_api_key",
    "depth": 0
   },
   {
    "start_line": 381,
    "end_line": 388,
    "kind": "function",
    "qualified_name": "_coerce_gemini_model",
    "depth": 0
   },
   {
    "start_line": 391,
    "end_line": 391,
    "kind": "constant",
    "qualified_name": "_FALLBACK_WARNED",
    "depth": 0
   },
   {
    "start_line": 394,
    "end_line": 404,
    "kind": "function",
    "qualified_name": "_warn_cli_fallback",
    "depth": 0
   },
   {
    "start_line": 407,
    "end_line": 407,
    "kind": "constant",
    "qualified_name": "_GEMINI_WARNED",
    "depth": 0
   },
   {
    "start_line": 410,
    "end_line": 420,
    "kind": "function",
    "qualified_name": "_warn_gemini_fallback",
    "depth": 0
   },
   {
    "start_line": 424,
    "end_line": 442,
    "kind": "function",
    "qualified_name": "fetch",
    "depth": 0
   }
  ],
  "expect_runs": null
 },
 {
  "name": "semantic_code_review/cli.py:@@ -88,14 +88,20",
  "moved": true,
  "old_start": 88,
  "old_count": 14,
  "new_start": 88,
  "new_count": 20,
  "header": "@@ -88,14 +88,20 @@ def _configure_logging(verbose: bool) -> None:",
  "body": "     )\n \n \n-def _select_client(backend: str):  # -> ClaudeClient, but keep import lazy\n-    \"\"\"Pick a `ClaudeClient`-shaped client based on env + explicit choice.\n+_DEFAULT_GEMINI_API_MODEL = \"gemini-2.5-pro\"\n+\n+\n+def _select_client(backend: str, *, model: str = \"claude-opus-4-7\"):\n+    \"\"\"Pick a backend handle based on env + explicit choice.\n+\n+    SDK backends (`api`, `gemini-api`) return an `SDKBackend` carrying\n+    a fully-qualified pydantic-ai model id; CLI backends return their\n+    existing subprocess client. The pipeline branches on\n+    `isinstance(client, SDKBackend)`.\n \n     backend ∈ {\"auto\",\"api\",\"cli\",\"gemini\",\"gemini-api\"}. \"auto\" picks\n-    Anthropic API if its key is set, else `claude -p` if on PATH, else\n-    raises. Both Gemini backends are opt-in only — they're never picked\n-    by auto, since their output quality and cost profile are different\n-    enough that we don't want to surprise users with them.\n+    the Anthropic SDK if `ANTHROPIC_API_KEY` is set, else `claude -p`\n+    if on PATH, else raises. Both Gemini backends are opt-in only.\n     \"\"\"\n     import shutil as _shutil\n \n",
  "head_spans": [
   {
    "start_line": 79,
    "end_line": 88,
    "kind": "function",
    "qualified_name": "_configure_logging",
    "depth": 0
   },
   {
    "start_line": 91,
    "end_line": 91,
    "kind": "constant",
    "qualified_name": "_DEFAULT_GEMINI_API_MODEL",
    "depth": 0
   },
   {
    "start_line": 94,
    "end_line": 187,
    "kind": "function",
    "qualified_name": "_select_client",
    "depth": 0
   }
  ],
  "base_spans": [
   {
    "start_line": 79,
    "end_line": 88,
    "kind": "function",
    "qualified_name": "_configure_logging",
    "depth": 0
   },
   {
    "start_line": 91,
    "end_line": 176,
    "kind": "function",
    "qualified_name": "_select_client",
    "depth": 0
   }
  ],
  "expect_runs": [
   {
    "kind": "ins",
    "start_idx": 6,
    "texts": [
     "def _select_client(backend: str, *, model: str = \"claude-opus-4-7\"):",
     "    \"\"\"Pick a backend handle based on env + explicit choice.",
     "",
     "    SDK backends (`api`, `gemini-api`) return an `SDKBackend` carrying",
     "    a fully-qualified pydantic-ai model id; CLI backends return their",
     "    existing subprocess client. The pipeline branches on",
     "    `isinstance(client, SDKBackend)`.",
     ""
    ]
   },
   {
    "kind": "del",
    "start_idx": 17,
    "texts": [
     "    by auto, since their output quality and cost profile are different",
     "    enough that we don't want to surprise users with them."
    ]
   }
  ]
 },
 {
  "name": "semantic_code_review/viewer/assets/text_highlight.ts:@@ -64,51 +75,21",
  "moved": false,
  "old_start": 64,
  "old_count": 51,
  "new_start": 75,
  "new_count": 21,
  "header": "@@ -64,51 +75,21 @@ export function wordDiff(a: string, b: string): SideRanges {",
  "body": "     }\n   }\n   // Backtrack: matched tokens advance both sides; an unmatched token on\n-  // whichever side keeps the longer LCS becomes a changed range.\n-  const oldRanges: CharRange[] = [];\n-  const newRanges: CharRange[] = [];\n+  // whichever side keeps the longer LCS becomes a changed range on its\n+  // line. Sentinels (line -1) are dropped — they only steer alignment.\n+  const mark = (t: _Token, into: CharRange[][]): void => {\n+    if (t.line >= 0) into[t.line].push([t.start, t.end]);\n+  };\n   let i = 0;\n   let j = 0;\n   while (i < n && j < m) {\n-    if (A[i].text === B[j].text) {\n-      i++;\n-      j++;\n-    } else if (dp[i + 1][j] >= dp[i][j + 1]) {\n-      oldRanges.push(_span(A[i++]));\n-    } else {\n-      newRanges.push(_span(B[j++]));\n-    }\n-  }\n-  while (i < n) oldRanges.push(_span(A[i++]));\n-  while (j < m) newRanges.push(_span(B[j++]));\n-  return { oldRanges, newRanges };\n-}\n-\n-/** Changed character ranges between a deleted line `a` and an inserted\n- *  line `b`, computed as the gap left after stripping the common prefix\n- *  and common suffix — one contiguous span per side. Used directly as the\n- *  long-line fallback for `wordDiff`; on a single-edit line the two agree.\n- *\n- *  Returns one range per side (or none, when that side is wholly within\n- *  the shared prefix/suffix — e.g. a pure insertion leaves `oldRanges`\n- *  empty). */\n-export function charDiff(a: string, b: string): SideRanges {\n-  const shorter = Math.min(a.length, b.length);\n-  let prefix = 0;\n-  while (prefix < shorter && a[prefix] === b[prefix]) prefix++;\n-  let suffix = 0;\n-  while (\n-    suffix < shorter - prefix &&\n-    a[a.length - 1 - suffix] === b[b.length - 1 - suffix]\n-  ) {\n-    suffix++;\n+    if (A[i].text === B[j].text) { i++; j++; }\n+    else if (dp[i + 1][j] >= dp[i][j + 1]) mark(A[i++], old);\n+    else mark(B[j++], neu);\n   }\n-  const aEnd = a.length - suffix;\n-  const bEnd = b.length - suffix;\n-  return {\n-    oldRanges: prefix < aEnd ? [[prefix, aEnd]] : [],\n-    newRanges: prefix < bEnd ? [[prefix, bEnd]] : [],\n-  };\n+  while (i < n) mark(A[i++], old);\n+  while (j < m) mark(B[j++], neu);\n+  return { old, new: neu };\n }\n \n /** Whole-identifier occurrences of `term` in `text`, as character ranges.\n",
  "head_spans": [
   {
    "start_line": 59,
    "end_line": 93,
    "kind": "function",
    "qualified_name": "blockDiff",
    "depth": 0
   },
   {
    "start_line": 80,
    "end_line": 82,
    "kind": "function",
    "qualified_name": "blockDiff.mark",
    "depth": 1
   },
   {
    "start_line": 101,
    "end_line": 111,
    "kind": "function",
    "qualified_name": "matchRanges",
    "depth": 0
   }
  ],
  "base_spans": [
   {
    "start_line": 49,
    "end_line": 85,
    "kind": "function",
    "qualified_name": "wordDiff",
    "depth": 0
   },
   {
    "start_line": 95,
    "end_line": 112,
    "kind": "function",
    "qualified_name": "charDiff",
    "depth": 0
   }
  ],
  "expect_runs": null
 },
 {
  "name": "semantic_code_review/viewer/assets/comments.ts:@@ -66,68 +78,11",
  "moved": true,
  "old_start": 66,
  "old_count": 68,
  "new_start": 78,
  "new_count": 11,
  "header": "@@ -66,68 +78,11 @@ function renderAll(): void {",
  "body": "   });\n }\n \n-// --- Storage (server-mediated, localStorage fallback) -------------------\n-\n-function _storageLoad(): void {\n-  const endpoint = _sessionEndpoint();\n-  if (endpoint !== null) {\n-    fetch(`${endpoint}/comments`)\n-      .then((r) => (r.ok ? r.json() : { comments: [] as ReviewerComment[] }))\n-      .then((d: { comments?: ReviewerComment[] }) => {\n-        for (const c of d.comments || []) _comments[c.id] = c;\n-        renderAll();\n-      })\n-      .catch(() => { /* server may have exited; ignore */ });\n-    return;\n-  }\n-  try {\n-    const raw = localStorage.getItem(_lsKey);\n-    if (!raw) return;\n-    const data = JSON.parse(raw) as { comments?: ReviewerComment[] };\n-    for (const c of data.comments || []) _comments[c.id] = c;\n-    renderAll();\n-  } catch (_) { /* ignore */ }\n-}\n-\n-function _storageFlush(): void {\n-  if (_sessionEndpoint() !== null) return;  // server round-trips per-mutation\n-  const payload = { comments: Object.values(_comments) };\n-  try { localStorage.setItem(_lsKey, JSON.stringify(payload)); } catch (_) { /* ignore */ }\n-}\n-\n-function _save(c: ReviewerComment): Promise<ReviewerComment | null> {\n-  _comments[c.id] = c;\n-  const endpoint = _sessionEndpoint();\n-  if (endpoint !== null) {\n-    return fetch(`${endpoint}/comments`, {\n-      method: \"POST\",\n-      headers: { \"Content-Type\": \"application/json\" },\n-      body: JSON.stringify(c),\n-    })\n-      .then((r) => (r.ok ? r.json() as Promise<ReviewerComment> : null))\n-      .catch(() => null);\n-  }\n-  _storageFlush();\n-  return Promise.resolve(c);\n-}\n-\n-function _delete(id: string): Promise<void> {\n-  delete _comments[id];\n-  const endpoint = _sessionEndpoint();\n-  if (endpoint !== null) {\n-    return fetch(`${endpoint}/comments/${encodeURIComponent(id)}`, {\n-      method: \"DELETE\",\n-    }).then(() => undefined).catch(() => undefined);\n-  }\n-  _storageFlush();\n-  return Promise.resolve();\n-}\n-\n-// --- Anchor key + lookup ------------------------------------------------\n+// --- Anchor lookup ------------------------------------------------------\n \n function _commentsFor(file: string, side: \"old\" | \"new\", line: number): ReviewerComment[] {\n   const k = `${file}|${side}|${line}`;\n-  return Object.values(_comments).filter(\n+  return _store.getAll().filter(\n     (c) => `${c.file}|${c.side}|${c.line}` === k,\n   );\n }\n",
  "head_spans": [
   {
    "start_line": 55,
    "end_line": 79,
    "kind": "function",
    "qualified_name": "renderAll",
    "depth": 0
   },
   {
    "start_line": 83,
    "end_line": 88,
    "kind": "function",
    "qualified_name": "_commentsFor",
    "depth": 0
   },
   {
    "start_line": 92,
    "end_line": 110,
    "kind": "function",
    "qualified_name": "_installGutter",
    "depth": 0
   },
   {
    "start_line": 114,
    "end_line": 120,
    "kind": "interface",
    "qualified_name": "EditorOpts",
    "depth": 0
   },
   {
    "start_line": 122,
    "end_line": 182,
    "kind": "function",
    "qualified_name": "_openEditor",
    "depth": 0
   }
  ],
  "base_spans": [
   {
    "start_line": 44,
    "end_line": 67,
    "kind": "function",
    "qualified_name": "renderAll",
    "depth": 0
   },
   {
    "start_line": 71,
    "end_line": 90,
    "kind": "function",
    "qualified_name": "_storageLoad",
    "depth": 0
   },
   {
    "start_line": 92,
    "end_line": 96,
    "kind": "function",
    "qualified_name": "_storageFlush",
    "depth": 0
   },
   {
    "start_line": 98,
    "end_line": 112,
    "kind": "function",
    "qualified_name": "_save",
    "depth": 0
   },
   {
    "start_line": 114,
    "end_line": 124,
    "kind": "function",
    "qualified_name": "_delete",
    "depth": 0
   },
   {
    "start_line": 128,
    "end_line": 133,
    "kind": "function",
    "qualified_name": "_commentsFor",
    "depth": 0
   }
  ],
  "expect_runs": [
   {
    "kind": "del",
    "start_idx": 5,
    "texts": [
     "function _storageLoad(): void {",
     "  const endpoint = _sessionEndpoint();",
     "  if (endpoint !== null) {",
     "    fetch(`${endpoint}/comments`)",
     "      .then((r) => (r.ok ? r.json() : { comments: [] as ReviewerComment[] }))",
     "      .then((d: { comments?: ReviewerComment[] }) => {",
     "        for (const c of d.comments || []) _comments[c.id] = c;",
     "        renderAll();",
     "      })",
     "      .catch(() => { /* server may have exited; ignore */ });",
     "    return;",
     "  }",
     "  try {",
     "    const raw = localStorage.getItem(_lsKey);",
     "    if (!raw) return;",
     "    const data = JSON.parse(raw) as { comments?: ReviewerComment[] };",
     "    for (const c of data.comments || []) _comments[c.id] = c;",
     "    renderAll();",
     "  } catch (_) { /* ignore */ }",
     "}",
     "",
     "function _storageFlush(): void {",
     "  if (_sessionEndpoint() !== null) return;  // server round-trips per-mutation",
     "  const payload = { comments: Object.values(_comments) };",
     "  try { localStorage.setItem(_lsKey, JSON.stringify(payload)); } catch (_) { /* ignore */ }",
     "}",
     "",
     "function _save(c: ReviewerComment): Promise<ReviewerComment | null> {",
     "  _comments[c.id] = c;",
     "  const endpoint = _sessionEndpoint();",
     "  if (endpoint !== null) {",
     "    return fetch(`${endpoint}/comments`, {",
     "      method: \"POST\",",
     "      headers: { \"Content-Type\": \"application/json\" },",
     "      body: JSON.stringify(c),",
     "    })",
     "      .then((r) => (r.ok ? r.json() as Promise<ReviewerComment> : null))",
     "      .catch(() => null);",
     "  }",
     "  _storageFlush();",
     "  return Promise.resolve(c);",
     "}",
     "",
     "function _delete(id: string): Promise<void> {",
     "  delete _comments[id];",
     "  const endpoint = _sessionEndpoint();",
     "  if (endpoint !== null) {",
     "    return fetch(`${endpoint}/comments/${encodeURIComponent(id)}`, {",
     "      method: \"DELETE\",",
     "    }).then(() => undefined).catch(() => undefined);",
     "  }",
     "  _storageFlush();",
     "  return Promise.resolve();",
     "}",
     "",
     "// --- Anchor key + lookup ------------------------------------------------",
     ""
    ]
   }
  ]
 },
 {
  "name": "semantic_code_review/review/server.py:@@ -58,30 +58,46",
  "moved": false,
  "old_start": 58,
  "old_count": 30,
  "new_start": 58,
  "new_count": 46,
  "header": "@@ -58,30 +58,46 @@ def _parse_hunk_id(hunk_id: str) -> tuple[int, int]:",
  "body": " \n \n def _update_viewer_json_fold(\n-    viewer_json: dict[str, Any], fi: int, hi: int,\n-    side: str, start: int, count: int, summary: str,\n+    viewer_json: dict[str, Any], payload: dict[str, Any],\n ) -> None:\n     \"\"\"Patch the matching fold_regions[i].summary in the viewer JSON\n-    so a fresh `/data.json` fetch sees the result.\"\"\"\n+    so a fresh `/data.json` fetch sees the result.\n+\n+    Walks every hunk in the addressed file looking for a region whose\n+    (context, right_start/end, left_start/end) match — the regions are\n+    addressed at the file level by the v2 protocol but still attached\n+    to individual hunks in the per-hunk fold_regions block.\n+    \"\"\"\n     files = viewer_json.get(\"files\") or []\n-    if fi >= len(files):\n-        return\n-    hunks = files[fi].get(\"hunks\") or []\n-    if hi >= len(hunks):\n+    fi = payload.get(\"file_idx\")\n+    if fi is None or fi >= len(files):\n         return\n-    end = start + count - 1\n-    if side == \"old\":\n-        start_key, end_key = \"old_start\", \"old_end\"\n-    else:\n-        start_key, end_key = \"new_start\", \"new_end\"\n-    for reg in hunks[hi].get(\"fold_regions\") or []:\n-        if (\n-            reg.get(\"side\") == side\n-            and reg.get(start_key) == start\n-            and reg.get(end_key) == end\n-        ):\n-            reg[\"summary\"] = summary\n-            return\n+    context = payload.get(\"context\")\n+    rs = payload.get(\"right_start\", 0); re_ = payload.get(\"right_end\", 0)\n+    ls = payload.get(\"left_start\", 0); le = payload.get(\"left_end\", 0)\n+    summary = payload.get(\"summary\", \"\")\n+    for hunk in files[fi].get(\"hunks\") or []:\n+        for reg in hunk.get(\"fold_regions\") or []:\n+            if (\n+                reg.get(\"context\") == context\n+                and (reg.get(\"right_start\") or 0) == rs\n+                and (reg.get(\"right_end\") or 0) == re_\n+                and (reg.get(\"left_start\") or 0) == ls\n+                and (reg.get(\"left_end\") or 0) == le\n+            ):\n+                reg[\"summary\"] = summary\n+                return\n+\n+\n+def _range_from_payload(payload: dict[str, Any], side: str) -> tuple[int, int] | None:\n+    \"\"\"Pull (start, end) for a side out of the request payload, or None\n+    if the keys aren't both present + parsable.\"\"\"\n+    try:\n+        s = int(payload[f\"{side}_start\"])\n+        e = int(payload[f\"{side}_end\"])\n+    except (KeyError, TypeError, ValueError):\n+        return None\n+    return (s, e)\n \n \n def _ctx_publish(ctx: ServerContext, event_type: str, payload: dict[str, Any]) -> None:\n",
  "head_spans": [
   {
    "start_line": 49,
    "end_line": 57,
    "kind": "function",
    "qualified_name": "_parse_hunk_id",
    "depth": 0
   },
   {
    "start_line": 60,
    "end_line": 89,
    "kind": "function",
    "qualified_name": "_update_viewer_json_fold",
    "depth": 0
   },
   {
    "start_line": 92,
    "end_line": 100,
    "kind": "function",
    "qualified_name": "_range_from_payload",
    "depth": 0
   },
   {
    "start_line": 103,
    "end_line": 120,
    "kind": "function",
    "qualified_name": "_ctx_publish",
    "depth": 0
   }
  ],
  "base_spans": [
   {
    "start_line": 49,
    "end_line": 57,
    "kind": "function",
    "qualified_name": "_parse_hunk_id",
    "depth": 0
   },
   {
    "start_line": 60,
    "end_line": 84,
    "kind": "function",
    "qualified_name": "_update_viewer_json_fold",
    "depth": 0
   },
   {
    "start_line": 87,
    "end_line": 104,
    "kind": "function",
    "qualified_name": "_ctx_publish",
    "depth": 0
   }
  ],
  "expect_runs": null
 },
 {
  "name": "semantic_code_review/viewer/assets/viewer.js:@@ -134,87 +168,143",
  "moved": false,
  "old_start": 134,
  "old_count": 87,
  "new_start": 168,
  "new_count": 143,
  "header": "@@ -134,87 +168,143 @@",
  "body": "     });\n   }\n \n-  // Build the sidebar listing each semantic group plus a \"Show all\"\n-  // button. Hidden entirely when the overview pass emitted no groups.\n+  // Build the sidebar's axis sections. Each axis with non-empty\n+  // groups gets its own labelled section + pill row. A single\n+  // \"Show all\" sits at the top and clears the active pill across\n+  // every axis. The themes axis goes first because its pills are\n+  // the most semantically dense; files follows for structural nav.\n   function renderGroupSidebar() {\n     const sidebar = document.getElementById(\"group-sidebar\");\n     if (!sidebar) return;\n     sidebar.innerHTML = \"\";\n-    if (!GROUPS.length) {\n+    const populated = AXES.filter((a) => a.groups.length > 0);\n+    if (populated.length === 0) {\n       sidebar.classList.add(\"empty\");\n       return;\n     }\n     sidebar.classList.remove(\"empty\");\n-    const header = el(\"div\", \"group-sidebar-header\");\n-    header.appendChild(el(\"h3\", null, \"Groups\"));\n-    sidebar.appendChild(header);\n \n     const showAll = el(\"button\", \"group-btn group-btn-all\", \"Show all\");\n     showAll.title = \"Clear filter — show every hunk\";\n-    if (STATE.activeGroup === null) showAll.classList.add(\"active\");\n-    showAll.addEventListener(\"click\", () => setActiveGroup(null));\n+    if (STATE.activePill === null) showAll.classList.add(\"active\");\n+    showAll.addEventListener(\"click\", () => setActivePill(null));\n     sidebar.appendChild(showAll);\n \n-    for (const g of GROUPS) {\n-      const btn = el(\"button\", \"group-btn\");\n-      const label = el(\"span\", \"group-btn-label\", g.title);\n-      const count = el(\"span\", \"group-btn-count\", String((g.hunk_ids || []).length));\n-      btn.appendChild(label);\n-      btn.appendChild(count);\n-      btn.title = g.rationale || g.title;\n-      if (STATE.activeGroup === g.id) btn.classList.add(\"active\");\n-      btn.addEventListener(\"click\", () => {\n-        setActiveGroup(STATE.activeGroup === g.id ? null : g.id);\n-      });\n-      sidebar.appendChild(btn);\n+    for (const axis of populated) {\n+      const section = el(\"div\", \"group-axis\");\n+      section.dataset.axis = axis.id;\n+      const header = el(\"div\", \"group-axis-header\");\n+      header.appendChild(el(\"h3\", null, axis.label));\n+      section.appendChild(header);\n+      for (const g of axis.groups) {\n+        const btn = el(\"button\", \"group-btn\");\n+        btn.dataset.axis = axis.id;\n+        btn.dataset.pillId = g.id;\n+        btn.appendChild(el(\"span\", \"group-btn-label\", g.title));\n+        btn.appendChild(el(\"span\", \"group-btn-count\", String((g.hunk_ids || []).length)));\n+        if (g.rationale) btn.title = g.rationale;\n+        if (isActivePill(axis.id, g.id)) btn.classList.add(\"active\");\n+        btn.addEventListener(\"click\", () => {\n+          setActivePill(\n+            isActivePill(axis.id, g.id) ? null : { axis: axis.id, id: g.id },\n+          );\n+        });\n+        section.appendChild(btn);\n+      }\n+      sidebar.appendChild(section);\n     }\n   }\n \n-  function setActiveGroup(groupId) {\n-    STATE.activeGroup = groupId;\n+  function isActivePill(axisId, pillId) {\n+    return STATE.activePill !== null\n+      && STATE.activePill.axis === axisId\n+      && STATE.activePill.id === pillId;\n+  }\n+\n+  function setActivePill(pill) {\n+    STATE.activePill = pill;\n     try {\n-      if (groupId === null) localStorage.removeItem(GROUP_LS_KEY);\n-      else localStorage.setItem(GROUP_LS_KEY, groupId);\n+      if (pill === null) localStorage.removeItem(GROUP_LS_KEY);\n+      else localStorage.setItem(GROUP_LS_KEY, `${pill.axis}:${pill.id}`);\n     } catch (_) { /* ignore */ }\n-    // Refresh button active-state.\n-    document.querySelectorAll(\".group-btn\").forEach(b => b.classList.remove(\"active\"));\n-    if (groupId === null) {\n+    document.querySelectorAll(\".group-btn\").forEach((b) => b.classList.remove(\"active\"));\n+    if (pill === null) {\n       const all = document.querySelector(\".group-btn-all\");\n       if (all) all.classList.add(\"active\");\n     } else {\n-      const btns = document.querySelectorAll(\".group-btn\");\n-      const gi = GROUPS.findIndex(g => g.id === groupId);\n-      if (gi >= 0 && btns[gi + 1]) btns[gi + 1].classList.add(\"active\");\n+      const sel = `.group-btn[data-axis=\"${pill.axis}\"][data-pill-id=\"${pill.id}\"]`;\n+      const btn = document.querySelector(sel);\n+      if (btn) btn.classList.add(\"active\");\n     }\n     applyGroupFilter();\n-    // Arrow geometry changes when rows appear/disappear.\n     window.ScrAnnotations.reflowAll();\n   }\n \n-  // Walk every .hunk element, tag .ungrouped for hunks no group claims,\n-  // and apply visibility based on STATE.activeGroup. Files with no\n-  // visible hunks are hidden too so the sidebar view reads cleanly.\n+  function activePillHunkIds() {\n+    if (STATE.activePill === null) return null;\n+    const axis = AXES.find((a) => a.id === STATE.activePill.axis);\n+    if (!axis) return null;\n+    const g = axis.byId[STATE.activePill.id];\n+    return g ? new Set(g.hunk_ids || []) : new Set();\n+  }\n+\n+  // Walk every .hunk element, tag .ungrouped for hunks no themes-axis\n+  // group claims (the file axis always covers every hunk so it's not\n+  // a useful \"ungrouped\" signal), and apply visibility based on the\n+  // currently-active pill. Files with no visible hunks are hidden too\n+  // so the sidebar view reads cleanly.\n   function applyGroupFilter() {\n-    const activeIds = STATE.activeGroup\n-      ? new Set(GROUP_BY_ID[STATE.activeGroup].hunk_ids || [])\n-      : null;\n-    document.querySelectorAll(\".file\").forEach(fileEl => {\n+    const activeIds = activePillHunkIds();\n+    document.querySelectorAll(\".file\").forEach((fileEl) => {\n       let visible = 0;\n-      fileEl.querySelectorAll(\".hunk\").forEach(hunkEl => {\n+      fileEl.querySelectorAll(\".hunk\").forEach((hunkEl) => {\n         const hid = hunkEl.dataset.id;\n         const inAnyGroup = (HUNK_GROUP_COUNT[hid] || 0) > 0;\n         hunkEl.classList.toggle(\"ungrouped\", !inAnyGroup);\n         const show = activeIds === null ? true : activeIds.has(hid);\n         hunkEl.style.display = show ? \"\" : \"none\";\n-        // Also hide / show the gap chips immediately surrounding this\n-        // hunk — if the hunk is hidden there's no reason to offer\n-        // \"expand 3 lines above\" above a gap between two hidden hunks.\n         if (show) visible++;\n       });\n       fileEl.style.display = visible === 0 && activeIds !== null ? \"none\" : \"\";\n     });\n   }\n \n+  // Build the by-file axis from DATA.files. One pill per file with\n+  // hunks, label = path (basename if path is deep), count = hunks\n+  // count. Skipped files (status = generated / binary / deleted with\n+  // no diff body) still get a pill — the reviewer might want to jump\n+  // to them. Re-buildable in place via `rebuildFilesAxis()`.\n+  function rebuildFilesAxis() {\n+    FILES_AXIS.groups.length = 0;\n+    for (const k of Object.keys(FILES_AXIS.byId)) delete FILES_AXIS.byId[k];\n+    for (const k of Object.keys(FILES_AXIS.hunkCount)) delete FILES_AXIS.hunkCount[k];\n+    for (let fi = 0; fi < (DATA.files || []).length; fi++) {\n+      const f = DATA.files[fi];\n+      if (!f.hunks || f.hunks.length === 0) continue;\n+      const hunk_ids = f.hunks.map((h) => h.id);\n+      const g = {\n+        id: `BF${fi}`,\n+        title: shortenPath(f.path),\n+        rationale: f.path,\n+        hunk_ids,\n+      };\n+      FILES_AXIS.groups.push(g);\n+      FILES_AXIS.byId[g.id] = g;\n+      for (const hid of hunk_ids) {\n+        FILES_AXIS.hunkCount[hid] = (FILES_AXIS.hunkCount[hid] || 0) + 1;\n+      }\n+    }\n+  }\n+\n+  function shortenPath(path) {\n+    // Long paths overflow the sidebar; show the basename, fall back\n+    // to the full path if it's already short enough.\n+    if (!path) return \"\";\n+    if (path.length <= 28) return path;\n+    const idx = path.lastIndexOf(\"/\");\n+    return idx >= 0 ? path.slice(idx + 1) : path;\n+  }\n+\n   function renderPRPanel(pr) {\n     const panel = el(\"section\", \"pr-panel\");\n     panel.appendChild(el(\"h2\", null, \"PR summary\"));\n",
  "head_spans": [
   {
    "start_line": 121,
    "end_line": 137,
    "kind": "function",
    "qualified_name": "chev",
    "depth": 0
   },
   {
    "start_line": 139,
    "end_line": 145,
    "kind": "function",
    "qualified_name": "smellPill",
    "depth": 0
   },
   {
    "start_line": 148,
    "end_line": 169,
    "kind": "function",
    "qualified_name": "render",
    "depth": 0
   },
   {
    "start_line": 176,
    "end_line": 216,
    "kind": "function",
    "qualified_name": "renderGroupSidebar",
    "depth": 0
   },
   {
    "start_line": 218,
    "end_line": 222,
    "kind": "function",
    "qualified_name": "isActivePill",
    "depth": 0
   },
   {
    "start_line": 224,
    "end_line": 241,
    "kind": "function",
    "qualified_name": "setActivePill",
    "depth": 0
   },
   {
    "start_line": 243,
    "end_line": 249,
    "kind": "function",
    "qualified_name": "activePillHunkIds",
    "depth": 0
   },
   {
    "start_line": 256,
    "end_line": 270,
    "kind": "function",
    "qualified_name": "applyGroupFilter",
    "depth": 0
   },
   {
    "start_line": 277,
    "end_line": 297,
    "kind": "function",
    "qualified_name": "rebuildFilesAxis",
    "depth": 0
   },
   {
    "start_line": 299,
    "end_line": 306,
    "kind": "function",
    "qualified_name": "shortenPath",
    "depth": 0
   },
   {
    "start_line": 308,
    "end_line": 318,
    "kind": "function",
    "qualified_name": "renderPRPanel",
    "depth": 0
   }
  ],
  "base_spans": [
   {
    "start_line": 114,
    "end_line": 135,
    "kind": "function",
    "qualified_name": "render",
    "depth": 0
   },
   {
    "start_line": 139,
    "end_line": 171,
    "kind": "function",
    "qualified_name": "renderGroupSidebar",
    "depth": 0
   },
   {
    "start_line": 173,
    "end_line": 192,
    "kind": "function",
    "qualified_name": "setActiveGroup",
    "depth": 0
   },
   {
    "start_line": 197,
    "end_line": 216,
    "kind": "function",
    "qualified_name": "applyGroupFilter",
    "depth": 0
   },
   {
    "start_line": 218,
    "end_line": 228,
    "kind": "function",
    "qualified_name": "renderPRPanel",
    "depth": 0
   },
   {
    "start_line": 230,
    "end_line": 250,
    "kind": "function",
    "qualified_name": "renderFile",
    "depth": 0
   },
   {
    "start_line": 254,
    "end_line": 264,
    "kind": "function",
    "qualified_name": "gapBeforeFirstHunk",
    "depth": 0
   },
   {
    "start_line": 266,
    "end_line": 288,
    "kind": "function",
    "qualified_name": "gapAfterHunk",
    "depth": 0
   },
   {
    "start_line": 290,
    "end_line": 304,
    "kind": "function",
    "qualified_name": "renderGapChip",
    "depth": 0
   },
   {
    "start_line": 306,
    "end_line": 340,
    "kind": "function",
    "qualified_name": "renderGapExpansion",
    "depth": 0
   }
  ],
  "expect_runs": null
 },
 {
  "name": "semantic_code_review/augment/pipeline.py:@@ -292,6 +319,67",
  "moved": false,
  "old_start": 292,
  "old_count": 6,
  "new_start": 319,
  "new_count": 67,
  "header": "@@ -292,6 +319,67 @@ async def _augment_one_hunk(",
  "body": "                 \"hunk %s @ %s failed: %s: %s\",\n                 fp.path, hunk.parsed.header, type(e).__name__, e,\n             )\n+            _safe_emit(on_event, \"hunk\", {\n+                \"file_idx\": fi, \"hunk_idx\": hi, \"ok\": False,\n+                \"error\": f\"{type(e).__name__}: {e}\",\n+            })\n+\n+\n+def _overview_event_payload(diff: AnnotatedDiff) -> dict[str, Any]:\n+    \"\"\"Build the `overview` SSE payload from the post-overview diff.\n+\n+    Carries the PR-level fields the viewer wants to update (summary,\n+    themes, symbols, callgraph), the semantic groups the sidebar\n+    filters by, and per-file summaries/symbols that show up in the\n+    file header. Mirrors the relevant slices of `build_viewer_json`'s\n+    output so the viewer can patch in place without re-fetching\n+    `data.json`.\n+    \"\"\"\n+    ov = diff.overview if isinstance(diff.overview, Overview) else None\n+    path_to_file_idx = {fp.path: i for i, fp in enumerate(diff.files)}\n+    groups: list[dict[str, Any]] = []\n+    if ov is not None:\n+        for gi, g in enumerate(ov.groups):\n+            hunk_ids: list[str] = []\n+            for m in g.members:\n+                fi = path_to_file_idx.get(m.path)\n+                if fi is None:\n+                    continue\n+                hunk_ids.append(f\"H{fi}_{m.hunk_index}\")\n+            if not hunk_ids:\n+                continue\n+            groups.append({\n+                \"id\": f\"G{gi}\", \"title\": g.title,\n+                \"rationale\": g.rationale, \"hunk_ids\": hunk_ids,\n+            })\n+    file_patches = [\n+        {\n+            \"file_idx\": i,\n+            \"path\": fp.path,\n+            \"summary\": fp.ann.summary,\n+            \"language\": fp.ann.lang or \"\",\n+            \"symbols\": (\n+                fp.ann.symbols.model_dump() if fp.ann.symbols\n+                else {\"added\": [], \"modified\": [], \"removed\": []}\n+            ),\n+            \"status\": fp.ann.role.value if fp.ann.role else \"modified\",\n+        }\n+        for i, fp in enumerate(diff.files)\n+    ]\n+    return {\n+        \"pr\": {\n+            \"summary\": ov.summary if ov else \"\",\n+            \"themes\": ov.themes if ov else [],\n+            \"symbols_added\": [s.model_dump() for s in (ov.symbols_added if ov else [])],\n+            \"symbols_modified\": [s.model_dump() for s in (ov.symbols_modified if ov else [])],\n+            \"symbols_removed\": [s.model_dump() for s in (ov.symbols_removed if ov else [])],\n+            \"callgraph_edges\": [\n+                e.model_dump(by_alias=True) for e in (ov.callgraph_edges if ov else [])\n+            ],\n+        },\n+        \"groups\": groups,\n+        \"files\": file_patches,\n+    }\n \n \n def _merge_hunk_results(\n",
  "head_spans": [
   {
    "start_line": 264,
    "end_line": 325,
    "kind": "function",
    "qualified_name": "_augment_one_hunk",
    "depth": 0
   },
   {
    "start_line": 328,
    "end_line": 382,
    "kind": "function",
    "qualified_name": "_overview_event_payload",
    "depth": 0
   },
   {
    "start_line": 385,
    "end_line": 401,
    "kind": "function",
    "qualified_name": "_merge_hunk_results",
    "depth": 0
   }
  ],
  "base_spans": [
   {
    "start_line": 244,
    "end_line": 294,
    "kind": "function",
    "qualified_name": "_augment_one_hunk",
    "depth": 0
   },
   {
    "start_line": 297,
    "end_line": 313,
    "kind": "function",
    "qualified_name": "_merge_hunk_results",
    "depth": 0
   },
   {
    "start_line": 316,
    "end_line": 328,
    "kind": "function",
    "qualified_name": "_attach_file_log",
    "depth": 0
   },
   {
    "start_line": 331,
    "end_line": 331,
    "kind": "constant",
    "qualified_name": "__all__",
    "depth": 0
   }
  ],
  "expect_runs": null
 },
 {
  "name": "semantic_code_review/viewer/assets/viewer.js:@@ -1072,6 +1086,110",
  "moved": false,
  "old_start": 1072,
  "old_count": 6,
  "new_start": 1086,
  "new_count": 110,
  "header": "@@ -1072,6 +1086,110 @@",
  "body": "     restoreHash();\n     render();\n     commentStorageLoad();\n+    installSessionEvents();\n+  }\n+\n+  // Subscribe to the server's SSE channel and patch the page as\n+  // overview / per-hunk events arrive from the augment pipeline.\n+  // Events:\n+  //   - overview: PR-level summary, themes, semantic groups, and\n+  //               per-file summaries land in DATA; the sidebar and\n+  //               file headers re-render.\n+  //   - hunk:     one hunk's block (the same shape as DATA.files[].hunks[i])\n+  //               replaces its slot in DATA and the corresponding\n+  //               .hunk DOM node, dropping the \"analysing…\" placeholder.\n+  //   - done:     augmentation finished; any hunks still pending get\n+  //               the \"(no intent — may need re-run)\" copy. The SSE\n+  //               connection is closed.\n+  function installSessionEvents() {\n+    if (!SESSION_ENDPOINT || typeof EventSource === \"undefined\") return;\n+    let es;\n+    try {\n+      es = new EventSource(SESSION_ENDPOINT + \"/events\");\n+    } catch (_) {\n+      return;\n+    }\n+    es.addEventListener(\"overview\", (e) => {\n+      try { applyOverviewPatch(JSON.parse(e.data)); } catch (_) { /* ignore */ }\n+    });\n+    es.addEventListener(\"hunk\", (e) => {\n+      try { applyHunkPatch(JSON.parse(e.data)); } catch (_) { /* ignore */ }\n+    });\n+    es.addEventListener(\"done\", () => {\n+      finaliseStreaming();\n+      try { es.close(); } catch (_) { /* ignore */ }\n+    });\n+  }\n+\n+  function applyHunkPatch(payload) {\n+    const fi = payload.file_idx;\n+    const hi = payload.hunk_idx;\n+    if (!DATA.files || !DATA.files[fi]) return;\n+    const file = DATA.files[fi];\n+    if (!file.hunks || !file.hunks[hi]) return;\n+    if (payload.ok && payload.block) {\n+      file.hunks[hi] = payload.block;\n+    } else {\n+      // Failure: mark the slot so finaliseStreaming() / renderHunk\n+      // show the re-run copy instead of the pending spinner.\n+      file.hunks[hi].intent = \"\";\n+      file.hunks[hi]._failed = true;\n+    }\n+    // Re-render the single hunk in place. STATE.renderedDiffs caches\n+    // by hunk id; drop the cached entry so the new annotations and\n+    // (possibly different) fold regions get fresh DOM.\n+    delete STATE.renderedDiffs[file.hunks[hi].id];\n+    const fresh = renderHunk(file.hunks[hi], file);\n+    const existing = document.querySelector('.hunk[data-id=\"' + cssEscape(file.hunks[hi].id) + '\"]');\n+    if (existing && existing.parentNode) {\n+      existing.parentNode.replaceChild(fresh, existing);\n+    }\n+  }\n+\n+  function applyOverviewPatch(payload) {\n+    if (payload.pr) Object.assign(DATA.pr || (DATA.pr = {}), payload.pr);\n+    if (Array.isArray(payload.files)) {\n+      for (const fp of payload.files) {\n+        const f = DATA.files && DATA.files[fp.file_idx];\n+        if (!f) continue;\n+        if (fp.summary !== undefined) f.summary = fp.summary;\n+        if (fp.language) f.language = fp.language;\n+        if (fp.symbols) f.symbols = fp.symbols;\n+        if (fp.status) f.status = fp.status;\n+      }\n+    }\n+    if (Array.isArray(payload.groups)) {\n+      DATA.groups = payload.groups;\n+      // Rebuild the lookup tables the sidebar reads from.\n+      for (const k of Object.keys(GROUP_BY_ID)) delete GROUP_BY_ID[k];\n+      for (const k of Object.keys(HUNK_GROUP_COUNT)) delete HUNK_GROUP_COUNT[k];\n+      for (const g of DATA.groups) {\n+        GROUP_BY_ID[g.id] = g;\n+        for (const hid of g.hunk_ids || []) {\n+          HUNK_GROUP_COUNT[hid] = (HUNK_GROUP_COUNT[hid] || 0) + 1;\n+        }\n+      }\n+    }\n+    // The PR header and groups sidebar live outside the hunk list and\n+    // are cheap to redraw; a full re-render keeps the logic in one\n+    // place and avoids drift between streamed and non-streamed paths.\n+    render();\n+  }\n+\n+  function finaliseStreaming() {\n+    // Drop the pending flag so any hunks the server never sent an\n+    // event for (filtered, skipped, crashed mid-pass) render the\n+    // failure copy on the next re-render instead of the spinner.\n+    DATA.pending = false;\n+    render();\n+  }\n+\n+  // Minimal CSS.escape polyfill — only needed because some older\n+  // browsers ship without `CSS.escape`. Hunk ids are simple ASCII\n+  // identifiers, so escaping is a defensive measure.\n+  function cssEscape(s) {\n+    if (window.CSS && typeof CSS.escape === \"function\") return CSS.escape(s);\n+    return String(s).replace(/[^a-zA-Z0-9_-]/g, c => \"\\\\\" + c);\n   }\n \n   if (document.readyState === \"loading\") {\n",
  "head_spans": [
   {
    "start_line": 1070,
    "end_line": 1090,
    "kind": "function",
    "qualified_name": "boot",
    "depth": 0
   },
   {
    "start_line": 1104,
    "end_line": 1122,
    "kind": "function",
    "qualified_name": "installSessionEvents",
    "depth": 0
   },
   {
    "start_line": 1124,
    "end_line": 1147,
    "kind": "function",
    "qualified_name": "applyHunkPatch",
    "depth": 0
   },
   {
    "start_line": 1149,
    "end_line": 1177,
    "kind": "function",
    "qualified_name": "applyOverviewPatch",
    "depth": 0
   },
   {
    "start_line": 1179,
    "end_line": 1185,
    "kind": "function",
    "qualified_name": "finaliseStreaming",
    "depth": 0
   },
   {
    "start_line": 1190,
    "end_line": 1193,
    "kind": "function",
    "qualified_name": "cssEscape",
    "depth": 0
   }
  ],
  "base_spans": [
   {
    "start_line": 1056,
    "end_line": 1075,
    "kind": "function",
    "qualified_name": "boot",
    "depth": 0
   }
  ],
  "expect_runs": null
 },
 {
  "name": "semantic_code_review/augment/trace_adapter.py:@@ -168,8 +172,38",
  "moved": false,
  "old_start": 168,
  "old_count": 8,
  "new_start": 172,
  "new_count": 38,
  "header": "@@ -168,8 +172,38 @@ def write_pydantic_ai_trace(",
  "body": "     tool_names: list[str],\n     submit_tool: str,\n ) -> None:\n-    \"\"\"Render `result` (an `AgentRunResult`) into the legacy trace shape.\"\"\"\n-    messages = list(result.all_messages())\n+    \"\"\"Render an `AgentRunResult` into the legacy trace shape and write it.\"\"\"\n+    submit_args = _submit_args_from_output(getattr(result, \"output\", None))\n+    write_partial_trace(\n+        list(result.all_messages()),\n+        trace_path=trace_path,\n+        model=model,\n+        system=system,\n+        tool_names=tool_names,\n+        submit_tool=submit_tool,\n+        submit_args=submit_args,\n+    )\n+\n+\n+def write_partial_trace(\n+    messages: list[Any],\n+    *,\n+    trace_path: Path,\n+    model: str,\n+    system: str,\n+    tool_names: list[str],\n+    submit_tool: str,\n+    submit_args: dict[str, Any] | None = None,\n+    error: BaseException | None = None,\n+) -> None:\n+    \"\"\"Write a trace for a (possibly partial) message history.\n+\n+    Distinct entry point from ``write_pydantic_ai_trace`` because the\n+    caller drives ``agent.iter()`` rather than ``agent.run()`` — on a\n+    `UsageLimitExceeded` or similar mid-run failure there is no\n+    `AgentRunResult` to extract `output`/`all_messages()` from, but\n+    the partial message history is still on the `AgentRun`.\n+    \"\"\"\n     requests = [m for m in messages if isinstance(m, ModelRequest)]\n     responses = [m for m in messages if isinstance(m, ModelResponse)]\n \n",
  "head_spans": [
   {
    "start_line": 166,
    "end_line": 185,
    "kind": "function",
    "qualified_name": "write_pydantic_ai_trace",
    "depth": 0
   },
   {
    "start_line": 188,
    "end_line": 293,
    "kind": "function",
    "qualified_name": "write_partial_trace",
    "depth": 0
   }
  ],
  "base_spans": [
   {
    "start_line": 162,
    "end_line": 246,
    "kind": "function",
    "qualified_name": "write_pydantic_ai_trace",
    "depth": 0
   }
  ],
  "expect_runs": null
 },
 {
  "name": "semantic_code_review/viewer/assets/folds.ts:@@ -676,11 +686,34",
  "moved": true,
  "old_start": 676,
  "old_count": 11,
  "new_start": 686,
  "new_count": 34,
  "header": "@@ -676,11 +686,34 @@ function _attachOneFold(",
  "body": " \n   const contentCell = anchor && (anchor.children[1] as HTMLElement | undefined);\n   if (contentCell) contentCell.prepend(marker);\n-  return { marker, foldHandle };\n+  return { marker, foldHandle, manifestHandle };\n+}\n+\n+/** The manifest of what a collapsed region is holding down, hung under\n+ *  the header row the chevron sits on.\n+ *\n+ *  The header row is the one line of the span that still renders, so a\n+ *  note on it is already on screen with its own comment row; listing it\n+ *  here as well would say the same thing twice.\n+ */\n+function _attachFoldManifest(\n+  file: FileBlock, region: FoldRegion, headerRow: RowWithEls,\n+  anchor: HTMLElement, shadow: HTMLElement, notes: ManifestNote[],\n+): AnnotationHandle | null {\n+  const spanId = Visibility.codeFoldSpanId(file.id, region);\n+  const hidden = Manifest.under(file.id, spanId, notes).filter(\n+    (n) => n.line !== (n.side === \"new\" ? headerRow.new_line : headerRow.old_line),\n+  );\n+  const content = Manifest.render(hidden);\n+  if (!content) return null;\n+  return Annotations.attach({\n+    anchor, shadowAnchor: shadow, variant: \"manifest\", content,\n+  });\n }\n \n function attachFileFolds(\n-  fileEl: HTMLElement, file: FileBlock, repaint: () => void,\n+  fileEl: HTMLElement, file: FileBlock, notes: ManifestNote[],\n+  repaint: () => void,\n ): void {\n   _teardownFileFolds(file.id);\n   const fileIdx = Number(file.id.replace(\"F\", \"\"));\n",
  "head_spans": [
   {
    "start_line": 619,
    "end_line": 690,
    "kind": "function",
    "qualified_name": "_attachOneFold",
    "depth": 0
   },
   {
    "start_line": 699,
    "end_line": 712,
    "kind": "function",
    "qualified_name": "_attachFoldManifest",
    "depth": 0
   },
   {
    "start_line": 714,
    "end_line": 740,
    "kind": "function",
    "qualified_name": "attachFileFolds",
    "depth": 0
   }
  ],
  "base_spans": [
   {
    "start_line": 617,
    "end_line": 680,
    "kind": "function",
    "qualified_name": "_attachOneFold",
    "depth": 0
   },
   {
    "start_line": 682,
    "end_line": 706,
    "kind": "function",
    "qualified_name": "attachFileFolds",
    "depth": 0
   }
  ],
  "expect_runs": [
   {
    "kind": "ins",
    "start_idx": 6,
    "texts": [
     "/** The manifest of what a collapsed region is holding down, hung under",
     " *  the header row the chevron sits on.",
     " *",
     " *  The header row is the one line of the span that still renders, so a",
     " *  note on it is already on screen with its own comment row; listing it",
     " *  here as well would say the same thing twice.",
     " */",
     "function _attachFoldManifest(",
     "  file: FileBlock, region: FoldRegion, headerRow: RowWithEls,",
     "  anchor: HTMLElement, shadow: HTMLElement, notes: ManifestNote[],",
     "): AnnotationHandle | null {",
     "  const spanId = Visibility.codeFoldSpanId(file.id, region);",
     "  const hidden = Manifest.under(file.id, spanId, notes).filter(",
     "    (n) => n.line !== (n.side === \"new\" ? headerRow.new_line : headerRow.old_line),",
     "  );",
     "  const content = Manifest.render(hidden);",
     "  if (!content) return null;",
     "  return Annotations.attach({",
     "    anchor, shadowAnchor: shadow, variant: \"manifest\", content,",
     "  });",
     "}",
     ""
    ]
   }
  ]
 },
 {
  "name": "semantic_code_review/viewer/assets/render.ts:@@ -534,15 +590,33",
  "moved": false,
  "old_start": 534,
  "old_count": 15,
  "new_start": 590,
  "new_count": 33,
  "header": "@@ -534,15 +590,33 @@ function _rowExtent(rows: RowBlock[], attr: \"new_line\" | \"old_line\"): LineRange",
  "body": " }\n \n /** A region renders as a chip while its span is hidden and as the diff\n- *  itself once the reviewer removes it. */\n+ *  itself once the reviewer removes it. Appends rather than returns\n+ *  because a chip carrying a manifest is two elements, not one. */\n function _renderRegion(\n-  f: FileBlock, region: DiffRegion, headed: Set<string>,\n-): HTMLElement {\n+  body: HTMLElement, f: FileBlock, region: DiffRegion, headed: Set<string>,\n+  notes: ManifestNote[],\n+): void {\n   const span = _regionSpan(f, region);\n   Visibility.seed(span);\n-  return Visibility.isHidden(f.id, span.id)\n-    ? _renderRegionChip(region, span)\n-    : _renderRegionExpansion(f, region, span, headed);\n+  if (!Visibility.isHidden(f.id, span.id)) {\n+    body.appendChild(_renderRegionExpansion(f, region, span, headed));\n+    return;\n+  }\n+  body.appendChild(_withManifest(\n+    _renderRegionChip(region, span),\n+    Manifest.under(f.id, span.id, notes),\n+  ));\n+}\n+\n+/** Hang a manifest under a one-line band (a gap chip, loaded or not).\n+ *  The band is its own click target, so the entries wrap onto their own\n+ *  line rather than sitting inside its centred flex row. */\n+function _withManifest(band: HTMLElement, entries: ManifestNote[]): HTMLElement {\n+  const manifest = Manifest.render(entries);\n+  if (!manifest) return band;\n+  band.classList.add(\"has-manifest\");\n+  band.appendChild(manifest);\n+  return band;\n }\n \n function _renderRegionChip(region: DiffRegion, span: HiddenSpan): HTMLElement {\n",
  "head_spans": [
   {
    "start_line": 532,
    "end_line": 536,
    "kind": "interface",
    "qualified_name": "DiffRegion",
    "depth": 0
   },
   {
    "start_line": 542,
    "end_line": 569,
    "kind": "function",
    "qualified_name": "_regionRows",
    "depth": 0
   },
   {
    "start_line": 550,
    "end_line": 558,
    "kind": "function",
    "qualified_name": "_regionRows.ctxTo",
    "depth": 1
   },
   {
    "start_line": 574,
    "end_line": 578,
    "kind": "function",
    "qualified_name": "_regionSpan",
    "depth": 0
   },
   {
    "start_line": 580,
    "end_line": 590,
    "kind": "function",
    "qualified_name": "_rowExtent",
    "depth": 0
   },
   {
    "start_line": 595,
    "end_line": 609,
    "kind": "function",
    "qualified_name": "_renderRegion",
    "depth": 0
   },
   {
    "start_line": 614,
    "end_line": 620,
    "kind": "function",
    "qualified_name": "_withManifest",
    "depth": 0
   },
   {
    "start_line": 622,
    "end_line": 633,
    "kind": "function",
    "qualified_name": "_renderRegionChip",
    "depth": 0
   }
  ],
  "base_spans": [
   {
    "start_line": 524,
    "end_line": 534,
    "kind": "function",
    "qualified_name": "_rowExtent",
    "depth": 0
   },
   {
    "start_line": 538,
    "end_line": 546,
    "kind": "function",
    "qualified_name": "_renderRegion",
    "depth": 0
   },
   {
    "start_line": 548,
    "end_line": 559,
    "kind": "function",
    "qualified_name": "_renderRegionChip",
    "depth": 0
   },
   {
    "start_line": 561,
    "end_line": 577,
    "kind": "function",
    "qualified_name": "_renderRegionExpansion",
    "depth": 0
   },
   {
    "start_line": 586,
    "end_line": 609,
    "kind": "function",
    "qualified_name": "_renderDiffRows",
    "depth": 0
   },
   {
    "start_line": 613,
    "end_line": 648,
    "kind": "function",
    "qualified_name": "_renderHunk",
    "depth": 0
   }
  ],
  "expect_runs": null
 },
 {
  "name": "semantic_code_review/augment/usage.py:@@ -83,7 +83,20",
  "moved": true,
  "old_start": 83,
  "old_count": 7,
  "new_start": 83,
  "new_count": 20,
  "header": "@@ -83,7 +83,20 @@ class _PassAccumulator:",
  "body": " \n     @property\n     def total_tokens(self) -> int:\n-        return self.input_tokens + self.output_tokens + self.cache_read_tokens + self.cache_write_tokens\n+        \"\"\"Tokens the request actually carried.\n+\n+        `input_tokens` already includes both cached portions, so adding\n+        them again counts every cached token twice — which reported SDK\n+        backends at roughly 2x their real usage while leaving the CLI\n+        (whose driver passed Anthropic's cache-exclusive figure straight\n+        through) correct, and made the two look incomparable.\n+        \"\"\"\n+        return self.input_tokens + self.output_tokens\n+\n+    @property\n+    def uncached_input_tokens(self) -> int:\n+        \"\"\"Input billed at the full rate — cache reads and writes are cheaper.\"\"\"\n+        return max(0, self.input_tokens - self.cache_read_tokens - self.cache_write_tokens)\n \n     def summary(self) -> dict[str, Any]:\n         out: dict[str, Any] = {\n",
  "head_spans": [
   {
    "start_line": 48,
    "end_line": 116,
    "kind": "class",
    "qualified_name": "_PassAccumulator",
    "depth": 0
   },
   {
    "start_line": 61,
    "end_line": 82,
    "kind": "function",
    "qualified_name": "_PassAccumulator.add_trace",
    "depth": 1
   },
   {
    "start_line": 85,
    "end_line": 94,
    "kind": "function",
    "qualified_name": "_PassAccumulator.total_tokens",
    "depth": 1
   },
   {
    "start_line": 97,
    "end_line": 99,
    "kind": "function",
    "qualified_name": "_PassAccumulator.uncached_input_tokens",
    "depth": 1
   },
   {
    "start_line": 101,
    "end_line": 116,
    "kind": "function",
    "qualified_name": "_PassAccumulator.summary",
    "depth": 1
   }
  ],
  "base_spans": [
   {
    "start_line": 48,
    "end_line": 103,
    "kind": "class",
    "qualified_name": "_PassAccumulator",
    "depth": 0
   },
   {
    "start_line": 61,
    "end_line": 82,
    "kind": "function",
    "qualified_name": "_PassAccumulator.add_trace",
    "depth": 1
   },
   {
    "start_line": 85,
    "end_line": 86,
    "kind": "function",
    "qualified_name": "_PassAccumulator.total_tokens",
    "depth": 1
   },
   {
    "start_line": 88,
    "end_line": 103,
    "kind": "function",
    "qualified_name": "_PassAccumulator.summary",
    "depth": 1
   }
  ],
  "expect_runs": [
   {
    "kind": "ins",
    "start_idx": 5,
    "texts": [
     "        `input_tokens` already includes both cached portions, so adding",
     "        them again counts every cached token twice — which reported SDK",
     "        backends at roughly 2x their real usage while leaving the CLI",
     "        (whose driver passed Anthropic's cache-exclusive figure straight",
     "        through) correct, and made the two look incomparable.",
     "        \"\"\"",
     "        return self.input_tokens + self.output_tokens",
     "",
     "    @property",
     "    def uncached_input_tokens(self) -> int:",
     "        \"\"\"Input billed at the full rate — cache reads and writes are cheaper.\"\"\"",
     "        return max(0, self.input_tokens - self.cache_read_tokens - self.cache_write_tokens)",
     ""
    ]
   }
  ]
 },
 {
  "name": "semantic_code_review/cli/init_cmd.py:@@ -82,17 +88,21",
  "moved": false,
  "old_start": 82,
  "old_count": 17,
  "new_start": 88,
  "new_count": 21,
  "header": "@@ -82,17 +88,21 @@ def _env_present(var: str | None) -> bool:",
  "body": "     return bool(var) and bool(os.environ.get(var or \"\", \"\").strip())\n \n \n-def _command_yields_key(argv: tuple[str, ...]) -> bool:\n-    \"\"\"True if ``argv`` runs, exits 0, and prints non-empty output.\n-\n-    Non-raising: any failure (missing binary, non-zero exit, timeout)\n-    is a plain False so detection never aborts the wizard.\n+def _command_key_value(argv: tuple[str, ...]) -> str | None:\n+    \"\"\"The key ``argv`` prints, or None. Non-raising: any failure (missing\n+    binary, non-zero exit, timeout, empty output) is None so detection\n+    never aborts the wizard.\n     \"\"\"\n     try:\n         proc = subprocess.run(list(argv), capture_output=True, text=True, timeout=10, check=False)\n     except (OSError, subprocess.SubprocessError):\n-        return False\n-    return proc.returncode == 0 and bool((proc.stdout or \"\").strip())\n+        return None\n+    out = (proc.stdout or \"\").strip()\n+    return out if (proc.returncode == 0 and out) else None\n+\n+\n+def _command_yields_key(argv: tuple[str, ...]) -> bool:\n+    return _command_key_value(argv) is not None\n \n \n def _detect(name: str, bdef: BackendDef) -> _Status:\n",
  "head_spans": [
   {
    "start_line": 76,
    "end_line": 84,
    "kind": "class",
    "qualified_name": "_Status",
    "depth": 0
   },
   {
    "start_line": 83,
    "end_line": 84,
    "kind": "function",
    "qualified_name": "_Status.marker",
    "depth": 1
   },
   {
    "start_line": 87,
    "end_line": 88,
    "kind": "function",
    "qualified_name": "_env_present",
    "depth": 0
   },
   {
    "start_line": 91,
    "end_line": 101,
    "kind": "function",
    "qualified_name": "_command_key_value",
    "depth": 0
   },
   {
    "start_line": 104,
    "end_line": 105,
    "kind": "function",
    "qualified_name": "_command_yields_key",
    "depth": 0
   },
   {
    "start_line": 108,
    "end_line": 126,
    "kind": "function",
    "qualified_name": "_detect",
    "depth": 0
   }
  ],
  "base_spans": [
   {
    "start_line": 81,
    "end_line": 82,
    "kind": "function",
    "qualified_name": "_env_present",
    "depth": 0
   },
   {
    "start_line": 85,
    "end_line": 95,
    "kind": "function",
    "qualified_name": "_command_yields_key",
    "depth": 0
   },
   {
    "start_line": 98,
    "end_line": 116,
    "kind": "function",
    "qualified_name": "_detect",
    "depth": 0
   }
  ],
  "expect_runs": null
 },
 {
  "name": "semantic_code_review/cli/init_cmd.py:@@ -128,89 +138,127",
  "moved": true,
  "old_start": 128,
  "old_count": 89,
  "new_start": 138,
  "new_count": 127,
  "header": "@@ -128,89 +138,127 @@ def _ordered_backends(cfg: ScrConfig) -> list[tuple[str, BackendDef, _Status]]:",
  "body": " \n \n def _choose_backend(cfg: ScrConfig) -> str:\n-    \"\"\"Numbered menu (ready first) with detection + description. Default is\n-    the current config backend, else the first ready one, else the first row.\n+    \"\"\"Arrow-key select (ready first) with detection marker + description.\n+    Default is the current config backend, else the first ready one.\n     \"\"\"\n     rows = _ordered_backends(cfg)\n-    typer.echo(\"Backends (✓ ready · ○ local · · needs a credential):\\n\")\n-    for i, (name, bdef, status) in enumerate(rows, start=1):\n-        typer.echo(f\"  [{i}] {status.marker} {name:<12} {status.detail}\")\n+    choices: list[tuple[str, str]] = []\n+    for name, bdef, status in rows:\n+        label = f\"{status.marker} {name:<12} {status.detail}\"\n         if bdef.description:\n-            typer.echo(f\"         {bdef.description}\")\n-    default_idx = _default_backend_index(cfg, rows)\n-    choice = typer.prompt(\"\\nDefault backend\", default=str(default_idx))\n-    idx = _parse_choice(choice, len(rows))\n-    return rows[idx - 1][0]\n+            label += f\"\\n     {bdef.description}\"\n+        choices.append((label, name))\n+    return prompt.select(\n+        \"Default backend  (✓ ready · ○ local · · needs a credential)\",\n+        choices,\n+        default=_default_backend(cfg, rows),\n+    )\n \n \n-def _default_backend_index(cfg: ScrConfig, rows: list[tuple[str, BackendDef, _Status]]) -> int:\n+def _default_backend(cfg: ScrConfig, rows: list[tuple[str, BackendDef, _Status]]) -> str:\n     names = [r[0] for r in rows]\n     if cfg.backend and cfg.backend in names:\n-        return names.index(cfg.backend) + 1\n-    for i, (_name, _bdef, status) in enumerate(rows, start=1):\n+        return cfg.backend\n+    for name, _bdef, status in rows:\n         if status.state == \"ready\":\n-            return i\n-    return 1\n-\n-\n-def _parse_choice(raw: str, count: int) -> int:\n-    try:\n-        idx = int(raw.strip())\n-    except ValueError:\n-        raise typer.BadParameter(f\"expected a number 1–{count}, got {raw!r}\") from None\n-    if not 1 <= idx <= count:\n-        raise typer.BadParameter(f\"choice {idx} out of range 1–{count}\")\n-    return idx\n+            return name\n+    return names[0]\n \n \n def _choose_model(name: str, bdef: BackendDef) -> str | None:\n-    \"\"\"Prompt for a model. Returns an override only when the user picks\n-    something other than the backend's builtin default (blank keeps it).\n+    \"\"\"Pick a model, live-listing the backend's models when the credential\n+    resolves. Returns an override only when the choice differs from the\n+    backend's builtin default (matching the default keeps it implicit).\n     \"\"\"\n     default = bdef.default_model or \"\"\n-    prompt = f\"Model for {name}\" + (f\" [{default}]\" if default else \" (required — this backend pins no default)\")\n-    entered = typer.prompt(prompt, default=default, show_default=False).strip()\n-    if not entered:\n+    key = os.environ.get(bdef.api_key_env) if bdef.api_key_env else None\n+    models = credentials.list_models(bdef, key)\n+\n+    if models:\n+        other = \"\\x00other\"  # sentinel value distinct from any real model id\n+        choices = [(m, m) for m in models]\n+        choices.append((\"other — type a model id\", other))\n+        picked = prompt.select(f\"Model for {name}\", choices, default=default if default in models else models[0])\n+        if picked == other:\n+            picked = prompt.text(f\"Model id for {name}\", default=default)\n+    else:\n+        label = f\"Model for {name}\" + (\"\" if default else \" (required — this backend pins no default)\")\n+        picked = prompt.text(label, default=default)\n+\n+    picked = picked.strip()\n+    if not picked or picked == (bdef.default_model or \"\"):\n         return None\n-    return entered if entered != (bdef.default_model or None) else None\n+    return picked\n+\n \n+@dataclasses.dataclass\n+class _Credential:\n+    \"\"\"What the credential step wants persisted. `api_key_command` goes\n+    under `[backends.<name>]`; `config_env` is a `(VAR, value)` written to\n+    the `[env]` table (the config-key source). Both may be None.\n+    \"\"\"\n+\n+    api_key_command: tuple[str, ...] | None = None\n+    config_env: tuple[str, str] | None = None\n \n-def _credential_step(name: str, bdef: BackendDef) -> tuple[str, ...] | None:\n-    \"\"\"Guide credential setup for the chosen backend.\n \n-    Returns an `api_key_command` tuple to persist in `[backends.<name>]`\n-    when the user opts for the fetch-command route, else None.\n+def _credential_step(name: str, bdef: BackendDef, scope: str) -> _Credential:\n+    \"\"\"Guide credential setup by offering the backend's allowed sources.\n+\n+    Whichever source is chosen, its key (when we can obtain it) is applied\n+    to this process's environment so the following model step can list the\n+    backend's models live.\n     \"\"\"\n     status = _detect(name, bdef)\n     if status.state == \"ready\":\n         typer.echo(f\"\\nscr: credential ready — {status.detail}.\")\n-        return None\n-    if status.state == \"local\":\n-        typer.echo(f\"\\nscr: {status.detail}.\")\n-        return None\n-    if bdef.type is BackendType.CLAUDE_CLI:\n+        return _Credential()\n+\n+    ids = credentials.allowed_source_ids(name, bdef, scope=scope)\n+    if status.state == \"local\" or ids == [\"none\"]:\n         typer.echo(f\"\\nscr: {status.detail}.\")\n-        return None\n+        return _Credential()\n \n     var = bdef.api_key_env or \"API_KEY\"\n-    typer.echo(f\"\\n{name} needs a credential (${var}). Choose how to provide it:\")\n-    typer.echo(\"  [1] I'll set the env var myself (in .env or my shell)\")\n-    typer.echo(\"  [2] Run a command to fetch it (stored in config; the key is not)\")\n-    typer.echo(\"  [3] Paste it now into a gitignored .env\")\n-    choice = typer.prompt(\"Credential source\", default=\"1\").strip()\n-\n-    if choice == \"2\":\n-        return _configure_api_key_command(name, var)\n-    if choice == \"3\":\n-        _write_env_key(var)\n+    choices = [(credentials.SOURCES[i].label.format(var=var), i) for i in ids]\n+    source = prompt.select(f\"\\n{name} needs a credential (${var}). How will you provide it?\", choices, default=ids[0])\n+\n+    if source == \"command\":\n+        return _Credential(api_key_command=_configure_api_key_command(name, var))\n+    if source == \"dotenv\":\n+        value = _acquire_key(var)\n+        if value:\n+            _write_env_key(var, value)\n+        return _Credential()\n+    if source == \"config\":\n+        value = _acquire_key(var)\n+        return _Credential(config_env=(var, value)) if value else _Credential()\n+    if source == \"vertex\":\n+        typer.echo(\n+            \"\\nUsing Vertex AI: set GOOGLE_CLOUD_PROJECT and authenticate with\\n\"\n+            \"    gcloud auth application-default login\"\n+        )\n+        return _Credential()\n+    # \"env\"\n+    typer.echo(f\"\\nExport it in your shell, or add it to a .env in the repo you review from:\\n    {var}=<your-key>\")\n+    return _Credential()\n+\n+\n+def _acquire_key(var: str) -> str | None:\n+    \"\"\"Prompt for a key (hidden), apply it to this process's env so the\n+    model step can list live, and return it (None when left blank).\n+    \"\"\"\n+    value = prompt.password(f\"Paste {var}\")\n+    if not value:\n+        typer.echo(\"scr: no value entered; skipping.\", err=True)\n         return None\n-    typer.echo(f\"\\nAdd this to a .env in the repo you review from, or export it in your shell:\\n    {var}=<your-key>\")\n-    return None\n+    os.environ[var] = value\n+    return value\n \n \n def _configure_api_key_command(name: str, var: str) -> tuple[str, ...] | None:\n     \"\"\"Prompt for a key-fetch command, validate it, return its argv.\"\"\"\n-    raw = typer.prompt(\"Command to fetch the key (e.g. `gh auth token`)\").strip()\n+    raw = prompt.text(\"Command to fetch the key (e.g. `gh auth token`)\")\n     try:\n         argv = tuple(shlex.split(raw))\n     except ValueError as e:\n",
  "head_spans": [
   {
    "start_line": 108,
    "end_line": 126,
    "kind": "function",
    "qualified_name": "_detect",
    "depth": 0
   },
   {
    "start_line": 129,
    "end_line": 134,
    "kind": "function",
    "qualified_name": "_ordered_backends",
    "depth": 0
   },
   {
    "start_line": 140,
    "end_line": 155,
    "kind": "function",
    "qualified_name": "_choose_backend",
    "depth": 0
   },
   {
    "start_line": 158,
    "end_line": 165,
    "kind": "function",
    "qualified_name": "_default_backend",
    "depth": 0
   },
   {
    "start_line": 168,
    "end_line": 191,
    "kind": "function",
    "qualified_name": "_choose_model",
    "depth": 0
   },
   {
    "start_line": 195,
    "end_line": 202,
    "kind": "class",
    "qualified_name": "_Credential",
    "depth": 0
   },
   {
    "start_line": 205,
    "end_line": 244,
    "kind": "function",
    "qualified_name": "_credential_step",
    "depth": 0
   },
   {
    "start_line": 247,
    "end_line": 256,
    "kind": "function",
    "qualified_name": "_acquire_key",
    "depth": 0
   },
   {
    "start_line": 259,
    "end_line": 280,
    "kind": "function",
    "qualified_name": "_configure_api_key_command",
    "depth": 0
   }
  ],
  "base_spans": [
   {
    "start_line": 130,
    "end_line": 143,
    "kind": "function",
    "qualified_name": "_choose_backend",
    "depth": 0
   },
   {
    "start_line": 146,
    "end_line": 153,
    "kind": "function",
    "qualified_name": "_default_backend_index",
    "depth": 0
   },
   {
    "start_line": 156,
    "end_line": 163,
    "kind": "function",
    "qualified_name": "_parse_choice",
    "depth": 0
   },
   {
    "start_line": 166,
    "end_line": 175,
    "kind": "function",
    "qualified_name": "_choose_model",
    "depth": 0
   },
   {
    "start_line": 178,
    "end_line": 208,
    "kind": "function",
    "qualified_name": "_credential_step",
    "depth": 0
   },
   {
    "start_line": 211,
    "end_line": 230,
    "kind": "function",
    "qualified_name": "_configure_api_key_command",
    "depth": 0
   },
   {
    "start_line": 233,
    "end_line": 244,
    "kind": "function",
    "qualified_name": "_write_env_key",
    "depth": 0
   },
   {
    "start_line": 247,
    "end_line": 260,
    "kind": "function",
    "qualified_name": "_ensure_gitignored",
    "depth": 0
   },
   {
    "start_line": 265,
    "end_line": 268,
    "kind": "constant",
    "qualified_name": "_FRESH_HEADER",
    "depth": 0
   }
  ],
  "expect_runs": [
   {
    "kind": "ins",
    "start_idx": 16,
    "texts": [
     "        default=_default_backend(cfg, rows),",
     "    )"
    ]
   },
   {
    "kind": "del",
    "start_idx": 30,
    "texts": [
     "def _parse_choice(raw: str, count: int) -> int:",
     "    try:",
     "        idx = int(raw.strip())",
     "    except ValueError:",
     "        raise typer.BadParameter(f\"expected a number 1–{count}, got {raw!r}\") from None",
     "    if not 1 <= idx <= count:",
     "        raise typer.BadParameter(f\"choice {idx} out of range 1–{count}\")",
     "    return idx",
     "",
     ""
    ]
   },
   {
    "kind": "ins",
    "start_idx": 49,
    "texts": [
     "    if models:",
     "        other = \"\\x00other\"  # sentinel value distinct from any real model id",
     "        choices = [(m, m) for m in models]",
     "        choices.append((\"other — type a model id\", other))",
     "        picked = prompt.select(f\"Model for {name}\", choices, default=default if default in models else models[0])",
     "        if picked == other:",
     "            picked = prompt.text(f\"Model id for {name}\", default=default)",
     "    else:",
     "        label = f\"Model for {name}\" + (\"\" if default else \" (required — this backend pins no default)\")",
     "        picked = prompt.text(label, default=default)",
     "",
     "    picked = picked.strip()",
     "    if not picked or picked == (bdef.default_model or \"\"):"
    ]
   },
   {
    "kind": "ins",
    "start_idx": 66,
    "texts": [
     "@dataclasses.dataclass",
     "class _Credential:",
     "    \"\"\"What the credential step wants persisted. `api_key_command` goes",
     "    under `[backends.<name>]`; `config_env` is a `(VAR, value)` written to",
     "    the `[env]` table (the config-key source). Both may be None.",
     "    \"\"\"",
     "",
     "    api_key_command: tuple[str, ...] | None = None",
     "    config_env: tuple[str, str] | None = None"
    ]
   },
   {
    "kind": "del",
    "start_idx": 76,
    "texts": [
     "def _credential_step(name: str, bdef: BackendDef) -> tuple[str, ...] | None:",
     "    \"\"\"Guide credential setup for the chosen backend."
    ]
   },
   {
    "kind": "ins",
    "start_idx": 81,
    "texts": [
     "",
     "    Whichever source is chosen, its key (when we can obtain it) is applied",
     "    to this process's environment so the following model step can list the",
     "    backend's models live."
    ]
   },
   {
    "kind": "ins",
    "start_idx": 108,
    "texts": [
     "    if source == \"config\":",
     "        value = _acquire_key(var)",
     "        return _Credential(config_env=(var, value)) if value else _Credential()",
     "    if source == \"vertex\":",
     "        typer.echo(",
     "            \"\\nUsing Vertex AI: set GOOGLE_CLOUD_PROJECT and authenticate with\\n\"",
     "            \"    gcloud auth application-default login\"",
     "        )",
     "        return _Credential()",
     "    # \"env\"",
     "    typer.echo(f\"\\nExport it in your shell, or add it to a .env in the repo you review from:\\n    {var}=<your-key>\")",
     "    return _Credential()",
     "",
     "",
     "def _acquire_key(var: str) -> str | None:",
     "    \"\"\"Prompt for a key (hidden), apply it to this process's env so the",
     "    model step can list live, and return it (None when left blank).",
     "    \"\"\"",
     "    value = prompt.password(f\"Paste {var}\")",
     "    if not value:",
     "        typer.echo(\"scr: no value entered; skipping.\", err=True)"
    ]
   }
  ]
 },
 {
  "name": "semantic_code_review/augment/explainer_section.py:@@ -105,33 +106,53",
  "moved": false,
  "old_start": 105,
  "old_count": 33,
  "new_start": 106,
  "new_count": 53,
  "header": "@@ -105,33 +106,53 @@ DOCUMENT_TURN_BUDGET = 36",
  "body": " MIN_TOOL_TURNS = 2\n \n \n-class SectionNotFound(KeyError):\n+class SectionNotFound(errors.ScrError, KeyError):\n     \"\"\"No fillable section with that id in this run's document.\n \n-    Maps to HTTP 404. The Map is written by the skeleton and subsections\n-    are written by their parent's pass, so neither is addressable here.\n+    The Map is written by the skeleton and subsections are written by\n+    their parent's pass, so neither is addressable here.\n     \"\"\"\n \n+    status = 404\n \n-class SectionNotReady(RuntimeError):\n+    def __init__(self, section_id: str) -> None:\n+        super().__init__(section_id)\n+        self.section_id = section_id\n+\n+    def body(self) -> dict[str, Any]:\n+        # KeyError renders its argument as a repr, which reads as a bare\n+        # quoted id; say what the id failed to address instead.\n+        return {\"error\": f\"no section {self.section_id!r} in this document\"}\n+\n+\n+class SectionNotReady(errors.ScrError):\n     \"\"\"The hunks this call is anchored to are not all annotated yet.\n \n     Prose built on half the hunk intents reads exactly as fluently as\n     prose built on all of them, which is why this is a refusal rather\n-    than a caveat in the output. Maps to HTTP 409 with the counts, so\n-    the viewer can say what it is waiting for.\n+    than a caveat in the output. Carries the counts, so the viewer can\n+    say what it is waiting for.\n     \"\"\"\n \n+    status = 409\n+\n     def __init__(self, annotated: int, total: int) -> None:\n         super().__init__(f\"{annotated} of {total} anchored hunks are annotated\")\n         self.annotated = annotated\n         self.total = total\n \n+    def body(self) -> dict[str, Any]:\n+        return {\n+            \"error\": f\"{self.annotated} of {self.total} hunks under this section are annotated\",\n+            \"annotated\": self.annotated,\n+            \"total\": self.total,\n+        }\n+\n \n-class SectionFailed(RuntimeError):\n+class SectionFailed(errors.ScrError):\n     \"\"\"A prose call raised; its sections are recorded `failed`.\n \n-    Carries the persisted document so the route can fan the `failed`\n+    Carries the persisted document so the session can fan the `failed`\n     state out to every tab rather than leaving the other readers on\n     `pending` forever.\n     \"\"\"\n",
  "head_spans": [
   {
    "start_line": 106,
    "end_line": 106,
    "kind": "constant",
    "qualified_name": "MIN_TOOL_TURNS",
    "depth": 0
   },
   {
    "start_line": 109,
    "end_line": 125,
    "kind": "class",
    "qualified_name": "SectionNotFound",
    "depth": 0
   },
   {
    "start_line": 118,
    "end_line": 120,
    "kind": "function",
    "qualified_name": "SectionNotFound.__init__",
    "depth": 1
   },
   {
    "start_line": 122,
    "end_line": 125,
    "kind": "function",
    "qualified_name": "SectionNotFound.body",
    "depth": 1
   },
   {
    "start_line": 128,
    "end_line": 149,
    "kind": "class",
    "qualified_name": "SectionNotReady",
    "depth": 0
   },
   {
    "start_line": 139,
    "end_line": 142,
    "kind": "function",
    "qualified_name": "SectionNotReady.__init__",
    "depth": 1
   },
   {
    "start_line": 144,
    "end_line": 149,
    "kind": "function",
    "qualified_name": "SectionNotReady.body",
    "depth": 1
   },
   {
    "start_line": 152,
    "end_line": 162,
    "kind": "class",
    "qualified_name": "SectionFailed",
    "depth": 0
   },
   {
    "start_line": 160,
    "end_line": 162,
    "kind": "function",
    "qualified_name": "SectionFailed.__init__",
    "depth": 1
   }
  ],
  "base_spans": [
   {
    "start_line": 105,
    "end_line": 105,
    "kind": "constant",
    "qualified_name": "MIN_TOOL_TURNS",
    "depth": 0
   },
   {
    "start_line": 108,
    "end_line": 113,
    "kind": "class",
    "qualified_name": "SectionNotFound",
    "depth": 0
   },
   {
    "start_line": 116,
    "end_line": 128,
    "kind": "class",
    "qualified_name": "SectionNotReady",
    "depth": 0
   },
   {
    "start_line": 125,
    "end_line": 128,
    "kind": "function",
    "qualified_name": "SectionNotReady.__init__",
    "depth": 1
   },
   {
    "start_line": 131,
    "end_line": 141,
    "kind": "class",
    "qualified_name": "SectionFailed",
    "depth": 0
   },
   {
    "start_line": 139,
    "end_line": 141,
    "kind": "function",
    "qualified_name": "SectionFailed.__init__",
    "depth": 1
   },
   {
    "start_line": 147,
    "end_line": 151,
    "kind": "class",
    "qualified_name": "SubmittedRef",
    "depth": 0
   },
   {
    "start_line": 154,
    "end_line": 179,
    "kind": "class",
    "qualified_name": "SubmittedFigure",
    "depth": 0
   }
  ],
  "expect_runs": null
 },
 {
  "name": "semantic_code_review/viewer/assets/render.ts:@@ -373,114 +377,109",
  "moved": false,
  "old_start": 373,
  "old_count": 114,
  "new_start": 377,
  "new_count": 109,
  "header": "@@ -373,114 +377,109 @@ function _renderFileOverview(f: FileBlock): HTMLElement | null {",
  "body": "   return div;\n }\n \n-// --- Inter-hunk context expansion ---------------------------------------\n+// --- Collapsible diff regions -------------------------------------------\n+//\n+// A region is the folded counterpart of a hunk: the same diff-row stream,\n+// shown behind an \"expand\" chip instead of a hunk header. In the normal\n+// view a region holds only unchanged context between hunks; under a\n+// filter it also swallows the demoted (non-live) hunks, so their changes\n+// render inline when expanded.\n \n-interface GapDescriptor {\n+interface DiffRegion {\n   position: \"top\" | \"between\" | \"bottom\";\n-  new_start: number;\n-  new_end: number;\n-  old_start: number;\n-  old_end: number;\n-}\n-\n-function _gapBeforeFirstHunk(f: FileBlock): GapDescriptor | null {\n-  if (!f.head_lines || f.hunks.length === 0) return null;\n-  const h = f.hunks[0];\n-  const newStart = 1, newEnd = h.new_start - 1;\n-  if (newEnd < newStart) return null;\n-  return {\n-    position: \"top\",\n-    new_start: newStart, new_end: newEnd,\n-    old_start: 1, old_end: h.old_start - 1,\n-  };\n+  rows: RowBlock[];\n+  marks: (_RowMarks | undefined)[];\n }\n \n-function _gapAfterHunk(f: FileBlock, i: number): GapDescriptor | null {\n-  if (!f.head_lines) return null;\n-  const h = f.hunks[i];\n-  const newStart = h.new_start + h.new_count;\n-  const oldStart = h.old_start + h.old_count;\n-  if (i + 1 < f.hunks.length) {\n-    const n = f.hunks[i + 1];\n-    const newEnd = n.new_start - 1;\n-    if (newEnd < newStart) return null;\n-    return {\n-      position: \"between\",\n-      new_start: newStart, new_end: newEnd,\n-      old_start: oldStart, old_end: n.old_start - 1,\n-    };\n-  }\n-  const total = f.head_lines.length;\n-  if (newStart > total) return null;\n-  return {\n-    position: \"bottom\",\n-    new_start: newStart, new_end: total,\n-    old_start: oldStart, old_end: oldStart + (total - newStart),\n+/** The row stream for a region: unchanged context (from head_lines)\n+ *  interleaved with the demoted hunks' own rows, in file order. `newEnd`\n+ *  bounds the trailing context; null (no head_lines) skips context\n+ *  entirely, leaving just the hunk rows. */\n+function _regionRows(\n+  f: FileBlock, newStart: number, newEnd: number | null, oldStart: number, hunks: HunkBlock[],\n+): { rows: RowBlock[]; marks: (_RowMarks | undefined)[] } {\n+  const rows: RowBlock[] = [];\n+  const marks: (_RowMarks | undefined)[] = [];\n+  const hl = f.head_lines;\n+  let cn = newStart;\n+  let co = oldStart;\n+  const ctxTo = (upTo: number): void => {\n+    if (!hl) return;\n+    while (cn < upTo) {\n+      const t = hl[cn - 1] ?? \"\";\n+      rows.push({ kind: \"ctx\", old_line: co, new_line: cn, old_text: t, new_text: t });\n+      marks.push(undefined);\n+      co++; cn++;\n+    }\n   };\n+  for (const h of hunks) {\n+    ctxTo(h.new_start);\n+    const hm = _blockMarks(h.rows || []);\n+    const hr = h.rows || [];\n+    for (let i = 0; i < hr.length; i++) { rows.push(hr[i]); marks.push(hm[i]); }\n+    cn = h.new_start + h.new_count;\n+    co = h.old_start + h.old_count;\n+  }\n+  if (hl && newEnd !== null) ctxTo(newEnd + 1);\n+  return { rows, marks };\n }\n \n-function _renderGapChip(f: FileBlock, gap: GapDescriptor): HTMLElement {\n+function _renderRegionChip(f: FileBlock, region: DiffRegion): HTMLElement {\n   const chip = _el(\"div\", \"gap-chip\");\n-  const count = gap.new_end - gap.new_start + 1;\n-  const icon = gap.position === \"top\" ? \"⬆\" : gap.position === \"bottom\" ? \"⬇\" : \"⋯\";\n+  const count = region.rows.length;\n+  const icon = region.position === \"top\" ? \"⬆\" : region.position === \"bottom\" ? \"⬇\" : \"⋯\";\n   const word = count === 1 ? \"line\" : \"lines\";\n-  const label = gap.position === \"top\" ? `expand ${count} ${word} above`\n-              : gap.position === \"bottom\" ? `expand ${count} ${word} below`\n+  const label = region.position === \"top\" ? `expand ${count} ${word} above`\n+              : region.position === \"bottom\" ? `expand ${count} ${word} below`\n               : `expand ${count} hidden ${word}`;\n   chip.innerHTML = `<span class=\"gap-icon\">${icon}</span> <span class=\"gap-label\">${label}</span>`;\n-  chip.title = `lines ${gap.new_start}–${gap.new_end}`;\n   chip.addEventListener(\"click\", () => {\n-    chip.replaceWith(_renderGapExpansion(f, gap));\n+    chip.replaceWith(_renderRegionExpansion(f, region));\n     _refreshFileFolds(f);\n   });\n   return chip;\n }\n \n-function _renderGapExpansion(f: FileBlock, gap: GapDescriptor): HTMLElement {\n+function _renderRegionExpansion(f: FileBlock, region: DiffRegion): HTMLElement {\n   const container = _el(\"div\", \"gap-expansion\");\n   const collapse = _el(\"button\", \"gap-collapse\", \"× collapse\");\n   collapse.title = \"Hide these lines again\";\n   collapse.addEventListener(\"click\", () => {\n-    container.replaceWith(_renderGapChip(f, gap));\n+    container.replaceWith(_renderRegionChip(f, region));\n     _refreshFileFolds(f);\n   });\n   container.appendChild(collapse);\n+  const { diff, oldEls, newEls } = _renderDiffRows(f, region.rows, region.marks);\n+  // The file-level fold walker (folds.ts) recovers the row stream + DOM\n+  // elements from the container.\n+  FileRows.record(container, { rows: region.rows, oldEls, newEls });\n+  container.appendChild(diff);\n+  return container;\n+}\n \n+/** Render a diff-row stream into a `.diff` grid, pairing old/new rows.\n+ *  The single primitive behind both hunk bodies and region expansions —\n+ *  what differs between them is only the surrounding chrome. */\n+function _renderDiffRows(\n+  f: FileBlock, rows: RowBlock[], marks: (_RowMarks | undefined)[],\n+): { diff: HTMLElement; oldEls: HTMLElement[]; newEls: HTMLElement[] } {\n   const diff = _el(\"div\", \"diff\");\n   const halfOld = _el(\"div\", \"half half-old\");\n   const halfNew = _el(\"div\", \"half half-new\");\n   diff.appendChild(halfOld);\n   diff.appendChild(halfNew);\n-\n-  const rows: RowBlock[] = [];\n-  const rowElsOld: HTMLElement[] = [];\n-  const rowElsNew: HTMLElement[] = [];\n-  const count = gap.new_end - gap.new_start + 1;\n-  const headLines = f.head_lines || [];\n-  for (let i = 0; i < count; i++) {\n-    const ol = gap.old_start + i;\n-    const nl = gap.new_start + i;\n-    const text = headLines[nl - 1] ?? \"\";\n-    const rowRecord: RowBlock = {\n-      kind: \"ctx\", old_line: ol, new_line: nl,\n-      old_text: text, new_text: text,\n-    };\n-    rows.push(rowRecord);\n-    const pair = _renderRow(rowRecord, f);\n+  const oldEls: HTMLElement[] = [];\n+  const newEls: HTMLElement[] = [];\n+  for (let i = 0; i < rows.length; i++) {\n+    const pair = _renderRow(rows[i], f, marks[i]?.old, marks[i]?.new);\n     (pair.old as { _scrPair?: HTMLElement })._scrPair = pair.new;\n     (pair.new as { _scrPair?: HTMLElement })._scrPair = pair.old;\n     halfOld.appendChild(pair.old);\n     halfNew.appendChild(pair.new);\n-    rowElsOld.push(pair.old);\n-    rowElsNew.push(pair.new);\n+    oldEls.push(pair.old);\n+    newEls.push(pair.new);\n   }\n-\n-  // The file-level fold walker (folds.ts) needs to recover the row\n-  // stream + per-side DOM elements; hand them off through FileRows.\n-  FileRows.record(container, { rows, oldEls: rowElsOld, newEls: rowElsNew });\n-\n-  container.appendChild(diff);\n-  return container;\n+  return { diff, oldEls, newEls };\n }\n \n function _refreshFileFolds(f: FileBlock): void {\n",
  "head_spans": [
   {
    "start_line": 368,
    "end_line": 378,
    "kind": "function",
    "qualified_name": "_renderFileOverview",
    "depth": 0
   },
   {
    "start_line": 388,
    "end_line": 392,
    "kind": "interface",
    "qualified_name": "DiffRegion",
    "depth": 0
   },
   {
    "start_line": 398,
    "end_line": 425,
    "kind": "function",
    "qualified_name": "_regionRows",
    "depth": 0
   },
   {
    "start_line": 406,
    "end_line": 414,
    "kind": "function",
    "qualified_name": "_regionRows.ctxTo",
    "depth": 1
   },
   {
    "start_line": 427,
    "end_line": 441,
    "kind": "function",
    "qualified_name": "_renderRegionChip",
    "depth": 0
   },
   {
    "start_line": 443,
    "end_line": 458,
    "kind": "function",
    "qualified_name": "_renderRegionExpansion",
    "depth": 0
   },
   {
    "start_line": 463,
    "end_line": 483,
    "kind": "function",
    "qualified_name": "_renderDiffRows",
    "depth": 0
   },
   {
    "start_line": 485,
    "end_line": 490,
    "kind": "function",
    "qualified_name": "_refreshFileFolds",
    "depth": 0
   }
  ],
  "base_spans": [
   {
    "start_line": 364,
    "end_line": 374,
    "kind": "function",
    "qualified_name": "_renderFileOverview",
    "depth": 0
   },
   {
    "start_line": 378,
    "end_line": 384,
    "kind": "interface",
    "qualified_name": "GapDescriptor",
    "depth": 0
   },
   {
    "start_line": 386,
    "end_line": 396,
    "kind": "function",
    "qualified_name": "_gapBeforeFirstHunk",
    "depth": 0
   },
   {
    "start_line": 398,
    "end_line": 420,
    "kind": "function",
    "qualified_name": "_gapAfterHunk",
    "depth": 0
   },
   {
    "start_line": 422,
    "end_line": 437,
    "kind": "function",
    "qualified_name": "_renderGapChip",
    "depth": 0
   },
   {
    "start_line": 439,
    "end_line": 484,
    "kind": "function",
    "qualified_name": "_renderGapExpansion",
    "depth": 0
   },
   {
    "start_line": 486,
    "end_line": 491,
    "kind": "function",
    "qualified_name": "_refreshFileFolds",
    "depth": 0
   }
  ],
  "expect_runs": null
 },
 {
  "name": "semantic_code_review/viewer/assets/sidebar.ts:@@ -379,7 +382,11",
  "moved": false,
  "old_start": 379,
  "old_count": 7,
  "new_start": 382,
  "new_count": 11,
  "header": "@@ -379,7 +382,11 @@ function _emitActivePill(): void {",
  "body": "   _onActivePillChange?.(_activeSymbolName());\n }\n \n-function _activePillHunkIds(): Set<string> | null {\n+/** The active pill's hunk-id set, or null when no pill is active.\n+ *  render.ts reads this to decide, per file, between the normal body and\n+ *  the focused merged-diff body (only the hunks in this set, no headers,\n+ *  no context gaps). */\n+function activeHunkIds(): Set<string> | null {\n   if (_activePill === null) return null;\n   const axis = AXES.find((a) => a.id === _activePill!.axis);\n   if (!axis) return null;\n",
  "head_spans": [
   {
    "start_line": 375,
    "end_line": 379,
    "kind": "function",
    "qualified_name": "_activeSymbolName",
    "depth": 0
   },
   {
    "start_line": 381,
    "end_line": 383,
    "kind": "function",
    "qualified_name": "_emitActivePill",
    "depth": 0
   },
   {
    "start_line": 389,
    "end_line": 395,
    "kind": "function",
    "qualified_name": "activeHunkIds",
    "depth": 0
   }
  ],
  "base_spans": [
   {
    "start_line": 372,
    "end_line": 376,
    "kind": "function",
    "qualified_name": "_activeSymbolName",
    "depth": 0
   },
   {
    "start_line": 378,
    "end_line": 380,
    "kind": "function",
    "qualified_name": "_emitActivePill",
    "depth": 0
   },
   {
    "start_line": 382,
    "end_line": 388,
    "kind": "function",
    "qualified_name": "_activePillHunkIds",
    "depth": 0
   },
   {
    "start_line": 395,
    "end_line": 411,
    "kind": "function",
    "qualified_name": "applyFilter",
    "depth": 0
   }
  ],
  "expect_runs": null
 },
 {
  "name": "semantic_code_review/viewer/assets/sidebar.ts:@@ -387,86 +394,17",
  "moved": false,
  "old_start": 387,
  "old_count": 86,
  "new_start": 394,
  "new_count": 17,
  "header": "@@ -387,86 +394,17 @@ function _activePillHunkIds(): Set<string> | null {",
  "body": "   return g ? new Set(g.hunk_ids || []) : new Set<string>();\n }\n \n-/** Walk every .hunk in the file body, tag `.ungrouped` for hunks no\n- *  themes-axis group claims, and toggle visibility based on the\n- *  active pill. Files with no visible hunks hide too — keeps the\n- *  filtered view tidy. Then collapse the filtered-out hunks and the\n- *  context gaps around them into a single fold per run. */\n+/** Tag `.ungrouped` on every rendered hunk no themes-axis group claims,\n+ *  the dotted-border tell for the unfiltered view. A no-op under an\n+ *  active filter: the focused bodies carry no themeable `.hunk` headers,\n+ *  and render.ts already selected which hunks appear. */\n function applyFilter(): void {\n-  const activeIds = _activePillHunkIds();\n-  document.querySelectorAll(\".file\").forEach((fileEl) => {\n-    let visible = 0;\n-    fileEl.querySelectorAll(\".hunk\").forEach((hunkEl) => {\n-      const hid = (hunkEl as HTMLElement).dataset.id || \"\";\n-      const inAnyGroup = (THEMES_AXIS.hunkCount[hid] || 0) > 0;\n-      hunkEl.classList.toggle(\"ungrouped\", !inAnyGroup);\n-      const show = activeIds === null ? true : activeIds.has(hid);\n-      (hunkEl as HTMLElement).style.display = show ? \"\" : \"none\";\n-      if (show) visible++;\n-    });\n-    _refreshInterstitialFolds(fileEl as HTMLElement, activeIds);\n-    (fileEl as HTMLElement).style.display =\n-      visible === 0 && activeIds !== null ? \"none\" : \"\";\n-  });\n-}\n-\n-/** Collapse each run of filtered-out hunks together with the context\n- *  gaps around them into one fold placeholder, so a symbol/file filter\n- *  shows the surviving hunks separated by a single \"N hidden hunks\"\n- *  fold rather than a scatter of orphaned \"expand context\" chips.\n- *\n- *  Rebuilt from scratch each call: prior placeholders are dropped and\n- *  every gap restored first, so a null filter (Show all) just tears the\n- *  collapse down. Runs of pure context gaps (no hidden hunk between two\n- *  survivors) are left alone — those are ordinary between-hunk chips.\n- *  Hunk display itself is owned by applyFilter; this only reads it. */\n-function _refreshInterstitialFolds(fileEl: HTMLElement, activeIds: Set<string> | null): void {\n-  fileEl.querySelectorAll(\".filter-fold\").forEach((el) => el.remove());\n-  fileEl.querySelectorAll<HTMLElement>(\".gap-chip, .gap-expansion\").forEach((el) => {\n-    el.style.display = \"\";\n-  });\n-  if (activeIds === null) return;\n-  const body = fileEl.querySelector(\".file-body\");\n-  if (!body) return;\n-\n-  const isGap = (el: HTMLElement): boolean =>\n-    el.classList.contains(\"gap-chip\") || el.classList.contains(\"gap-expansion\");\n-  const isHiddenHunk = (el: HTMLElement): boolean =>\n-    el.classList.contains(\"hunk\") && el.style.display === \"none\";\n-\n-  const children = Array.from(body.children) as HTMLElement[];\n-  let i = 0;\n-  while (i < children.length) {\n-    if (!isGap(children[i]) && !isHiddenHunk(children[i])) { i++; continue; }\n-    const run: HTMLElement[] = [];\n-    let hiddenHunks = 0;\n-    while (i < children.length && (isGap(children[i]) || isHiddenHunk(children[i]))) {\n-      run.push(children[i]);\n-      if (isHiddenHunk(children[i])) hiddenHunks++;\n-      i++;\n-    }\n-    if (hiddenHunks === 0) continue;   // pure context gap — leave it be\n-    body.insertBefore(_filterFold(run, hiddenHunks), run[0]);\n-    for (const el of run) el.style.display = \"none\";\n-  }\n-}\n-\n-/** One collapse placeholder standing in for `run` (the hidden hunks and\n- *  their surrounding gaps). Clicking toggles the run back into view for\n- *  a peek without leaving the filter. */\n-function _filterFold(run: HTMLElement[], hiddenHunks: number): HTMLElement {\n-  const fold = _el(\"div\", \"filter-fold\");\n-  const word = hiddenHunks === 1 ? \"hunk\" : \"hunks\";\n-  const label = (expanded: boolean): string =>\n-    expanded ? `× collapse ${hiddenHunks} hidden ${word}` : `⋯ ${hiddenHunks} hidden ${word}`;\n-  fold.textContent = label(false);\n-  fold.addEventListener(\"click\", () => {\n-    const expanded = !fold.classList.contains(\"expanded\");\n-    fold.classList.toggle(\"expanded\", expanded);\n-    fold.textContent = label(expanded);\n-    for (const el of run) el.style.display = expanded ? \"\" : \"none\";\n+  const filtered = _activePill !== null;\n+  document.querySelectorAll<HTMLElement>(\".hunk\").forEach((hunkEl) => {\n+    const hid = hunkEl.dataset.id || \"\";\n+    const inAnyGroup = (THEMES_AXIS.hunkCount[hid] || 0) > 0;\n+    hunkEl.classList.toggle(\"ungrouped\", !filtered && !inAnyGroup);\n   });\n-  return fold;\n }\n \n interface CommentCounts { total: number; unresolved: number }\n",
  "head_spans": [
   {
    "start_line": 389,
    "end_line": 395,
    "kind": "function",
    "qualified_name": "activeHunkIds",
    "depth": 0
   },
   {
    "start_line": 401,
    "end_line": 408,
    "kind": "function",
    "qualified_name": "applyFilter",
    "depth": 0
   },
   {
    "start_line": 410,
    "end_line": 410,
    "kind": "interface",
    "qualified_name": "CommentCounts",
    "depth": 0
   },
   {
    "start_line": 424,
    "end_line": 434,
    "kind": "function",
    "qualified_name": "_commentCountsByFilePath",
    "depth": 0
   },
   {
    "start_line": 440,
    "end_line": 462,
    "kind": "function",
    "qualified_name": "_fileGroupCommentCounts",
    "depth": 0
   },
   {
    "start_line": 443,
    "end_line": 459,
    "kind": "function",
    "qualified_name": "_fileGroupCommentCounts.visit",
    "depth": 1
   },
   {
    "start_line": 464,
    "end_line": 475,
    "kind": "function",
    "qualified_name": "_renderCommentCountBadge",
    "depth": 0
   }
  ],
  "base_spans": [
   {
    "start_line": 382,
    "end_line": 388,
    "kind": "function",
    "qualified_name": "_activePillHunkIds",
    "depth": 0
   },
   {
    "start_line": 395,
    "end_line": 411,
    "kind": "function",
    "qualified_name": "applyFilter",
    "depth": 0
   },
   {
    "start_line": 423,
    "end_line": 452,
    "kind": "function",
    "qualified_name": "_refreshInterstitialFolds",
    "depth": 0
   },
   {
    "start_line": 432,
    "end_line": 433,
    "kind": "function",
    "qualified_name": "_refreshInterstitialFolds.isGap",
    "depth": 1
   },
   {
    "start_line": 434,
    "end_line": 435,
    "kind": "function",
    "qualified_name": "_refreshInterstitialFolds.isHiddenHunk",
    "depth": 1
   },
   {
    "start_line": 457,
    "end_line": 470,
    "kind": "function",
    "qualified_name": "_filterFold",
    "depth": 0
   },
   {
    "start_line": 460,
    "end_line": 461,
    "kind": "function",
    "qualified_name": "_filterFold.label",
    "depth": 1
   },
   {
    "start_line": 472,
    "end_line": 472,
    "kind": "interface",
    "qualified_name": "CommentCounts",
    "depth": 0
   }
  ],
  "expect_runs": null
 },
 {
  "name": "semantic_code_review/viewer/assets/sidebar.ts:@@ -118,29 +121,84",
  "moved": false,
  "old_start": 118,
  "old_count": 29,
  "new_start": 121,
  "new_count": 84,
  "header": "@@ -118,29 +121,84 @@ function refreshThemes(groups: GroupBlock[]): void {",
  "body": "   }\n }\n \n-/** Derive the by-file axis from DATA.files. One pill per file with\n- *  hunks; pill ids `BF<fi>` (distinct ID space from themes' `G<i>`).\n- *  Skipped files (zero hunks) get no pill. */\n+/** Derive the by-file axis from DATA.files as a foldable directory\n+ *  tree. Files with hunks become leaf nodes (id `BF<fi>`, title the\n+ *  basename); their parent directories become interior nodes (id\n+ *  `BD<n>`) whose hunk_ids are the subtree union — clicking a directory\n+ *  filters to every changed hunk beneath it. A directory that holds a\n+ *  single subdirectory and no files is collapsed into its child\n+ *  (\"src/main\" as one node). Siblings (directories and files together)\n+ *  are sorted alphanumerically. Ids are distinct ID spaces from themes'\n+ *  `G<i>` and symbols' `SY<i>`. Skipped files (zero hunks) get no node.\n+ *  `groups` holds the roots; `byId` flattens every node for active-pill\n+ *  lookup. */\n function rebuildFilesAxis(): void {\n   if (!_data) return;\n   FILES_AXIS.groups.length = 0;\n   for (const k of Object.keys(FILES_AXIS.byId)) delete FILES_AXIS.byId[k];\n   for (const k of Object.keys(FILES_AXIS.hunkCount)) delete FILES_AXIS.hunkCount[k];\n+\n+  interface FileLeaf { fi: number; name: string; path: string; hunkIds: string[]; }\n+  interface FileDir { name: string; dirs: Map<string, FileDir>; files: FileLeaf[]; }\n+  const newDir = (name: string): FileDir => ({ name, dirs: new Map(), files: [] });\n+\n+  const root = newDir(\"\");\n   for (let fi = 0; fi < (_data.files || []).length; fi++) {\n     const f = _data.files[fi];\n     if (!f.hunks || f.hunks.length === 0) continue;\n-    const hunk_ids = f.hunks.map((h) => h.id);\n-    const g: GroupBlock = {\n-      id: `BF${fi}`,\n-      title: _shortenPath(f.path),\n-      rationale: f.path,\n-      hunk_ids,\n-    };\n-    FILES_AXIS.groups.push(g);\n+    const parts = f.path.split(\"/\");\n+    const fileName = parts.pop() as string;\n+    let cur = root;\n+    for (const seg of parts) {\n+      let next = cur.dirs.get(seg);\n+      if (!next) { next = newDir(seg); cur.dirs.set(seg, next); }\n+      cur = next;\n+    }\n+    cur.files.push({ fi, name: fileName, path: f.path, hunkIds: f.hunks.map((h) => h.id) });\n+  }\n+\n+  let dirSeq = 0;\n+  const leafGroup = (leaf: FileLeaf): GroupBlock =>\n+    ({ id: `BF${leaf.fi}`, title: leaf.name, rationale: leaf.path, hunk_ids: leaf.hunkIds });\n+\n+  // A directory's children (subdirs + files) as GroupBlocks, sorted\n+  // alphanumerically by display name. `prefix` is the parent's full\n+  // path, threaded so directory nodes carry their full path as tooltip.\n+  const childrenOf = (dir: FileDir, prefix: string): GroupBlock[] => {\n+    const out: GroupBlock[] = [];\n+    for (const sub of dir.dirs.values()) out.push(dirGroup(sub, prefix));\n+    for (const leaf of dir.files) out.push(leafGroup(leaf));\n+    out.sort((a, b) => a.title.localeCompare(b.title, undefined, { numeric: true }));\n+    return out;\n+  };\n+\n+  const dirGroup = (dir: FileDir, prefix: string): GroupBlock => {\n+    // Compress single-subdir chains: a directory with no files and\n+    // exactly one subdirectory merges its name into this node.\n+    let name = dir.name;\n+    let cur = dir;\n+    while (cur.files.length === 0 && cur.dirs.size === 1) {\n+      const only = cur.dirs.values().next().value as FileDir;\n+      name = `${name}/${only.name}`;\n+      cur = only;\n+    }\n+    const fullPath = prefix ? `${prefix}/${name}` : name;\n+    const children = childrenOf(cur, fullPath);\n+    const hunk_ids: string[] = [];\n+    for (const c of children) for (const hid of c.hunk_ids) hunk_ids.push(hid);\n+    return { id: `BD${dirSeq++}`, title: name, rationale: fullPath, hunk_ids, children };\n+  };\n+\n+  const register = (g: GroupBlock): void => {\n     FILES_AXIS.byId[g.id] = g;\n-    for (const hid of hunk_ids) {\n+    for (const hid of g.hunk_ids || []) {\n       FILES_AXIS.hunkCount[hid] = (FILES_AXIS.hunkCount[hid] || 0) + 1;\n     }\n+    for (const c of g.children || []) register(c);\n+  };\n+  for (const g of childrenOf(root, \"\")) {\n+    FILES_AXIS.groups.push(g);\n+    register(g);\n   }\n }\n \n",
  "head_spans": [
   {
    "start_line": 111,
    "end_line": 122,
    "kind": "function",
    "qualified_name": "refreshThemes",
    "depth": 0
   },
   {
    "start_line": 135,
    "end_line": 203,
    "kind": "function",
    "qualified_name": "rebuildFilesAxis",
    "depth": 0
   },
   {
    "start_line": 141,
    "end_line": 141,
    "kind": "interface",
    "qualified_name": "rebuildFilesAxis.FileLeaf",
    "depth": 1
   },
   {
    "start_line": 142,
    "end_line": 142,
    "kind": "interface",
    "qualified_name": "rebuildFilesAxis.FileDir",
    "depth": 1
   },
   {
    "start_line": 143,
    "end_line": 143,
    "kind": "function",
    "qualified_name": "rebuildFilesAxis.newDir",
    "depth": 1
   },
   {
    "start_line": 161,
    "end_line": 162,
    "kind": "function",
    "qualified_name": "rebuildFilesAxis.leafGroup",
    "depth": 1
   },
   {
    "start_line": 167,
    "end_line": 173,
    "kind": "function",
    "qualified_name": "rebuildFilesAxis.childrenOf",
    "depth": 1
   },
   {
    "start_line": 175,
    "end_line": 190,
    "kind": "function",
    "qualified_name": "rebuildFilesAxis.dirGroup",
    "depth": 1
   },
   {
    "start_line": 192,
    "end_line": 198,
    "kind": "function",
    "qualified_name": "rebuildFilesAxis.register",
    "depth": 1
   }
  ],
  "base_spans": [
   {
    "start_line": 108,
    "end_line": 119,
    "kind": "function",
    "qualified_name": "refreshThemes",
    "depth": 0
   },
   {
    "start_line": 124,
    "end_line": 145,
    "kind": "function",
    "qualified_name": "rebuildFilesAxis",
    "depth": 0
   },
   {
    "start_line": 154,
    "end_line": 169,
    "kind": "function",
    "qualified_name": "rebuildSymbolsAxis",
    "depth": 0
   },
   {
    "start_line": 158,
    "end_line": 164,
    "kind": "function",
    "qualified_name": "rebuildSymbolsAxis.register",
    "depth": 1
   },
   {
    "start_line": 173,
    "end_line": 205,
    "kind": "function",
    "qualified_name": "render",
    "depth": 0
   }
  ],
  "expect_runs": null
 },
 {
  "name": "semantic_code_review/viewer/assets/sidebar.ts:@@ -234,25 +293,31",
  "moved": false,
  "old_start": 234,
  "old_count": 25,
  "new_start": 293,
  "new_count": 31,
  "header": "@@ -234,25 +293,31 @@ function _pillButton(",
  "body": "   return btn;\n }\n \n-/** Render one symbol-tree node: an optional expand/collapse toggle, the\n- *  node's own filtering pill, and (recursively) its children indented\n- *  beneath it. Ancestors are shown for context even when unchanged;\n- *  clicking the pill filters to the subtree union, clicking a leaf to\n- *  just that symbol's hunks. */\n-function _symbolNode(axis: SidebarAxis, g: GroupBlock): HTMLElement {\n+/** Render one tree node (Files directory tree or Symbols class ▸ method\n+ *  tree): an optional expand/collapse toggle, the node's own filtering\n+ *  pill, and (recursively) its children indented beneath it. Interior\n+ *  nodes are shown for context even when they aren't themselves changed;\n+ *  clicking a node's pill filters to its subtree union, clicking a leaf\n+ *  to just that file's/symbol's hunks. `commentCounts` (Files axis only)\n+ *  is passed through to every pill. */\n+function _treeNode(\n+  axis: SidebarAxis,\n+  g: GroupBlock,\n+  commentCounts: Record<string, CommentCounts> | null,\n+): HTMLElement {\n   const node = _el(\"div\", \"group-tree-node\");\n   const row = _el(\"div\", \"group-tree-row\");\n   const kids = g.children || [];\n   let childWrap: HTMLElement | null = null;\n   if (kids.length > 0) {\n-    const collapsed = _collapsedSymbols.has(g.id);\n+    const collapsed = _collapsedNodes.has(g.id);\n     const toggle = _el(\"button\", \"group-tree-toggle\", collapsed ? \"▸\" : \"▾\");\n     toggle.title = collapsed ? \"Expand\" : \"Collapse\";\n     toggle.addEventListener(\"click\", (e) => {\n       e.stopPropagation();\n-      const nowCollapsed = !_collapsedSymbols.has(g.id);\n-      if (nowCollapsed) _collapsedSymbols.add(g.id);\n-      else _collapsedSymbols.delete(g.id);\n+      const nowCollapsed = !_collapsedNodes.has(g.id);\n+      if (nowCollapsed) _collapsedNodes.add(g.id);\n+      else _collapsedNodes.delete(g.id);\n       toggle.textContent = nowCollapsed ? \"▸\" : \"▾\";\n       toggle.title = nowCollapsed ? \"Expand\" : \"Collapse\";\n       if (childWrap) childWrap.style.display = nowCollapsed ? \"none\" : \"\";\n",
  "head_spans": [
   {
    "start_line": 231,
    "end_line": 264,
    "kind": "function",
    "qualified_name": "render",
    "depth": 0
   },
   {
    "start_line": 269,
    "end_line": 294,
    "kind": "function",
    "qualified_name": "_pillButton",
    "depth": 0
   },
   {
    "start_line": 303,
    "end_line": 339,
    "kind": "function",
    "qualified_name": "_treeNode",
    "depth": 0
   }
  ],
  "base_spans": [
   {
    "start_line": 210,
    "end_line": 235,
    "kind": "function",
    "qualified_name": "_pillButton",
    "depth": 0
   },
   {
    "start_line": 242,
    "end_line": 274,
    "kind": "function",
    "qualified_name": "_symbolNode",
    "depth": 0
   },
   {
    "start_line": 276,
    "end_line": 280,
    "kind": "function",
    "qualified_name": "_isActivePill",
    "depth": 0
   },
   {
    "start_line": 282,
    "end_line": 302,
    "kind": "function",
    "qualified_name": "setActivePill",
    "depth": 0
   },
   {
    "start_line": 307,
    "end_line": 311,
    "kind": "function",
    "qualified_name": "_activeSymbolName",
    "depth": 0
   },
   {
    "start_line": 313,
    "end_line": 315,
    "kind": "function",
    "qualified_name": "_emitActivePill",
    "depth": 0
   },
   {
    "start_line": 317,
    "end_line": 323,
    "kind": "function",
    "qualified_name": "_activePillHunkIds",
    "depth": 0
   }
  ],
  "expect_runs": null
 },
 {
  "name": "semantic_code_review/viewer/assets/folds.ts:@@ -108,32 +136,128",
  "moved": true,
  "old_start": 108,
  "old_count": 32,
  "new_start": 136,
  "new_count": 128,
  "header": "@@ -108,32 +136,128 @@ function _teardownFileFolds(fileId: string): void {",
  "body": " }\n \n // Walk the file body's .diff / .gap-expansion containers in DOM order,\n-// pull each one's row stream out of `FileRows` (recorded by render.ts\n-// at construction time), and flatten into one indexable list so folds\n-// can straddle hunks and adjacent gap-context.\n+// pull each one's rows out of `FileRows` (recorded by render.ts at\n+// construction time), and flatten into one indexable list so folds can\n+// straddle hunks and adjacent gap-context.\n+//\n+// These are the rows this render put on screen, which is where a chevron\n+// can hang — never the detection input. What a file's regions *are*\n+// comes from its content, so it does not depend on what is on screen.\n function _collectFileRows(fileEl: HTMLElement): RowWithEls[] {\n   const body = fileEl.querySelector(\".file-body\");\n   if (!body) return [];\n-  const out: RowWithEls[] = [];\n+  const placed: RowWithEls[] = [];\n   for (const child of Array.from(body.children) as HTMLElement[]) {\n     const cls = child.classList;\n-    let source: HTMLElement | null = null;\n+    let container: HTMLElement | null = null;\n     if (cls.contains(\"hunk\")) {\n-      source = child.querySelector(\".diff\");\n+      container = child.querySelector(\".diff\");\n     } else if (cls.contains(\"gap-expansion\")) {\n-      source = child;\n+      container = child;\n     }\n-    if (!source) continue;\n-    const entry = FileRows.get(source);\n+    if (!container) continue;\n+    const entry = FileRows.get(container);\n     if (!entry) continue;\n     for (let i = 0; i < entry.rows.length; i++) {\n-      out.push({\n+      placed.push({\n         ...entry.rows[i],\n         oldEl: entry.oldEls[i], newEl: entry.newEls[i],\n       });\n     }\n   }\n-  return out;\n+  return placed;\n+}\n+\n+// The file's whole row stream: unchanged head context interleaved with\n+// the hunks' own rows, in file order. This is the detection input — it\n+// depends only on the file's content, so the regions it yields (and the\n+// absolute line spans that identify them) are the same no matter which\n+// part of the file is currently rendered.\n+//\n+// Returns null when the file has no content to read: `/file-text` has\n+// not answered yet, or it served neither side (a binary file, or one\n+// over the route's 2 MB per-side cap). No content means no *new* fold is\n+// offered — a region detected off the rendered rows would move when the\n+// reviewer revealed context, which is #10, and offering it would be a\n+// fix that works everywhere except where it silently does not. A\n+// `CodeFold` the reviewer already collapsed needs none of this: its\n+// `HiddenSpan` describes itself and the renderer honours it at first\n+// paint, before any of this has run.\n+function _fileRowStream(file: FileBlock): RowBlock[] | null {\n+  if (!FileText.hasContent(file.id)) return null;\n+  const rows: RowBlock[] = [];\n+  let cn = 1;\n+  let co = 1;\n+  for (const h of file.hunks || []) {\n+    const b = FileText.hunkBounds(h);\n+    for (const r of FileText.contextRows(file.id, cn, b.firstNew, co)) rows.push(r);\n+    for (const r of h.rows || []) rows.push(r);\n+    cn = b.nextNew;\n+    co = b.nextOld;\n+  }\n+  const end = FileText.tailEnd(file.id, cn, co);\n+  if (end !== null) {\n+    for (const r of FileText.contextRows(file.id, cn, end + 1, co)) rows.push(r);\n+  }\n+  return rows;\n+}\n+\n+// Detection is O(rows x definition spans) over the whole file, so it is\n+// memoised per FileBlock and only re-run when the content it reads\n+// changes: an SSE `hunk` event swaps the HunkBlock object (DataStore\n+// .replaceHunk), which the identity check below catches. A file whose\n+// content has not landed is not cached — the next render re-asks.\n+interface DetectionCache {\n+  hunks: HunkBlock[];\n+  regions: DetectedRegion[];\n+}\n+\n+const _DETECTION_CACHE = new WeakMap<FileBlock, DetectionCache>();\n+\n+function _detectFileRegions(file: FileBlock): DetectedRegion[] {\n+  const stream = _fileRowStream(file);\n+  if (stream === null) return [];\n+  const syms = file.fold_symbols || { head: [], base: [] };\n+  const hunks = file.hunks || [];\n+  const cached = _DETECTION_CACHE.get(file);\n+  if (cached\n+      && cached.hunks.length === hunks.length\n+      && cached.hunks.every((h, i) => h === hunks[i])) {\n+    return cached.regions;\n+  }\n+  const regions = _computeFoldRegions(stream, syms.head, syms.base);\n+  _DETECTION_CACHE.set(file, { hunks: hunks.slice(), regions });\n+  return regions;\n+}\n+\n+// Which of the rows this render produced the chevron hangs off: the first\n+// one inside the region's span on either side, even when the definition's\n+// own opening line is not rendered — the chevron has to sit on a row that\n+// exists. A collapsed region has exactly that one row on screen (the\n+// renderer drops the rest), so it always places; an expanded one needs at\n+// least two, or there is nothing to fold away.\n+function _placeRegion(\n+  det: DetectedRegion, rows: RowBlock[], collapsed: boolean,\n+): number | null {\n+  let first = -1;\n+  let count = 0;\n+  for (let i = 0; i < rows.length; i++) {\n+    if (!_rowInRegion(rows[i], det)) continue;\n+    if (first < 0) first = i;\n+    count++;\n+  }\n+  if (first < 0) return null;\n+  if (!collapsed && count < 2) return null;\n+  return first;\n+}\n+\n+function _rowInRegion(row: RowBlock, det: DetectedRegion): boolean {\n+  if (det.right_start != null && det.right_end != null && row.new_line != null\n+      && row.new_line >= det.right_start && row.new_line <= det.right_end) {\n+    return true;\n+  }\n+  return det.left_start != null && det.left_end != null && row.old_line != null\n+    && row.old_line >= det.left_start && row.old_line <= det.left_end;\n }\n \n function _findExistingFoldRecord(\n",
  "head_spans": [
   {
    "start_line": 100,
    "end_line": 116,
    "kind": "function",
    "qualified_name": "_chev",
    "depth": 0
   },
   {
    "start_line": 118,
    "end_line": 122,
    "kind": "function",
    "qualified_name": "_isRowContentEmpty",
    "depth": 0
   },
   {
    "start_line": 126,
    "end_line": 136,
    "kind": "function",
    "qualified_name": "_teardownFileFolds",
    "depth": 0
   },
   {
    "start_line": 146,
    "end_line": 169,
    "kind": "function",
    "qualified_name": "_collectFileRows",
    "depth": 0
   },
   {
    "start_line": 186,
    "end_line": 203,
    "kind": "function",
    "qualified_name": "_fileRowStream",
    "depth": 0
   },
   {
    "start_line": 210,
    "end_line": 213,
    "kind": "interface",
    "qualified_name": "DetectionCache",
    "depth": 0
   },
   {
    "start_line": 217,
    "end_line": 231,
    "kind": "function",
    "qualified_name": "_detectFileRegions",
    "depth": 0
   },
   {
    "start_line": 239,
    "end_line": 252,
    "kind": "function",
    "qualified_name": "_placeRegion",
    "depth": 0
   },
   {
    "start_line": 254,
    "end_line": 261,
    "kind": "function",
    "qualified_name": "_rowInRegion",
    "depth": 0
   },
   {
    "start_line": 263,
    "end_line": 278,
    "kind": "function",
    "qualified_name": "_findExistingFoldRecord",
    "depth": 0
   }
  ],
  "base_spans": [
   {
    "start_line": 98,
    "end_line": 108,
    "kind": "function",
    "qualified_name": "_teardownFileFolds",
    "depth": 0
   },
   {
    "start_line": 114,
    "end_line": 137,
    "kind": "function",
    "qualified_name": "_collectFileRows",
    "depth": 0
   },
   {
    "start_line": 139,
    "end_line": 156,
    "kind": "function",
    "qualified_name": "_findExistingFoldRecord",
    "depth": 0
   },
   {
    "start_line": 158,
    "end_line": 195,
    "kind": "function",
    "qualified_name": "_upsertFoldRegion",
    "depth": 0
   },
   {
    "start_line": 197,
    "end_line": 205,
    "kind": "function",
    "qualified_name": "_anyChangesInRange",
    "depth": 0
   },
   {
    "start_line": 209,
    "end_line": 219,
    "kind": "function",
    "qualified_name": "_rowIndent",
    "depth": 0
   },
   {
    "start_line": 221,
    "end_line": 246,
    "kind": "function",
    "qualified_name": "_indentRawRegions",
    "depth": 0
   },
   {
    "start_line": 223,
    "end_line": 228,
    "kind": "function",
    "qualified_name": "_indentRawRegions.nextNonBlank",
    "depth": 1
   },
   {
    "start_line": 251,
    "end_line": 262,
    "kind": "function",
    "qualified_name": "_rowSymbols",
    "depth": 0
   }
  ],
  "expect_runs": [
   {
    "kind": "ins",
    "start_idx": 6,
    "texts": [
     "//",
     "// These are the rows this render put on screen, which is where a chevron",
     "// can hang — never the detection input. What a file's regions *are*",
     "// comes from its content, so it does not depend on what is on screen."
    ]
   },
   {
    "kind": "ins",
    "start_idx": 35,
    "texts": [
     "// The file's whole row stream: unchanged head context interleaved with",
     "// the hunks' own rows, in file order. This is the detection input — it",
     "// depends only on the file's content, so the regions it yields (and the",
     "// absolute line spans that identify them) are the same no matter which",
     "// part of the file is currently rendered.",
     "//",
     "// Returns null when the file has no content to read: `/file-text` has",
     "// not answered yet, or it served neither side (a binary file, or one",
     "// over the route's 2 MB per-side cap). No content means no *new* fold is",
     "// offered — a region detected off the rendered rows would move when the",
     "// reviewer revealed context, which is #10, and offering it would be a",
     "// fix that works everywhere except where it silently does not. A",
     "// `CodeFold` the reviewer already collapsed needs none of this: its",
     "// `HiddenSpan` describes itself and the renderer honours it at first",
     "// paint, before any of this has run.",
     "function _fileRowStream(file: FileBlock): RowBlock[] | null {",
     "  if (!FileText.hasContent(file.id)) return null;",
     "  const rows: RowBlock[] = [];",
     "  let cn = 1;",
     "  let co = 1;",
     "  for (const h of file.hunks || []) {",
     "    const b = FileText.hunkBounds(h);",
     "    for (const r of FileText.contextRows(file.id, cn, b.firstNew, co)) rows.push(r);",
     "    for (const r of h.rows || []) rows.push(r);",
     "    cn = b.nextNew;",
     "    co = b.nextOld;",
     "  }",
     "  const end = FileText.tailEnd(file.id, cn, co);",
     "  if (end !== null) {",
     "    for (const r of FileText.contextRows(file.id, cn, end + 1, co)) rows.push(r);",
     "  }",
     "  return rows;",
     "}",
     "",
     "// Detection is O(rows x definition spans) over the whole file, so it is",
     "// memoised per FileBlock and only re-run when the content it reads",
     "// changes: an SSE `hunk` event swaps the HunkBlock object (DataStore",
     "// .replaceHunk), which the identity check below catches. A file whose",
     "// content has not landed is not cached — the next render re-asks.",
     "interface DetectionCache {",
     "  hunks: HunkBlock[];",
     "  regions: DetectedRegion[];",
     "}",
     "",
     "const _DETECTION_CACHE = new WeakMap<FileBlock, DetectionCache>();",
     "",
     "function _detectFileRegions(file: FileBlock): DetectedRegion[] {",
     "  const stream = _fileRowStream(file);",
     "  if (stream === null) return [];",
     "  const syms = file.fold_symbols || { head: [], base: [] };",
     "  const hunks = file.hunks || [];",
     "  const cached = _DETECTION_CACHE.get(file);",
     "  if (cached",
     "      && cached.hunks.length === hunks.length",
     "      && cached.hunks.every((h, i) => h === hunks[i])) {",
     "    return cached.regions;",
     "  }",
     "  const regions = _computeFoldRegions(stream, syms.head, syms.base);",
     "  _DETECTION_CACHE.set(file, { hunks: hunks.slice(), regions });",
     "  return regions;",
     "}",
     "",
     "// Which of the rows this render produced the chevron hangs off: the first",
     "// one inside the region's span on either side, even when the definition's",
     "// own opening line is not rendered — the chevron has to sit on a row that",
     "// exists. A collapsed region has exactly that one row on screen (the",
     "// renderer drops the rest), so it always places; an expanded one needs at",
     "// least two, or there is nothing to fold away.",
     "function _placeRegion(",
     "  det: DetectedRegion, rows: RowBlock[], collapsed: boolean,",
     "): number | null {",
     "  let first = -1;",
     "  let count = 0;",
     "  for (let i = 0; i < rows.length; i++) {",
     "    if (!_rowInRegion(rows[i], det)) continue;",
     "    if (first < 0) first = i;",
     "    count++;",
     "  }",
     "  if (first < 0) return null;",
     "  if (!collapsed && count < 2) return null;",
     "  return first;",
     "}",
     "",
     "function _rowInRegion(row: RowBlock, det: DetectedRegion): boolean {",
     "  if (det.right_start != null && det.right_end != null && row.new_line != null",
     "      && row.new_line >= det.right_start && row.new_line <= det.right_end) {",
     "    return true;",
     "  }",
     "  return det.left_start != null && det.left_end != null && row.old_line != null",
     "    && row.old_line >= det.left_start && row.old_line <= det.left_end;",
     "}",
     ""
    ]
   }
  ]
 },
 {
  "name": "semantic_code_review/viewer/assets/folds.ts:@@ -472,76 +626,108",
  "moved": false,
  "old_start": 472,
  "old_count": 76,
  "new_start": 626,
  "new_count": 108,
  "header": "@@ -472,76 +626,108 @@ function _attachOneFold(",
  "body": "   const anchor = side === \"new\" ? headerNew : headerOld;\n   const shadow = side === \"new\" ? headerOld : headerNew;\n \n-  const marker = _chev(false, \"fold-chev\");\n+  const marker = _chev(collapsed, \"fold-chev\");\n   marker.setAttribute(\"role\", \"button\");\n   marker.setAttribute(\"tabindex\", \"0\");\n \n+  // Attached before the summary box because each annotation inserts\n+  // itself directly under the anchor: last attached sits nearest the\n+  // row, so the summary ends up between the header and the manifest.\n+  const manifestHandle = collapsed\n+    ? _attachFoldManifest(file, det, headerRow, anchor, shadow, notes)\n+    : null;\n+\n   let foldHandle: AnnotationHandle | null = null;\n-  const canSummarise = _canRequestFoldSummary(fileIdx, region);\n+  const canSummarise = _canRequestFoldSummary(fileIdx, det);\n   if (region.summary || region.has_changes || canSummarise) {\n-    // Seed the placeholder with the symbol identity (if any) followed by\n-    // the summary or its pending/run-augment stand-in.\n-    const label = _foldLabel(region);\n-    const pending = !region.summary && canSummarise;\n-    const bodyText = region.summary\n-      || (canSummarise\n-        ? \"summarising…\"\n-        : \"(changes here; run augment to generate a description)\");\n+    // The placeholder is built either way and shown only while the region\n+    // is collapsed, so opening and closing it needs no rebuild.\n     foldHandle = Annotations.attach({\n       anchor, shadowAnchor: shadow,\n-      variant: \"fold\", content: label + bodyText,\n+      variant: \"fold\", content: _foldBoxText(region, canSummarise),\n     });\n-    if (!region.summary) {\n-      const box = foldHandle.element.querySelector(\".annot-box\");\n-      if (box) box.classList.add(\"missing\");\n-      if (pending && box) box.classList.add(\"pending\");\n+    const box = foldHandle.element.querySelector(\".annot-box\") as HTMLElement | null;\n+    if (box && !region.summary) {\n+      box.classList.add(\"missing\");\n+      if (region._summaryFailed) {\n+        box.classList.add(\"failed\");\n+        box.style.cursor = \"pointer\";\n+        box.addEventListener(\"click\", () => {\n+          _requestFoldSummary(fileIdx, det, region, repaint);\n+          repaint();\n+        });\n+      } else if (canSummarise) {\n+        box.classList.add(\"pending\");\n+      }\n+    }\n+    foldHandle.element.style.display = collapsed ? \"\" : \"none\";\n+    if (foldHandle.placeholder) {\n+      foldHandle.placeholder.style.display = collapsed ? \"\" : \"none\";\n     }\n-    foldHandle.element.style.display = \"none\";\n-    if (foldHandle.placeholder) foldHandle.placeholder.style.display = \"none\";\n+    if (collapsed) foldHandle.resize();\n   }\n \n   marker.addEventListener(\"click\", (e) => {\n     e.stopPropagation();\n-    const nowOpen = marker.classList.toggle(\"open\");\n-    for (let i = bodyStart; i <= bodyEnd; i++) {\n-      const r = rows[i];\n-      if (!r) continue;\n-      if (r.oldEl) r.oldEl.style.display = nowOpen ? \"\" : \"none\";\n-      if (r.newEl) r.newEl.style.display = nowOpen ? \"\" : \"none\";\n-    }\n-    if (foldHandle) {\n-      foldHandle.element.style.display = nowOpen ? \"none\" : \"\";\n-      if (foldHandle.placeholder) {\n-        foldHandle.placeholder.style.display = nowOpen ? \"none\" : \"\";\n-      }\n-      if (!nowOpen) foldHandle.resize();\n-    }\n-    if (!nowOpen && !region.summary && foldHandle\n-        && _canRequestFoldSummary(fileIdx, region)) {\n-      _requestFoldSummary(fileIdx, region, foldHandle);\n+    const nowHidden = Visibility.toggle(\n+      Visibility.codeFoldSpan(file.id, det, \"user\"),\n+    );\n+    if (nowHidden && !region.summary && _canRequestFoldSummary(fileIdx, det)) {\n+      _requestFoldSummary(fileIdx, det, region, repaint);\n     }\n-    Annotations.reflow(anchor);\n+    repaint();\n   });\n \n   const contentCell = anchor && (anchor.children[1] as HTMLElement | undefined);\n   if (contentCell) contentCell.prepend(marker);\n-  return { marker, foldHandle };\n+  return { marker, foldHandle, manifestHandle };\n }\n \n-function attachFileFolds(fileEl: HTMLElement, file: FileBlock): void {\n+/** The manifest of what a collapsed region is holding down, hung under\n+ *  the header row the chevron sits on.\n+ *\n+ *  The header row is the one line of the span that still renders, so a\n+ *  note on it is already on screen with its own comment row; listing it\n+ *  here as well would say the same thing twice.\n+ */\n+function _attachFoldManifest(\n+  file: FileBlock, det: DetectedRegion, headerRow: RowWithEls,\n+  anchor: HTMLElement, shadow: HTMLElement, notes: ManifestNote[],\n+): AnnotationHandle | null {\n+  const spanId = Visibility.codeFoldSpanId(file.id, det);\n+  const hidden = Manifest.under(file.id, spanId, notes).filter(\n+    (n) => n.line !== (n.side === \"new\" ? headerRow.new_line : headerRow.old_line),\n+  );\n+  const content = Manifest.render(hidden);\n+  if (!content) return null;\n+  return Annotations.attach({\n+    anchor, shadowAnchor: shadow, variant: \"manifest\", content,\n+  });\n+}\n+\n+function attachFileFolds(\n+  fileEl: HTMLElement, file: FileBlock, notes: ManifestNote[],\n+  repaint: () => void,\n+): void {\n   _teardownFileFolds(file.id);\n   const fileIdx = Number(file.id.replace(\"F\", \"\"));\n   const rows = _collectFileRows(fileEl);\n   if (rows.length === 0) return;\n-  const syms = file.fold_symbols || { head: [], base: [] };\n-  const detected = _computeFoldRegions(rows, syms.head, syms.base);\n   const handles: AnnotationHandle[] = [];\n   const chevrons: SVGElement[] = [];\n-  for (const det of detected) {\n-    const region = _upsertFoldRegion(file, det, rows);\n-    const attached = _attachOneFold(rows, region, fileIdx);\n+  for (const det of _detectFileRegions(file)) {\n+    const collapsed = Visibility.isHidden(\n+      file.id, Visibility.codeFoldSpanId(file.id, det),\n+    );\n+    const headerIdx = _placeRegion(det, rows, collapsed);\n+    if (headerIdx === null) continue;   // nothing of this region is on screen\n+    const region = _upsertFoldRegion(file, det);\n+    const attached = _attachOneFold(\n+      rows, file, det, region, headerIdx, fileIdx, collapsed, notes, repaint,\n+    );\n     if (!attached) continue;\n     if (attached.foldHandle) handles.push(attached.foldHandle);\n+    if (attached.manifestHandle) handles.push(attached.manifestHandle);\n     if (attached.marker) chevrons.push(attached.marker);\n   }\n   _FILE_FOLD_STATE[file.id] = { handles, chevrons };\n",
  "head_spans": [
   {
    "start_line": 444,
    "end_line": 497,
    "kind": "function",
    "qualified_name": "_computeFoldRegions",
    "depth": 0
   },
   {
    "start_line": 499,
    "end_line": 504,
    "kind": "function",
    "qualified_name": "_indentRawRegion",
    "depth": 0
   },
   {
    "start_line": 506,
    "end_line": 514,
    "kind": "function",
    "qualified_name": "_firstLine",
    "depth": 0
   },
   {
    "start_line": 516,
    "end_line": 524,
    "kind": "function",
    "qualified_name": "_lastLine",
    "depth": 0
   },
   {
    "start_line": 528,
    "end_line": 533,
    "kind": "function",
    "qualified_name": "_canRequestFoldSummary",
    "depth": 0
   },
   {
    "start_line": 539,
    "end_line": 553,
    "kind": "function",
    "qualified_name": "_foldAddress",
    "depth": 0
   },
   {
    "start_line": 558,
    "end_line": 562,
    "kind": "function",
    "qualified_name": "_foldLabel",
    "depth": 0
   },
   {
    "start_line": 569,
    "end_line": 594,
    "kind": "function",
    "qualified_name": "_requestFoldSummary",
    "depth": 0
   },
   {
    "start_line": 578,
    "end_line": 582,
    "kind": "function",
    "qualified_name": "_requestFoldSummary.settle",
    "depth": 1
   },
   {
    "start_line": 597,
    "end_line": 603,
    "kind": "function",
    "qualified_name": "_foldBoxText",
    "depth": 0
   },
   {
    "start_line": 613,
    "end_line": 684,
    "kind": "function",
    "qualified_name": "_attachOneFold",
    "depth": 0
   },
   {
    "start_line": 693,
    "end_line": 706,
    "kind": "function",
    "qualified_name": "_attachFoldManifest",
    "depth": 0
   },
   {
    "start_line": 708,
    "end_line": 734,
    "kind": "function",
    "qualified_name": "attachFileFolds",
    "depth": 0
   }
  ],
  "base_spans": [
   {
    "start_line": 457,
    "end_line": 529,
    "kind": "function",
    "qualified_name": "_attachOneFold",
    "depth": 0
   },
   {
    "start_line": 531,
    "end_line": 548,
    "kind": "function",
    "qualified_name": "attachFileFolds",
    "depth": 0
   }
  ],
  "expect_runs": null
 },
 {
  "name": "semantic_code_review/viewer/assets/render.ts:@@ -346,22 +420,66",
  "moved": true,
  "old_start": 346,
  "old_count": 22,
  "new_start": 420,
  "new_count": 66,
  "header": "@@ -346,22 +420,66 @@ function _renderFileBody(",
  "body": "     const demoted = f.hunks.filter(\n       (h) => !isLive(h) && h.new_start >= curNew && (endNew === null || h.new_start <= endNew),\n     );\n-    const { rows, marks } = _regionRows(f, curNew, endNew, curOld, demoted);\n-    if (rows.length === 0) return;\n-    body.appendChild(_renderRegionChip(f, { position, rows, marks }));\n+    const pieces = _regionPieces(f, curNew, endNew, curOld, demoted);\n+    for (let i = 0; i < pieces.length; i++) {\n+      const piece = pieces[i];\n+      // Only the outermost pieces inherit the flush's position: an\n+      // absent band or a chip in the middle of one reads as \"between\".\n+      const pos = i === 0 ? position\n+        : i === pieces.length - 1 && position === \"bottom\" ? \"bottom\" : \"between\";\n+      if (\"absent\" in piece) {\n+        // Only between two rendered things: at the head or the foot of a\n+        // file nothing precedes or follows the missing lines, so there\n+        // is no step in the gutter for the band to account for.\n+        if (pos === \"between\") {\n+          body.appendChild(_renderAbsentContext(piece.absent, notes));\n+        }\n+        continue;\n+      }\n+      _renderRegion(body, f, { position: pos, ...piece }, headed, notes);\n+    }\n   };\n \n   // Just after a sidebar pill click, reveal the focused hunks' code\n   // (ephemeral); the fold-level slider clears the flag and takes over.\n   const reveal = liveIds !== null && _state.focusReveal;\n   for (const h of f.hunks.filter(isLive)) {\n-    flush(h.new_start - 1, emittedLive ? \"between\" : \"top\");\n-    body.appendChild(_renderHunk(h, f, reveal));\n+    const b = FileText.hunkBounds(h);\n+    flush(b.firstNew - 1, emittedLive ? \"between\" : \"top\");\n+    body.appendChild(_renderHunk(h, f, headed, notes, reveal));\n     emittedLive = true;\n-    curNew = h.new_start + h.new_count;\n-    curOld = h.old_start + h.old_count;\n+    curNew = b.nextNew;\n+    curOld = b.nextOld;\n   }\n-  flush(total, \"bottom\");\n+  flush(FileText.tailEnd(f.id, curNew, curOld), \"bottom\");\n+}\n+\n+/** The band standing in for unchanged lines the viewer has no text for:\n+ *  `/file-text` has not answered yet, or it served neither side — a\n+ *  binary file, or one over the route's 2 MB per-side cap.\n+ *\n+ *  Inert, and no `HiddenSpan`: there are no rows to reveal, so an expand\n+ *  chip would open onto nothing and a span would be a record no click\n+ *  could retract. What it adds over rendering nothing is that the lines\n+ *  are named, and that the notes sitting on them have somewhere to\n+ *  appear — an ingested comment on unchanged context in a file the route\n+ *  cannot serve would otherwise be nowhere in the page.\n+ */\n+function _renderAbsentContext(\n+  range: AbsentRange, notes: ManifestNote[],\n+): HTMLElement {\n+  const count = range.endNew - range.startNew + 1;\n+  const band = _el(\"div\", \"gap-chip gap-absent\");\n+  band.title = \"The viewer has no text for these lines, so they cannot be\"\n+    + \" expanded.\";\n+  const word = count === 1 ? \"line\" : \"lines\";\n+  band.innerHTML = `<span class=\"gap-icon\">⋯</span>`\n+    + `<span class=\"gap-label\">${count} unchanged ${word} (not loaded)</span>`;\n+  return _withManifest(band, Manifest.inRange(\n+    notes,\n+    { start: range.startNew, end: range.endNew },\n+    { start: range.startOld, end: range.startOld + count - 1 },\n+  ));\n }\n \n function _renderFileHeader(f: FileBlock, folded: boolean): HTMLElement {\n",
  "head_spans": [
   {
    "start_line": 344,
    "end_line": 354,
    "kind": "function",
    "qualified_name": "_renderPRPanel",
    "depth": 0
   },
   {
    "start_line": 360,
    "end_line": 400,
    "kind": "function",
    "qualified_name": "_renderFile",
    "depth": 0
   },
   {
    "start_line": 410,
    "end_line": 455,
    "kind": "function",
    "qualified_name": "_renderFileBody",
    "depth": 0
   },
   {
    "start_line": 414,
    "end_line": 414,
    "kind": "function",
    "qualified_name": "_renderFileBody.isLive",
    "depth": 1
   },
   {
    "start_line": 419,
    "end_line": 441,
    "kind": "function",
    "qualified_name": "_renderFileBody.flush",
    "depth": 1
   },
   {
    "start_line": 468,
    "end_line": 483,
    "kind": "function",
    "qualified_name": "_renderAbsentContext",
    "depth": 0
   },
   {
    "start_line": 485,
    "end_line": 503,
    "kind": "function",
    "qualified_name": "_renderFileHeader",
    "depth": 0
   }
  ],
  "base_spans": [
   {
    "start_line": 336,
    "end_line": 365,
    "kind": "function",
    "qualified_name": "_renderFileBody",
    "depth": 0
   },
   {
    "start_line": 345,
    "end_line": 352,
    "kind": "function",
    "qualified_name": "_renderFileBody.flush",
    "depth": 1
   },
   {
    "start_line": 367,
    "end_line": 385,
    "kind": "function",
    "qualified_name": "_renderFileHeader",
    "depth": 0
   },
   {
    "start_line": 390,
    "end_line": 401,
    "kind": "function",
    "qualified_name": "_renderMdToggle",
    "depth": 0
   },
   {
    "start_line": 403,
    "end_line": 410,
    "kind": "function",
    "qualified_name": "_uniqueFileSmells",
    "depth": 0
   },
   {
    "start_line": 412,
    "end_line": 422,
    "kind": "function",
    "qualified_name": "_renderFileOverview",
    "depth": 0
   },
   {
    "start_line": 432,
    "end_line": 436,
    "kind": "interface",
    "qualified_name": "DiffRegion",
    "depth": 0
   },
   {
    "start_line": 442,
    "end_line": 469,
    "kind": "function",
    "qualified_name": "_regionRows",
    "depth": 0
   },
   {
    "start_line": 450,
    "end_line": 458,
    "kind": "function",
    "qualified_name": "_regionRows.ctxTo",
    "depth": 1
   },
   {
    "start_line": 471,
    "end_line": 485,
    "kind": "function",
    "qualified_name": "_renderRegionChip",
    "depth": 0
   },
   {
    "start_line": 487,
    "end_line": 502,
    "kind": "function",
    "qualified_name": "_renderRegionExpansion",
    "depth": 0
   }
  ],
  "expect_runs": [
   {
    "kind": "ins",
    "start_idx": 6,
    "texts": [
     "      // Only the outermost pieces inherit the flush's position: an",
     "      // absent band or a chip in the middle of one reads as \"between\".",
     "      const pos = i === 0 ? position",
     "        : i === pieces.length - 1 && position === \"bottom\" ? \"bottom\" : \"between\";",
     "      if (\"absent\" in piece) {",
     "        // Only between two rendered things: at the head or the foot of a",
     "        // file nothing precedes or follows the missing lines, so there",
     "        // is no step in the gutter for the band to account for.",
     "        if (pos === \"between\") {",
     "          body.appendChild(_renderAbsentContext(piece.absent, notes));",
     "        }",
     "        continue;",
     "      }",
     "      _renderRegion(body, f, { position: pos, ...piece }, headed, notes);",
     "    }"
    ]
   },
   {
    "kind": "ins",
    "start_idx": 37,
    "texts": [
     "/** The band standing in for unchanged lines the viewer has no text for:",
     " *  `/file-text` has not answered yet, or it served neither side — a",
     " *  binary file, or one over the route's 2 MB per-side cap.",
     " *",
     " *  Inert, and no `HiddenSpan`: there are no rows to reveal, so an expand",
     " *  chip would open onto nothing and a span would be a record no click",
     " *  could retract. What it adds over rendering nothing is that the lines",
     " *  are named, and that the notes sitting on them have somewhere to",
     " *  appear — an ingested comment on unchanged context in a file the route",
     " *  cannot serve would otherwise be nowhere in the page.",
     " */",
     "function _renderAbsentContext(",
     "  range: AbsentRange, notes: ManifestNote[],",
     "): HTMLElement {",
     "  const count = range.endNew - range.startNew + 1;",
     "  const band = _el(\"div\", \"gap-chip gap-absent\");",
     "  band.title = \"The viewer has no text for these lines, so they cannot be\"",
     "    + \" expanded.\";",
     "  const word = count === 1 ? \"line\" : \"lines\";",
     "  band.innerHTML = `<span class=\"gap-icon\">⋯</span>`",
     "    + `<span class=\"gap-label\">${count} unchanged ${word} (not loaded)</span>`;",
     "  return _withManifest(band, Manifest.inRange(",
     "    notes,",
     "    { start: range.startNew, end: range.endNew },",
     "    { start: range.startOld, end: range.startOld + count - 1 },",
     "  ));",
     "}",
     ""
    ]
   }
  ]
 },
 {
  "name": "semantic_code_review/viewer/assets/render.ts:@@ -435,40 +553,123",
  "moved": false,
  "old_start": 435,
  "old_count": 40,
  "new_start": 553,
  "new_count": 123,
  "header": "@@ -435,40 +553,123 @@ interface DiffRegion {",
  "body": "   marks: (_RowMarks | undefined)[];\n }\n \n-/** The row stream for a region: unchanged context (from head_lines)\n- *  interleaved with the demoted hunks' own rows, in file order. `newEnd`\n- *  bounds the trailing context; null (no head_lines) skips context\n- *  entirely, leaving just the hunk rows. */\n-function _regionRows(\n-  f: FileBlock, newStart: number, newEnd: number | null, oldStart: number, hunks: HunkBlock[],\n-): { rows: RowBlock[]; marks: (_RowMarks | undefined)[] } {\n-  const rows: RowBlock[] = [];\n-  const marks: (_RowMarks | undefined)[] = [];\n-  const hl = f.head_lines;\n+/** A run of unchanged lines with no text behind it, so nothing can be\n+ *  rendered for them but the band. */\n+interface AbsentRange {\n+  startNew: number;\n+  endNew: number;\n+  startOld: number;\n+}\n+\n+type RegionPiece =\n+  | { rows: RowBlock[]; marks: (_RowMarks | undefined)[] }\n+  | { absent: AbsentRange };\n+\n+/** What the renderer puts between two live hunks: unchanged context\n+ *  interleaved with the demoted hunks' own rows, in file order.\n+ *\n+ *  One piece while the file's content is there to fill the gaps — the\n+ *  whole run is one expandable region. Without content a gap cannot be\n+ *  filled, so the run *breaks* at it: the region either side stands on\n+ *  its own and a band names the lines in between. Splicing the two\n+ *  hunks' rows together instead is what stepped the gutter silently\n+ *  under a filter (slice 5), and left the notes on those lines nowhere.\n+ *\n+ *  `newEnd` bounds the trailing context; null (an unbounded tail, i.e. no\n+ *  content at all) ends the run at the last hunk row. */\n+function _regionPieces(\n+  f: FileBlock, newStart: number, newEnd: number | null, oldStart: number,\n+  hunks: HunkBlock[],\n+): RegionPiece[] {\n+  const pieces: RegionPiece[] = [];\n+  let rows: RowBlock[] = [];\n+  let marks: (_RowMarks | undefined)[] = [];\n   let cn = newStart;\n   let co = oldStart;\n-  const ctxTo = (upTo: number): void => {\n-    if (!hl) return;\n-    while (cn < upTo) {\n-      const t = hl[cn - 1] ?? \"\";\n-      rows.push({ kind: \"ctx\", old_line: co, new_line: cn, old_text: t, new_text: t });\n-      marks.push(undefined);\n-      co++; cn++;\n+  const closeRows = (): void => {\n+    if (rows.length === 0) return;\n+    pieces.push({ rows, marks });\n+    rows = [];\n+    marks = [];\n+  };\n+  const gapTo = (upTo: number): void => {\n+    if (upTo <= cn) return;\n+    const ctx = FileText.contextRows(f.id, cn, upTo, co);\n+    if (ctx.length === 0) {\n+      closeRows();\n+      pieces.push({ absent: { startNew: cn, endNew: upTo - 1, startOld: co } });\n+    } else {\n+      for (const r of ctx) { rows.push(r); marks.push(undefined); }\n     }\n+    co += upTo - cn;\n+    cn = upTo;\n   };\n   for (const h of hunks) {\n-    ctxTo(h.new_start);\n-    const hm = _blockMarks(h.rows || []);\n+    const b = FileText.hunkBounds(h);\n+    gapTo(b.firstNew);\n     const hr = h.rows || [];\n+    const hm = _blockMarks(hr);\n     for (let i = 0; i < hr.length; i++) { rows.push(hr[i]); marks.push(hm[i]); }\n-    cn = h.new_start + h.new_count;\n-    co = h.old_start + h.old_count;\n+    cn = b.nextNew;\n+    co = b.nextOld;\n+  }\n+  if (newEnd !== null) gapTo(newEnd + 1);\n+  closeRows();\n+  return pieces;\n+}\n+\n+/** The span standing for one region. Its id is the region's own extent,\n+ *  so the same gap under the same layout is the same span across renders\n+ *  — which is what makes a reveal survive one. */\n+function _regionSpan(f: FileBlock, region: DiffRegion): HiddenSpan {\n+  return Visibility.contextSpan(\n+    f.id, _rowExtent(region.rows, \"new_line\"), _rowExtent(region.rows, \"old_line\"),\n+  );\n+}\n+\n+function _rowExtent(rows: RowBlock[], attr: \"new_line\" | \"old_line\"): LineRange | null {\n+  let start: number | null = null;\n+  let end = 0;\n+  for (const r of rows) {\n+    const v = r[attr];\n+    if (v == null) continue;\n+    if (start === null) start = v;\n+    end = v;\n+  }\n+  return start === null ? null : { start, end };\n+}\n+\n+/** A region renders as a chip while its span is hidden and as the diff\n+ *  itself once the reviewer removes it. Appends rather than returns\n+ *  because a chip carrying a manifest is two elements, not one. */\n+function _renderRegion(\n+  body: HTMLElement, f: FileBlock, region: DiffRegion, headed: Set<string>,\n+  notes: ManifestNote[],\n+): void {\n+  const span = _regionSpan(f, region);\n+  Visibility.seed(span);\n+  if (!Visibility.isHidden(f.id, span.id)) {\n+    body.appendChild(_renderRegionExpansion(f, region, span, headed));\n+    return;\n   }\n-  if (hl && newEnd !== null) ctxTo(newEnd + 1);\n-  return { rows, marks };\n+  body.appendChild(_withManifest(\n+    _renderRegionChip(region, span),\n+    Manifest.under(f.id, span.id, notes),\n+  ));\n }\n \n-function _renderRegionChip(f: FileBlock, region: DiffRegion): HTMLElement {\n+/** Hang a manifest under a one-line band (a gap chip, loaded or not).\n+ *  The band is its own click target, so the entries wrap onto their own\n+ *  line rather than sitting inside its centred flex row. */\n+function _withManifest(band: HTMLElement, entries: ManifestNote[]): HTMLElement {\n+  const manifest = Manifest.render(entries);\n+  if (!manifest) return band;\n+  band.classList.add(\"has-manifest\");\n+  band.appendChild(manifest);\n+  return band;\n+}\n+\n+function _renderRegionChip(region: DiffRegion, span: HiddenSpan): HTMLElement {\n   const chip = _el(\"div\", \"gap-chip\");\n   const count = region.rows.length;\n   const icon = region.position === \"top\" ? \"⬆\" : region.position === \"bottom\" ? \"⬇\" : \"⋯\";\n",
  "head_spans": [
   {
    "start_line": 410,
    "end_line": 455,
    "kind": "function",
    "qualified_name": "_renderFileBody",
    "depth": 0
   },
   {
    "start_line": 419,
    "end_line": 441,
    "kind": "function",
    "qualified_name": "_renderFileBody.flush",
    "depth": 1
   },
   {
    "start_line": 468,
    "end_line": 483,
    "kind": "function",
    "qualified_name": "_renderAbsentContext",
    "depth": 0
   },
   {
    "start_line": 485,
    "end_line": 503,
    "kind": "function",
    "qualified_name": "_renderFileHeader",
    "depth": 0
   },
   {
    "start_line": 508,
    "end_line": 519,
    "kind": "function",
    "qualified_name": "_renderMdToggle",
    "depth": 0
   },
   {
    "start_line": 521,
    "end_line": 528,
    "kind": "function",
    "qualified_name": "_uniqueFileSmells",
    "depth": 0
   },
   {
    "start_line": 530,
    "end_line": 540,
    "kind": "function",
    "qualified_name": "_renderFileOverview",
    "depth": 0
   },
   {
    "start_line": 550,
    "end_line": 554,
    "kind": "interface",
    "qualified_name": "DiffRegion",
    "depth": 0
   },
   {
    "start_line": 558,
    "end_line": 562,
    "kind": "interface",
    "qualified_name": "AbsentRange",
    "depth": 0
   },
   {
    "start_line": 564,
    "end_line": 566,
    "kind": "type",
    "qualified_name": "RegionPiece",
    "depth": 0
   },
   {
    "start_line": 580,
    "end_line": 619,
    "kind": "function",
    "qualified_name": "_regionPieces",
    "depth": 0
   },
   {
    "start_line": 589,
    "end_line": 594,
    "kind": "function",
    "qualified_name": "_regionPieces.closeRows",
    "depth": 1
   },
   {
    "start_line": 595,
    "end_line": 606,
    "kind": "function",
    "qualified_name": "_regionPieces.gapTo",
    "depth": 1
   },
   {
    "start_line": 624,
    "end_line": 628,
    "kind": "function",
    "qualified_name": "_regionSpan",
    "depth": 0
   },
   {
    "start_line": 630,
    "end_line": 640,
    "kind": "function",
    "qualified_name": "_rowExtent",
    "depth": 0
   },
   {
    "start_line": 645,
    "end_line": 659,
    "kind": "function",
    "qualified_name": "_renderRegion",
    "depth": 0
   },
   {
    "start_line": 664,
    "end_line": 670,
    "kind": "function",
    "qualified_name": "_withManifest",
    "depth": 0
   },
   {
    "start_line": 672,
    "end_line": 683,
    "kind": "function",
    "qualified_name": "_renderRegionChip",
    "depth": 0
   }
  ],
  "base_spans": [
   {
    "start_line": 432,
    "end_line": 436,
    "kind": "interface",
    "qualified_name": "DiffRegion",
    "depth": 0
   },
   {
    "start_line": 442,
    "end_line": 469,
    "kind": "function",
    "qualified_name": "_regionRows",
    "depth": 0
   },
   {
    "start_line": 450,
    "end_line": 458,
    "kind": "function",
    "qualified_name": "_regionRows.ctxTo",
    "depth": 1
   },
   {
    "start_line": 471,
    "end_line": 485,
    "kind": "function",
    "qualified_name": "_renderRegionChip",
    "depth": 0
   },
   {
    "start_line": 487,
    "end_line": 502,
    "kind": "function",
    "qualified_name": "_renderRegionExpansion",
    "depth": 0
   },
   {
    "start_line": 507,
    "end_line": 527,
    "kind": "function",
    "qualified_name": "_renderDiffRows",
    "depth": 0
   },
   {
    "start_line": 529,
    "end_line": 534,
    "kind": "function",
    "qualified_name": "_refreshFileFolds",
    "depth": 0
   },
   {
    "start_line": 538,
    "end_line": 571,
    "kind": "function",
    "qualified_name": "_renderHunk",
    "depth": 0
   },
   {
    "start_line": 573,
    "end_line": 581,
    "kind": "function",
    "qualified_name": "_renderRefs",
    "depth": 0
   },
   {
    "start_line": 583,
    "end_line": 596,
    "kind": "function",
    "qualified_name": "_buildRefLink",
    "depth": 0
   },
   {
    "start_line": 601,
    "end_line": 612,
    "kind": "function",
    "qualified_name": "_displaySegments",
    "depth": 0
   },
   {
    "start_line": 614,
    "end_line": 655,
    "kind": "function",
    "qualified_name": "_renderHunkHeader",
    "depth": 0
   },
   {
    "start_line": 657,
    "end_line": 674,
    "kind": "function",
    "qualified_name": "_renderSegmentFolded",
    "depth": 0
   },
   {
    "start_line": 676,
    "end_line": 688,
    "kind": "function",
    "qualified_name": "_renderHunkDiff",
    "depth": 0
   }
  ],
  "expect_runs": null
 },
 {
  "name": "semantic_code_review/viewer/assets/render.ts:@@ -477,84 +678,99",
  "moved": true,
  "old_start": 477,
  "old_count": 84,
  "new_start": 678,
  "new_count": 99,
  "header": "@@ -477,84 +678,99 @@ function _renderRegionChip(f: FileBlock, region: DiffRegion): HTMLElement {",
  "body": "               : region.position === \"bottom\" ? `expand ${count} ${word} below`\n               : `expand ${count} hidden ${word}`;\n   chip.innerHTML = `<span class=\"gap-icon\">${icon}</span><span class=\"gap-label\">${label}</span>`;\n-  chip.addEventListener(\"click\", () => {\n-    chip.replaceWith(_renderRegionExpansion(f, region));\n-    _refreshFileFolds(f);\n-  });\n+  chip.addEventListener(\"click\", () => _setHidden(span, false));\n   return chip;\n }\n \n-function _renderRegionExpansion(f: FileBlock, region: DiffRegion): HTMLElement {\n+function _renderRegionExpansion(\n+  f: FileBlock, region: DiffRegion, span: HiddenSpan, headed: Set<string>,\n+): HTMLElement {\n   const container = _el(\"div\", \"gap-expansion\");\n   const collapse = _el(\"button\", \"gap-collapse\", \"× collapse\");\n   collapse.title = \"Hide these lines again\";\n-  collapse.addEventListener(\"click\", () => {\n-    container.replaceWith(_renderRegionChip(f, region));\n-    _refreshFileFolds(f);\n-  });\n+  collapse.addEventListener(\"click\", () => _setHidden(span, true));\n   container.appendChild(collapse);\n-  const { diff, oldEls, newEls } = _renderDiffRows(f, region.rows, region.marks);\n+  const { diff, rows, oldEls, newEls } = _renderDiffRows(\n+    f, region.rows, region.marks, headed,\n+  );\n   // The file-level fold walker (folds.ts) recovers the row stream + DOM\n   // elements from the container.\n-  FileRows.record(container, { rows: region.rows, oldEls, newEls });\n+  FileRows.record(container, { rows, oldEls, newEls });\n   container.appendChild(diff);\n   return container;\n }\n \n /** Render a diff-row stream into a `.diff` grid, pairing old/new rows.\n  *  The single primitive behind both hunk bodies and region expansions —\n- *  what differs between them is only the surrounding chrome. */\n+ *  what differs between them is only the surrounding chrome.\n+ *\n+ *  Rows inside a collapsed `CodeFold` are not emitted at all, so the\n+ *  returned `rows` (and the element arrays beside them) are what actually\n+ *  landed on screen; every caller keys off those, never the input. */\n function _renderDiffRows(\n   f: FileBlock, rows: RowBlock[], marks: (_RowMarks | undefined)[],\n-): { diff: HTMLElement; oldEls: HTMLElement[]; newEls: HTMLElement[] } {\n+  headed: Set<string>,\n+): { diff: HTMLElement; rows: RowBlock[]; oldEls: HTMLElement[]; newEls: HTMLElement[] } {\n   const diff = _el(\"div\", \"diff\");\n   const halfOld = _el(\"div\", \"half half-old\");\n   const halfNew = _el(\"div\", \"half half-new\");\n   diff.appendChild(halfOld);\n   diff.appendChild(halfNew);\n+  const shown: RowBlock[] = [];\n   const oldEls: HTMLElement[] = [];\n   const newEls: HTMLElement[] = [];\n-  for (let i = 0; i < rows.length; i++) {\n+  for (const i of Visibility.planRows(f.id, rows, headed)) {\n     const pair = _renderRow(rows[i], f, marks[i]?.old, marks[i]?.new);\n     (pair.old as { _scrPair?: HTMLElement })._scrPair = pair.new;\n     (pair.new as { _scrPair?: HTMLElement })._scrPair = pair.old;\n     halfOld.appendChild(pair.old);\n     halfNew.appendChild(pair.new);\n+    shown.push(rows[i]);\n     oldEls.push(pair.old);\n     newEls.push(pair.new);\n   }\n-  return { diff, oldEls, newEls };\n-}\n-\n-function _refreshFileFolds(f: FileBlock): void {\n-  const fileEl = document.querySelector(\n-    '.file[data-id=\"' + _cssEscape(f.id) + '\"]',\n-  ) as HTMLElement | null;\n-  if (fileEl) Folds.attachFileFolds(fileEl, f);\n+  return { diff, rows: shown, oldEls, newEls };\n }\n \n // --- Hunk + diff body ---------------------------------------------------\n \n-function _renderHunk(h: HunkBlock, f: FileBlock, reveal = false): HTMLElement {\n+function _renderHunk(\n+  h: HunkBlock, f: FileBlock, headed: Set<string>, notes: ManifestNote[],\n+  reveal = false,\n+): HTMLElement {\n   const div = _el(\"div\", \"hunk\");\n   div.dataset.id = h.id;\n-  // reveal (focus) forces the hunk fully open — code, not summaries — but\n-  // an explicit fold override the reviewer set still wins.\n-  const folded = _isFolded(h.id, reveal ? false : _defaultHunkFolded());\n+  const folded = _hunkHidden(f, h, reveal);\n   div.classList.toggle(\"folded\", folded);\n   div.appendChild(_renderHunkHeader(h, folded, f));\n-  if (!folded) {\n-    // The collapse ladder shows segment summaries (never raw code) until\n-    // `off` or a reveal. A hunk with no segments folds as one synthetic\n-    // segment spanning it, so every hunk behaves uniformly at this level.\n-    const segs = _displaySegments(h);\n-    const anyOpen = segs.some((s) => _isFolded(s.id, _defaultSegmentFolded()) === false);\n-    if (!reveal && _defaultSegmentFolded() && !anyOpen) {\n+  if (folded) {\n+    const manifest = Manifest.render(\n+      Manifest.under(f.id, Visibility.hunkSpanId(h.id), notes),\n+    );\n+    if (manifest) div.appendChild(manifest);\n+  } else {\n+    // The collapse ladder shows segment summaries (never raw code) while\n+    // every segment span is in place. A hunk with no segments folds as one\n+    // synthetic segment spanning it, so every hunk behaves uniformly.\n+    const segs = Visibility.displaySegments(h);\n+    const anyOpen = segs.some(\n+      (s) => !Visibility.isHidden(f.id, Visibility.segmentSpanId(s.id)),\n+    );\n+    if (!reveal && !anyOpen) {\n+      // One manifest for the body, not one per summary row: what the\n+      // seg-list stands in for is the whole hunk — its base side and its\n+      // context rows included — and no single segment span says that.\n+      const manifest = Manifest.render(Manifest.inRange(\n+        notes,\n+        { start: h.new_start, end: h.new_start + h.new_count - 1 },\n+        { start: h.old_start, end: h.old_start + h.old_count - 1 },\n+      ));\n+      if (manifest) div.appendChild(manifest);\n       const list = _el(\"div\", \"seg-list\");\n       for (const s of segs) list.appendChild(_renderSegmentFolded(s, f));\n       div.appendChild(list);\n     } else {\n-      div.appendChild(_renderHunkDiff(h, f));\n+      div.appendChild(_renderHunkDiff(h, f, headed));\n     }\n     if (h.context) {\n       const c = _el(\"div\", \"context-note\");\n",
  "head_spans": [
   {
    "start_line": 468,
    "end_line": 483,
    "kind": "function",
    "qualified_name": "_renderAbsentContext",
    "depth": 0
   },
   {
    "start_line": 485,
    "end_line": 503,
    "kind": "function",
    "qualified_name": "_renderFileHeader",
    "depth": 0
   },
   {
    "start_line": 508,
    "end_line": 519,
    "kind": "function",
    "qualified_name": "_renderMdToggle",
    "depth": 0
   },
   {
    "start_line": 521,
    "end_line": 528,
    "kind": "function",
    "qualified_name": "_uniqueFileSmells",
    "depth": 0
   },
   {
    "start_line": 530,
    "end_line": 540,
    "kind": "function",
    "qualified_name": "_renderFileOverview",
    "depth": 0
   },
   {
    "start_line": 550,
    "end_line": 554,
    "kind": "interface",
    "qualified_name": "DiffRegion",
    "depth": 0
   },
   {
    "start_line": 558,
    "end_line": 562,
    "kind": "interface",
    "qualified_name": "AbsentRange",
    "depth": 0
   },
   {
    "start_line": 564,
    "end_line": 566,
    "kind": "type",
    "qualified_name": "RegionPiece",
    "depth": 0
   },
   {
    "start_line": 580,
    "end_line": 619,
    "kind": "function",
    "qualified_name": "_regionPieces",
    "depth": 0
   },
   {
    "start_line": 589,
    "end_line": 594,
    "kind": "function",
    "qualified_name": "_regionPieces.closeRows",
    "depth": 1
   },
   {
    "start_line": 595,
    "end_line": 606,
    "kind": "function",
    "qualified_name": "_regionPieces.gapTo",
    "depth": 1
   },
   {
    "start_line": 624,
    "end_line": 628,
    "kind": "function",
    "qualified_name": "_regionSpan",
    "depth": 0
   },
   {
    "start_line": 630,
    "end_line": 640,
    "kind": "function",
    "qualified_name": "_rowExtent",
    "depth": 0
   },
   {
    "start_line": 645,
    "end_line": 659,
    "kind": "function",
    "qualified_name": "_renderRegion",
    "depth": 0
   },
   {
    "start_line": 664,
    "end_line": 670,
    "kind": "function",
    "qualified_name": "_withManifest",
    "depth": 0
   },
   {
    "start_line": 672,
    "end_line": 683,
    "kind": "function",
    "qualified_name": "_renderRegionChip",
    "depth": 0
   },
   {
    "start_line": 685,
    "end_line": 701,
    "kind": "function",
    "qualified_name": "_renderRegionExpansion",
    "depth": 0
   },
   {
    "start_line": 710,
    "end_line": 733,
    "kind": "function",
    "qualified_name": "_renderDiffRows",
    "depth": 0
   },
   {
    "start_line": 737,
    "end_line": 787,
    "kind": "function",
    "qualified_name": "_renderHunk",
    "depth": 0
   }
  ],
  "base_spans": [
   {
    "start_line": 471,
    "end_line": 485,
    "kind": "function",
    "qualified_name": "_renderRegionChip",
    "depth": 0
   },
   {
    "start_line": 487,
    "end_line": 502,
    "kind": "function",
    "qualified_name": "_renderRegionExpansion",
    "depth": 0
   },
   {
    "start_line": 507,
    "end_line": 527,
    "kind": "function",
    "qualified_name": "_renderDiffRows",
    "depth": 0
   },
   {
    "start_line": 529,
    "end_line": 534,
    "kind": "function",
    "qualified_name": "_refreshFileFolds",
    "depth": 0
   },
   {
    "start_line": 538,
    "end_line": 571,
    "kind": "function",
    "qualified_name": "_renderHunk",
    "depth": 0
   },
   {
    "start_line": 573,
    "end_line": 581,
    "kind": "function",
    "qualified_name": "_renderRefs",
    "depth": 0
   },
   {
    "start_line": 583,
    "end_line": 596,
    "kind": "function",
    "qualified_name": "_buildRefLink",
    "depth": 0
   },
   {
    "start_line": 601,
    "end_line": 612,
    "kind": "function",
    "qualified_name": "_displaySegments",
    "depth": 0
   },
   {
    "start_line": 614,
    "end_line": 655,
    "kind": "function",
    "qualified_name": "_renderHunkHeader",
    "depth": 0
   },
   {
    "start_line": 657,
    "end_line": 674,
    "kind": "function",
    "qualified_name": "_renderSegmentFolded",
    "depth": 0
   },
   {
    "start_line": 676,
    "end_line": 688,
    "kind": "function",
    "qualified_name": "_renderHunkDiff",
    "depth": 0
   },
   {
    "start_line": 690,
    "end_line": 718,
    "kind": "function",
    "qualified_name": "_attachLineNotes",
    "depth": 0
   },
   {
    "start_line": 715,
    "end_line": 715,
    "kind": "function",
    "qualified_name": "_attachLineNotes.onInsert",
    "depth": 1
   },
   {
    "start_line": 723,
    "end_line": 751,
    "kind": "function",
    "qualified_name": "_buildLineNoteContent",
    "depth": 0
   },
   {
    "start_line": 753,
    "end_line": 768,
    "kind": "function",
    "qualified_name": "_renderRow",
    "depth": 0
   },
   {
    "start_line": 770,
    "end_line": 770,
    "kind": "interface",
    "qualified_name": "_RowMarks",
    "depth": 0
   },
   {
    "start_line": 778,
    "end_line": 801,
    "kind": "function",
    "qualified_name": "_blockMarks",
    "depth": 0
   }
  ],
  "expect_runs": [
   {
    "kind": "del",
    "start_idx": 4,
    "texts": [
     "    chip.replaceWith(_renderRegionExpansion(f, region));",
     "    _refreshFileFolds(f);",
     "  });"
    ]
   },
   {
    "kind": "ins",
    "start_idx": 11,
    "texts": [
     "  f: FileBlock, region: DiffRegion, span: HiddenSpan, headed: Set<string>,",
     "): HTMLElement {"
    ]
   },
   {
    "kind": "del",
    "start_idx": 17,
    "texts": [
     "    container.replaceWith(_renderRegionChip(f, region));",
     "    _refreshFileFolds(f);",
     "  });"
    ]
   },
   {
    "kind": "ins",
    "start_idx": 22,
    "texts": [
     "    f, region.rows, region.marks, headed,",
     "  );"
    ]
   },
   {
    "kind": "ins",
    "start_idx": 34,
    "texts": [
     " *",
     " *  Rows inside a collapsed `CodeFold` are not emitted at all, so the",
     " *  returned `rows` (and the element arrays beside them) are what actually",
     " *  landed on screen; every caller keys off those, never the input. */"
    ]
   },
   {
    "kind": "del",
    "start_idx": 63,
    "texts": [
     "function _refreshFileFolds(f: FileBlock): void {",
     "  const fileEl = document.querySelector(",
     "    '.file[data-id=\"' + _cssEscape(f.id) + '\"]',",
     "  ) as HTMLElement | null;",
     "  if (fileEl) Folds.attachFileFolds(fileEl, f);",
     "}",
     ""
    ]
   },
   {
    "kind": "ins",
    "start_idx": 73,
    "texts": [
     "  h: HunkBlock, f: FileBlock, headed: Set<string>, notes: ManifestNote[],",
     "  reveal = false,",
     "): HTMLElement {"
    ]
   },
   {
    "kind": "del",
    "start_idx": 79,
    "texts": [
     "  // an explicit fold override the reviewer set still wins.",
     "  const folded = _isFolded(h.id, reveal ? false : _defaultHunkFolded());"
    ]
   },
   {
    "kind": "ins",
    "start_idx": 90,
    "texts": [
     "    // every segment span is in place. A hunk with no segments folds as one",
     "    // synthetic segment spanning it, so every hunk behaves uniformly.",
     "    const segs = Visibility.displaySegments(h);",
     "    const anyOpen = segs.some(",
     "      (s) => !Visibility.isHidden(f.id, Visibility.segmentSpanId(s.id)),",
     "    );",
     "    if (!reveal && !anyOpen) {",
     "      // One manifest for the body, not one per summary row: what the",
     "      // seg-list stands in for is the whole hunk — its base side and its",
     "      // context rows included — and no single segment span says that.",
     "      const manifest = Manifest.render(Manifest.inRange(",
     "        notes,",
     "        { start: h.new_start, end: h.new_start + h.new_count - 1 },",
     "        { start: h.old_start, end: h.old_start + h.old_count - 1 },",
     "      ));",
     "      if (manifest) div.appendChild(manifest);"
    ]
   }
  ]
 },
 {
  "name": "tests/js/viewer.test.ts:@@ -689,10 +797,14",
  "moved": false,
  "old_start": 689,
  "old_count": 10,
  "new_start": 797,
  "new_count": 14,
  "header": "@@ -689,10 +797,14 @@ describe(\"streaming events\", () => {",
  "body": " \n   // --- Fold-level ladder + focus reveal -----------------------------------\n \n-  const foldFile = (): Record<string, unknown> => ({\n+  /** Registers its own source with the `/file-text` stub — the caller\n+   *  boots straight after, and the unchanged context between the three\n+   *  hunks only exists if the route serves it. */\n+  const foldFile = (): Record<string, unknown> => {\n+    serveFileText(\"F0\", [\"l1\", \"l2\", \"l3\", \"l4\", \"l5\", \"l6\", \"l7\", \"l8\", \"l9\"]);\n+    return {\n     id: \"F0\", path: \"a.py\", status: \"modified\", language: \"python\",\n     adds: 3, dels: 3, summary: \"\",\n-    head_lines: [\"l1\", \"l2\", \"l3\", \"l4\", \"l5\", \"l6\", \"l7\", \"l8\", \"l9\"],\n     symbols: { added: [], modified: [], removed: [] },\n     hunks: [\n       makeHunkBlock(\"H0\", \"\", { old_start: 2, old_count: 1, new_start: 2, new_count: 1,\n",
  "head_spans": [
   {
    "start_line": 803,
    "end_line": 818,
    "kind": "function",
    "qualified_name": "foldFile",
    "depth": 0
   }
  ],
  "base_spans": [
   {
    "start_line": 692,
    "end_line": 705,
    "kind": "function",
    "qualified_name": "foldFile",
    "depth": 0
   },
   {
    "start_line": 706,
    "end_line": 707,
    "kind": "function",
    "qualified_name": "fold",
    "depth": 0
   },
   {
    "start_line": 708,
    "end_line": 709,
    "kind": "function",
    "qualified_name": "codeRows",
    "depth": 0
   }
  ],
  "expect_runs": null
 },
 {
  "name": "semantic_code_review/review/pr_flow.py:@@ -222,24 +226,32",
  "moved": false,
  "old_start": 222,
  "old_count": 24,
  "new_start": 226,
  "new_count": 32,
  "header": "@@ -222,24 +226,32 @@ def _resolve_pr_number(repo: str) -> tuple[int | None, int | None]:",
  "body": "     return None, picked\n \n \n-def _build_tasks(\n-    opts: PrFlowOptions,\n-    run_dir: Path,\n-) -> tuple[\n-    Callable | None,\n-    Callable | None,\n-    Callable | None,\n-    Callable[[Callable[[dict], None]], None] | None,\n-]:\n-    \"\"\"Build the augment + fold-summary + console closures plus a debug-sink\n-    binder, or all-``None`` when augmentation is skipped (the console grounds\n-    its answers in the augment sidecar, so it's unavailable without it).\n+@dataclass(frozen=True)\n+class _ServerTasks:\n+    \"\"\"The optional closures ``serve_review`` is handed for one PR review.\n+\n+    Every field is None on a ``--no-augment`` run: each one needs either\n+    an LLM backend or the augment sidecar, and often both.\n+    \"\"\"\n+\n+    augment: Callable | None = None\n+    fold_summary: Callable | None = None\n+    console: Callable | None = None\n+    explainer: Callable | None = None\n+    bind_debug_sink: Callable[[Callable[[dict], None]], None] | None = None\n+\n+\n+def _build_tasks(opts: PrFlowOptions, run_dir: Path) -> _ServerTasks:\n+    \"\"\"Build the augment + fold-summary + console + explainer closures plus\n+    a debug-sink binder, or an all-``None`` bundle when augmentation is\n+    skipped (the console grounds its answers in the augment sidecar, so\n+    it's unavailable without it).\n \n     The binder, present only in ``--debug``, lets the server route the CLI\n     driver's per-spawn records to its SSE fan-out (see ``serve_review``).\n     \"\"\"\n     if not opts.augment:\n-        return None, None, None, None\n+        return _ServerTasks()\n \n     # Imports inside: anthropic SDK + augment pipeline are lazy-loaded so\n     # `--no-augment` runs (and `scr --help`) don't pay the cost.\n",
  "head_spans": [
   {
    "start_line": 201,
    "end_line": 226,
    "kind": "function",
    "qualified_name": "_resolve_pr_number",
    "depth": 0
   },
   {
    "start_line": 230,
    "end_line": 241,
    "kind": "class",
    "qualified_name": "_ServerTasks",
    "depth": 0
   },
   {
    "start_line": 244,
    "end_line": 312,
    "kind": "function",
    "qualified_name": "_build_tasks",
    "depth": 0
   }
  ],
  "base_spans": [
   {
    "start_line": 197,
    "end_line": 222,
    "kind": "function",
    "qualified_name": "_resolve_pr_number",
    "depth": 0
   },
   {
    "start_line": 225,
    "end_line": 289,
    "kind": "function",
    "qualified_name": "_build_tasks",
    "depth": 0
   },
   {
    "start_line": 259,
    "end_line": 273,
    "kind": "function",
    "qualified_name": "_build_tasks.augment_task",
    "depth": 1
   }
  ],
  "expect_runs": null
 },
 {
  "name": "semantic_code_review/viewer/assets/render.ts:@@ -884,6 +976,32",
  "moved": false,
  "old_start": 884,
  "old_count": 6,
  "new_start": 976,
  "new_count": 32,
  "header": "@@ -884,6 +976,32 @@ function _updateSliderButtons(): void {",
  "body": "     const btn = b as HTMLElement;\n     btn.classList.toggle(\"active\", btn.dataset.fold === _state.fold);\n   });\n+  _updateModeButton();\n+}\n+\n+/** Repaint the overview-mode button: pressed state, and disabled until\n+ *  the skeleton's inputs exist server-side (pressing earlier can only\n+ *  409). Absent entirely when the feature is off for this review. */\n+function _updateModeButton(): void {\n+  const strip = document.querySelector(\".mode-strip\");\n+  if (!_explainerEnabled) {\n+    strip?.remove();\n+    return;\n+  }\n+  const btn = document.getElementById(\"overview-btn\") as HTMLButtonElement | null;\n+  if (!btn) return;\n+  btn.classList.toggle(\"active\", _state.mode === \"overview\");\n+  btn.disabled = !Explainer.isReady();\n+  btn.title = Explainer.isReady()\n+    ? \"Reading guide for this change\"\n+    : \"Available once the change overview lands\";\n+}\n+\n+/** Called by boot when the overview SSE event arrives. */\n+function markExplainerReady(): void {\n+  if (!_explainerEnabled) return;\n+  Explainer.markReady();\n+  _updateModeButton();\n }\n \n function _updateStatus(): void {\n",
  "head_spans": [
   {
    "start_line": 870,
    "end_line": 893,
    "kind": "function",
    "qualified_name": "_blockMarks",
    "depth": 0
   },
   {
    "start_line": 895,
    "end_line": 903,
    "kind": "function",
    "qualified_name": "_renderLineno",
    "depth": 0
   },
   {
    "start_line": 905,
    "end_line": 938,
    "kind": "function",
    "qualified_name": "_renderContent",
    "depth": 0
   },
   {
    "start_line": 920,
    "end_line": 920,
    "kind": "method",
    "qualified_name": "_renderContent.highlight",
    "depth": 1
   },
   {
    "start_line": 945,
    "end_line": 953,
    "kind": "function",
    "qualified_name": "setSymbolSearch",
    "depth": 0
   },
   {
    "start_line": 955,
    "end_line": 959,
    "kind": "function",
    "qualified_name": "_applySymbolHits",
    "depth": 0
   },
   {
    "start_line": 963,
    "end_line": 970,
    "kind": "function",
    "qualified_name": "_clearSymbolHits",
    "depth": 0
   },
   {
    "start_line": 974,
    "end_line": 980,
    "kind": "function",
    "qualified_name": "_updateSliderButtons",
    "depth": 0
   },
   {
    "start_line": 985,
    "end_line": 998,
    "kind": "function",
    "qualified_name": "_updateModeButton",
    "depth": 0
   },
   {
    "start_line": 1001,
    "end_line": 1005,
    "kind": "function",
    "qualified_name": "markExplainerReady",
    "depth": 0
   },
   {
    "start_line": 1007,
    "end_line": 1030,
    "kind": "function",
    "qualified_name": "_updateStatus",
    "depth": 0
   }
  ],
  "base_spans": [
   {
    "start_line": 882,
    "end_line": 887,
    "kind": "function",
    "qualified_name": "_updateSliderButtons",
    "depth": 0
   },
   {
    "start_line": 889,
    "end_line": 912,
    "kind": "function",
    "qualified_name": "_updateStatus",
    "depth": 0
   },
   {
    "start_line": 914,
    "end_line": 923,
    "kind": "function",
    "qualified_name": "_syncHash",
    "depth": 0
   },
   {
    "start_line": 925,
    "end_line": 936,
    "kind": "function",
    "qualified_name": "_restoreHash",
    "depth": 0
   },
   {
    "start_line": 938,
    "end_line": 951,
    "kind": "function",
    "qualified_name": "_onKeydown",
    "depth": 0
   },
   {
    "start_line": 953,
    "end_line": 956,
    "kind": "function",
    "qualified_name": "_toggleHelp",
    "depth": 0
   },
   {
    "start_line": 957,
    "end_line": 960,
    "kind": "function",
    "qualified_name": "_closeHelp",
    "depth": 0
   },
   {
    "start_line": 962,
    "end_line": 989,
    "kind": "function",
    "qualified_name": "_wireInputs",
    "depth": 0
   }
  ],
  "expect_runs": null
 },
 {
  "name": "tests/test_github_pr_review.py:@@ -241,22 +242,34",
  "moved": false,
  "old_start": 241,
  "old_count": 22,
  "new_start": 242,
  "new_count": 34,
  "header": "@@ -241,22 +242,34 @@ def _pr_opts(tmp_path, *, augment: bool, debug: bool = False):",
  "body": " def test_build_tasks_wires_console_when_augmenting(tmp_path) -> None:\n     from semantic_code_review.review.pr_flow import _build_tasks\n \n-    augment, fold, console, bind_debug = _build_tasks(_pr_opts(tmp_path, augment=True), tmp_path)\n-    assert augment is not None\n-    assert fold is not None\n-    assert console is not None  # the console callback the server installs\n+    tasks = _build_tasks(_pr_opts(tmp_path, augment=True), tmp_path)\n+    assert tasks.augment is not None\n+    assert tasks.fold_summary is not None\n+    assert tasks.console is not None  # the console callback the server installs\n+    assert tasks.explainer is not None  # opt-out, so on unless config says otherwise\n     # Debug off by default → no sink binder.\n-    assert bind_debug is None\n+    assert tasks.bind_debug_sink is None\n \n \n def test_build_tasks_binds_debug_sink_when_debug(tmp_path) -> None:\n     from semantic_code_review.review.pr_flow import _build_tasks\n \n-    _augment, _fold, _console, bind_debug = _build_tasks(_pr_opts(tmp_path, augment=True, debug=True), tmp_path)\n-    assert bind_debug is not None\n+    tasks = _build_tasks(_pr_opts(tmp_path, augment=True, debug=True), tmp_path)\n+    assert tasks.bind_debug_sink is not None\n \n \n-def test_build_tasks_returns_none_quad_without_augment(tmp_path) -> None:\n+def test_build_tasks_omits_the_explainer_when_it_is_disabled(tmp_path) -> None:\n     from semantic_code_review.review.pr_flow import _build_tasks\n \n-    assert _build_tasks(_pr_opts(tmp_path, augment=False), tmp_path) == (None, None, None, None)\n+    opts = dataclasses.replace(_pr_opts(tmp_path, augment=True), explainer=False)\n+    tasks = _build_tasks(opts, tmp_path)\n+    # Everything else still wires; only the explainer drops out, so the\n+    # server reports the feature disabled rather than \"not ready yet\".\n+    assert tasks.console is not None\n+    assert tasks.explainer is None\n+\n+\n+def test_build_tasks_returns_an_empty_bundle_without_augment(tmp_path) -> None:\n+    from semantic_code_review.review.pr_flow import _build_tasks, _ServerTasks\n+\n+    assert _build_tasks(_pr_opts(tmp_path, augment=False), tmp_path) == _ServerTasks()\n",
  "head_spans": [
   {
    "start_line": 219,
    "end_line": 239,
    "kind": "function",
    "qualified_name": "_pr_opts",
    "depth": 0
   },
   {
    "start_line": 242,
    "end_line": 251,
    "kind": "function",
    "qualified_name": "test_build_tasks_wires_console_when_augmenting",
    "depth": 0
   },
   {
    "start_line": 254,
    "end_line": 258,
    "kind": "function",
    "qualified_name": "test_build_tasks_binds_debug_sink_when_debug",
    "depth": 0
   },
   {
    "start_line": 261,
    "end_line": 269,
    "kind": "function",
    "qualified_name": "test_build_tasks_omits_the_explainer_when_it_is_disabled",
    "depth": 0
   },
   {
    "start_line": 272,
    "end_line": 275,
    "kind": "function",
    "qualified_name": "test_build_tasks_returns_an_empty_bundle_without_augment",
    "depth": 0
   }
  ],
  "base_spans": [
   {
    "start_line": 218,
    "end_line": 238,
    "kind": "function",
    "qualified_name": "_pr_opts",
    "depth": 0
   },
   {
    "start_line": 241,
    "end_line": 249,
    "kind": "function",
    "qualified_name": "test_build_tasks_wires_console_when_augmenting",
    "depth": 0
   },
   {
    "start_line": 252,
    "end_line": 256,
    "kind": "function",
    "qualified_name": "test_build_tasks_binds_debug_sink_when_debug",
    "depth": 0
   },
   {
    "start_line": 259,
    "end_line": 262,
    "kind": "function",
    "qualified_name": "test_build_tasks_returns_none_quad_without_augment",
    "depth": 0
   }
  ],
  "expect_runs": null
 },
 {
  "name": "semantic_code_review/augment/hunks.py:@@ -45,29 +50,152",
  "moved": true,
  "old_start": 45,
  "old_count": 29,
  "new_start": 50,
  "new_count": 152,
  "header": "@@ -45,29 +50,152 @@",
  "body": " \n # Anthropic prompt-caching settings applied to every per-hunk call.\n #\n-# The cacheable prefix on this pass is `[system prompt] + [tool defs] +\n+# The cacheable prefix on this pass is `[tool defs] + [system prompt] +\n # [overview] + [file summary]`. The first two stay byte-identical for\n # every hunk in the run, so caching them buys cross-hunk reuse on a\n # multi-hunk PR. The CachePoint markers in `format_hunk_prompt` then\n # split the user prompt so within-file (overview + summary cached) and\n # within-PR (overview cached) prefixes are reused too.\n #\n+# `anthropic_cache_messages` is the one that matters on a tool-using\n+# pass, and its absence was costing more than the other two save. The\n+# agent loop appends [tool_use][tool_result] per turn *after* every\n+# fixed breakpoint, so without a rolling breakpoint the whole growing\n+# conversation is re-sent as fresh input every turn: measured on a\n+# 51-turn hunk, cache_read sat flat at 7,076 while fresh input climbed\n+# 7,740 -> 46,698 and cache_write stayed at 0 throughout. Cost per turn\n+# grows linearly and the loop's total grows quadratically. With the\n+# rolling breakpoint each turn writes only its own delta and reads the\n+# accumulated prefix at a tenth of the price.\n+#\n # AnthropicModelSettings keys are silently ignored by non-Anthropic\n # backends (TypedDict total=False), so this is safe to apply\n # unconditionally — Google + the CLI drivers see them as no-ops.\n+# Anthropic allows 4 breakpoints per request. Over budget, pydantic-ai\n+# keeps every system and tool one and trims *message* breakpoints\n+# oldest-first, which silently evicts the overview marker — the only\n+# prefix shared across files. Tool definitions get no breakpoint of\n+# their own: Anthropic renders tools -> system -> messages, so caching\n+# after the tools block caches `[tools]`, a strict prefix of the\n+# `[tools + system]` that the instructions breakpoint already caches,\n+# with both constant for the whole run. It can never produce a hit the\n+# longer entry doesn't, and it was costing the slot that mattered.\n _HUNK_CACHE_SETTINGS: dict[str, Any] = {\n     \"anthropic_cache_instructions\": True,  # system prompt block\n-    \"anthropic_cache_tool_definitions\": True,  # tools/<RepoTools>\n+    \"anthropic_cache_messages\": True,  # rolling breakpoint on the latest turn\n+}\n+\n+# Adaptive thinking, with the summary returned rather than omitted.\n+# Leaving `thinking` unset means the model does not think at all — so\n+# every SDK-backend hunk had been annotated with reasoning off, while the\n+# CLI backend got Claude Code's own. `display=\"summarized\"` is what makes\n+# a trace show *why* a pass investigated the way it did, otherwise only\n+# reconstructible from the tool-call sequence.\n+#\n+# Applied only alongside native structured output: Anthropic rejects\n+# thinking combined with the forced tool choice `ToolOutput` produces, so\n+# a model that cannot do native output must not be asked to think either.\n+_THINKING_SETTINGS: dict[str, Any] = {\n+    \"anthropic_thinking\": {\"type\": \"adaptive\", \"display\": \"summarized\"},\n }\n \n \n+def _hunk_model_settings(client: Client) -> dict[str, Any]:\n+    \"\"\"Model settings for a per-hunk call against `client`.\"\"\"\n+    if not client.native_output:\n+        return _HUNK_CACHE_SETTINGS\n+    return {**_HUNK_CACHE_SETTINGS, **_THINKING_SETTINGS}\n+\n+\n+#: Entries rendered per section before truncating. A wholesale file\n+#: deletion can remove thousands of symbols, and this seed rides in\n+#: every hunk prompt for the file — uncapped, one measured case reached\n+#: ~62k tokens per hunk, which is the cost blowup the seed exists to\n+#: avoid paying in tool calls.\n+_REMOVED_SEED_MAX = 40\n+\n+\n+def format_removed_symbols(\n+    delta: structural.SymbolDelta | None,\n+    *,\n+    path: str,\n+    base_sha: str,\n+) -> str:\n+    \"\"\"Render what this change deletes, for one file's hunk prompt.\n+\n+    Every tool we expose searches the head worktree, so a symbol the\n+    change removes returns nothing from `grep`, `read_file`, or the\n+    outline seed. The model cannot tell \"empty because it was deleted\"\n+    from \"empty because my pattern was wrong\", and rephrases: one\n+    observed hunk spent 50 tool calls — zero exact repeats, four distinct\n+    paths — hunting for three symbols this commit had removed.\n+\n+    Three sections, because three different questions get asked. Symbols\n+    gone from *this* file, with base-side spans and the base SHA to read\n+    them at. Symbols gone from *elsewhere in the change*, because a hunk\n+    that deletes call sites is looking for a definition in another file —\n+    the case that motivated the seed, and the one a per-file list misses.\n+    And symbols that only *moved*, which a per-path set-diff reports as\n+    removed even though they are still in the head worktree; telling the\n+    model to stop searching for those would earn a confidently wrong\n+    annotation on an ordinary refactor.\n+    \"\"\"\n+    if delta is None:\n+        return \"\"\n+    added_by_name: dict[str, str] = {sym.qualified_name: sym.path for sym in delta.added}\n+    here: list[str] = []\n+    moved: list[str] = []\n+    elsewhere: list[str] = []\n+    for sym in delta.removed:\n+        destination = added_by_name.get(sym.qualified_name)\n+        if destination is not None and destination != sym.path:\n+            moved.append(f\"{sym.kind} {sym.qualified_name}: {sym.path} -> {destination}\")\n+        elif sym.path == path:\n+            label = sym.signature or f\"{sym.kind} {sym.qualified_name}\"\n+            here.append(f\"{label}  (base {sym.range.start_line}-{sym.range.end_line})\")\n+        else:\n+            elsewhere.append(f\"{sym.kind} {sym.qualified_name}  ({sym.path})\")\n+\n+    sections: list[str] = []\n+    if here:\n+        sections.append(\n+            \"\\n\".join(\n+                [\n+                    f\"# Removed from this file (base side — NOT in the head worktree, so \"\n+                    f\"`grep`/`read_file` will not find them; read them with \"\n+                    f\"`read_file_at(sha={base_sha!r}, ...)`)\",\n+                    *_capped(here),\n+                ]\n+            )\n+        )\n+    if elsewhere:\n+        sections.append(\"\\n\".join([\"# Removed elsewhere in this change (same caveat)\", *_capped(elsewhere)]))\n+    if moved:\n+        sections.append(\n+            \"\\n\".join(\n+                [\n+                    \"# Moved, not removed — these ARE in the head worktree at the new path\",\n+                    *_capped(moved),\n+                ]\n+            )\n+        )\n+    return \"\\n\\n\".join(sections)\n+\n+\n+def _capped(entries: list[str]) -> list[str]:\n+    if len(entries) <= _REMOVED_SEED_MAX:\n+        return entries\n+    dropped = len(entries) - _REMOVED_SEED_MAX\n+    return [*entries[:_REMOVED_SEED_MAX], f\"... and {dropped} more (list truncated)\"]\n+\n+\n def format_hunk_prompt(\n     fp: AnnotatedFile,\n     hunk: AnnotatedHunk,\n     overview_json: str,\n     file_summary: str,\n     file_outline: str = \"\",\n-    surrounding: str = \"\",\n+    removed_symbols: str = \"\",\n ) -> list[UserContent]:\n     \"\"\"Assemble the user-prompt blocks for one hunk call.\n \n",
  "head_spans": [
   {
    "start_line": 44,
    "end_line": 48,
    "kind": "constant",
    "qualified_name": "_HUNK",
    "depth": 0
   },
   {
    "start_line": 83,
    "end_line": 86,
    "kind": "constant",
    "qualified_name": "_HUNK_CACHE_SETTINGS",
    "depth": 0
   },
   {
    "start_line": 98,
    "end_line": 100,
    "kind": "constant",
    "qualified_name": "_THINKING_SETTINGS",
    "depth": 0
   },
   {
    "start_line": 103,
    "end_line": 107,
    "kind": "function",
    "qualified_name": "_hunk_model_settings",
    "depth": 0
   },
   {
    "start_line": 115,
    "end_line": 115,
    "kind": "constant",
    "qualified_name": "_REMOVED_SEED_MAX",
    "depth": 0
   },
   {
    "start_line": 118,
    "end_line": 182,
    "kind": "function",
    "qualified_name": "format_removed_symbols",
    "depth": 0
   },
   {
    "start_line": 185,
    "end_line": 189,
    "kind": "function",
    "qualified_name": "_capped",
    "depth": 0
   },
   {
    "start_line": 192,
    "end_line": 230,
    "kind": "function",
    "qualified_name": "format_hunk_prompt",
    "depth": 0
   }
  ],
  "base_spans": [
   {
    "start_line": 39,
    "end_line": 43,
    "kind": "constant",
    "qualified_name": "_HUNK",
    "depth": 0
   },
   {
    "start_line": 58,
    "end_line": 61,
    "kind": "constant",
    "qualified_name": "_HUNK_CACHE_SETTINGS",
    "depth": 0
   },
   {
    "start_line": 64,
    "end_line": 102,
    "kind": "function",
    "qualified_name": "format_hunk_prompt",
    "depth": 0
   },
   {
    "start_line": 105,
    "end_line": 116,
    "kind": "function",
    "qualified_name": "_hunk_trace_path",
    "depth": 0
   },
   {
    "start_line": 119,
    "end_line": 160,
    "kind": "function",
    "qualified_name": "run_hunk_pass",
    "depth": 0
   },
   {
    "start_line": 163,
    "end_line": 249,
    "kind": "function",
    "qualified_name": "build_hunk_annotations",
    "depth": 0
   }
  ],
  "expect_runs": [
   {
    "kind": "ins",
    "start_idx": 10,
    "texts": [
     "# `anthropic_cache_messages` is the one that matters on a tool-using",
     "# pass, and its absence was costing more than the other two save. The",
     "# agent loop appends [tool_use][tool_result] per turn *after* every",
     "# fixed breakpoint, so without a rolling breakpoint the whole growing",
     "# conversation is re-sent as fresh input every turn: measured on a",
     "# 51-turn hunk, cache_read sat flat at 7,076 while fresh input climbed",
     "# 7,740 -> 46,698 and cache_write stayed at 0 throughout. Cost per turn",
     "# grows linearly and the loop's total grows quadratically. With the",
     "# rolling breakpoint each turn writes only its own delta and reads the",
     "# accumulated prefix at a tenth of the price.",
     "#"
    ]
   },
   {
    "kind": "ins",
    "start_idx": 24,
    "texts": [
     "# Anthropic allows 4 breakpoints per request. Over budget, pydantic-ai",
     "# keeps every system and tool one and trims *message* breakpoints",
     "# oldest-first, which silently evicts the overview marker — the only",
     "# prefix shared across files. Tool definitions get no breakpoint of",
     "# their own: Anthropic renders tools -> system -> messages, so caching",
     "# after the tools block caches `[tools]`, a strict prefix of the",
     "# `[tools + system]` that the instructions breakpoint already caches,",
     "# with both constant for the whole run. It can never produce a hit the",
     "# longer entry doesn't, and it was costing the slot that mattered."
    ]
   },
   {
    "kind": "ins",
    "start_idx": 38,
    "texts": [
     "# Adaptive thinking, with the summary returned rather than omitted.",
     "# Leaving `thinking` unset means the model does not think at all — so",
     "# every SDK-backend hunk had been annotated with reasoning off, while the",
     "# CLI backend got Claude Code's own. `display=\"summarized\"` is what makes",
     "# a trace show *why* a pass investigated the way it did, otherwise only",
     "# reconstructible from the tool-call sequence.",
     "#",
     "# Applied only alongside native structured output: Anthropic rejects",
     "# thinking combined with the forced tool choice `ToolOutput` produces, so",
     "# a model that cannot do native output must not be asked to think either.",
     "_THINKING_SETTINGS: dict[str, Any] = {",
     "    \"anthropic_thinking\": {\"type\": \"adaptive\", \"display\": \"summarized\"},",
     "}",
     ""
    ]
   },
   {
    "kind": "ins",
    "start_idx": 53,
    "texts": [
     "def _hunk_model_settings(client: Client) -> dict[str, Any]:",
     "    \"\"\"Model settings for a per-hunk call against `client`.\"\"\"",
     "    if not client.native_output:",
     "        return _HUNK_CACHE_SETTINGS",
     "    return {**_HUNK_CACHE_SETTINGS, **_THINKING_SETTINGS}",
     "",
     "",
     "#: Entries rendered per section before truncating. A wholesale file",
     "#: deletion can remove thousands of symbols, and this seed rides in",
     "#: every hunk prompt for the file — uncapped, one measured case reached",
     "#: ~62k tokens per hunk, which is the cost blowup the seed exists to",
     "#: avoid paying in tool calls.",
     "_REMOVED_SEED_MAX = 40",
     "",
     "",
     "def format_removed_symbols(",
     "    delta: structural.SymbolDelta | None,",
     "    *,",
     "    path: str,",
     "    base_sha: str,",
     ") -> str:",
     "    \"\"\"Render what this change deletes, for one file's hunk prompt.",
     "",
     "    Every tool we expose searches the head worktree, so a symbol the",
     "    change removes returns nothing from `grep`, `read_file`, or the",
     "    outline seed. The model cannot tell \"empty because it was deleted\"",
     "    from \"empty because my pattern was wrong\", and rephrases: one",
     "    observed hunk spent 50 tool calls — zero exact repeats, four distinct",
     "    paths — hunting for three symbols this commit had removed.",
     "",
     "    Three sections, because three different questions get asked. Symbols",
     "    gone from *this* file, with base-side spans and the base SHA to read",
     "    them at. Symbols gone from *elsewhere in the change*, because a hunk",
     "    that deletes call sites is looking for a definition in another file —",
     "    the case that motivated the seed, and the one a per-file list misses.",
     "    And symbols that only *moved*, which a per-path set-diff reports as",
     "    removed even though they are still in the head worktree; telling the",
     "    model to stop searching for those would earn a confidently wrong",
     "    annotation on an ordinary refactor.",
     "    \"\"\"",
     "    if delta is None:",
     "        return \"\"",
     "    added_by_name: dict[str, str] = {sym.qualified_name: sym.path for sym in delta.added}",
     "    here: list[str] = []",
     "    moved: list[str] = []",
     "    elsewhere: list[str] = []",
     "    for sym in delta.removed:",
     "        destination = added_by_name.get(sym.qualified_name)",
     "        if destination is not None and destination != sym.path:",
     "            moved.append(f\"{sym.kind} {sym.qualified_name}: {sym.path} -> {destination}\")",
     "        elif sym.path == path:",
     "            label = sym.signature or f\"{sym.kind} {sym.qualified_name}\"",
     "            here.append(f\"{label}  (base {sym.range.start_line}-{sym.range.end_line})\")",
     "        else:",
     "            elsewhere.append(f\"{sym.kind} {sym.qualified_name}  ({sym.path})\")",
     "",
     "    sections: list[str] = []",
     "    if here:",
     "        sections.append(",
     "            \"\\n\".join(",
     "                [",
     "                    f\"# Removed from this file (base side — NOT in the head worktree, so \"",
     "                    f\"`grep`/`read_file` will not find them; read them with \"",
     "                    f\"`read_file_at(sha={base_sha!r}, ...)`)\",",
     "                    *_capped(here),",
     "                ]",
     "            )",
     "        )",
     "    if elsewhere:",
     "        sections.append(\"\\n\".join([\"# Removed elsewhere in this change (same caveat)\", *_capped(elsewhere)]))",
     "    if moved:",
     "        sections.append(",
     "            \"\\n\".join(",
     "                [",
     "                    \"# Moved, not removed — these ARE in the head worktree at the new path\",",
     "                    *_capped(moved),",
     "                ]",
     "            )",
     "        )",
     "    return \"\\n\\n\".join(sections)",
     "",
     "",
     "def _capped(entries: list[str]) -> list[str]:",
     "    if len(entries) <= _REMOVED_SEED_MAX:",
     "        return entries",
     "    dropped = len(entries) - _REMOVED_SEED_MAX",
     "    return [*entries[:_REMOVED_SEED_MAX], f\"... and {dropped} more (list truncated)\"]",
     "",
     ""
    ]
   }
  ]
 },
 {
  "name": "semantic_code_review/augment/pass_.py:@@ -90,10 +104,94",
  "moved": false,
  "old_start": 90,
  "old_count": 10,
  "new_start": 104,
  "new_count": 94,
  "header": "@@ -90,10 +104,94 @@ async def run_pass(",
  "body": "                 _write_cache_hit_marker(trace_path, meta.name, entry)\n             return entry[\"response\"]\n \n+    # Bound the agentic loop. The CLI drivers get this from `--max-turns`;\n+    # SDK backends have no equivalent, so without a limit here a pass that\n+    # cannot answer its question keeps investigating until pydantic-ai's\n+    # default ceiling — losing the hunk after spending the most on it.\n+    usage_limits = UsageLimits(request_limit=client.request_limit) if client.request_limit else None\n+    # The last attempt is outside the loop so the function has one exit on\n+    # each path — the retries swallow the grammar error, the final call\n+    # propagates whatever it raises.\n+    for attempt in range(_GRAMMAR_RETRIES - 1):\n+        try:\n+            return await _drive_agent(\n+                meta,\n+                client=client,\n+                agent=agent,\n+                user_content=user_content,\n+                system=system,\n+                deps=deps,\n+                model_settings=model_settings,\n+                usage_limits=usage_limits,\n+                cache=cache,\n+                key=key,\n+                trace_path=trace_path,\n+                cache_request=cache_request,\n+            )\n+        except Exception as exc:\n+            if _GRAMMAR_TIMEOUT not in str(exc):\n+                raise\n+            log.info(\n+                \"%s pass: schema grammar not compiled yet (attempt %d/%d) — retrying\",\n+                meta.name,\n+                attempt + 1,\n+                _GRAMMAR_RETRIES,\n+            )\n+            _preserve_attempt_trace(trace_path, attempt + 1)\n+            await asyncio.sleep(_GRAMMAR_BACKOFF_SECONDS * (attempt + 1))\n+    return await _drive_agent(\n+        meta,\n+        client=client,\n+        agent=agent,\n+        user_content=user_content,\n+        system=system,\n+        deps=deps,\n+        model_settings=model_settings,\n+        usage_limits=usage_limits,\n+        cache=cache,\n+        key=key,\n+        trace_path=trace_path,\n+        cache_request=cache_request,\n+    )\n+\n+\n+def _preserve_attempt_trace(trace_path: Path | None, attempt: int) -> None:\n+    \"\"\"Move a failed attempt's trace aside so the retry doesn't erase it.\n+\n+    Every attempt drives a fresh `agent.iter`, so a retry re-runs and\n+    re-bills the whole tool loop, and it writes to the same path. Left\n+    alone the successful attempt overwrites the failed one and\n+    `usage.json` — which is derived from these files alone — reports a\n+    hunk that cost two loops as having cost one.\n+    \"\"\"\n+    if trace_path is None or not trace_path.exists():\n+        return\n+    kept = trace_path.with_name(f\"{trace_path.stem}-attempt{attempt}{trace_path.suffix}\")\n+    with contextlib.suppress(OSError):\n+        trace_path.replace(kept)\n+\n+\n+async def _drive_agent(\n+    meta: PassMeta,\n+    *,\n+    client: Client,\n+    agent: Agent[Any, Any],\n+    user_content: Any,\n+    system: str,\n+    deps: Any,\n+    model_settings: Any,\n+    usage_limits: Any,\n+    cache: CacheStore | None,\n+    key: CacheKey | None,\n+    trace_path: Path | None,\n+    cache_request: dict[str, Any] | None,\n+) -> dict[str, Any] | None:\n+    \"\"\"One attempt at the agent loop: drive, trace, account, cache.\"\"\"\n     async with agent.iter(\n         user_content,\n         deps=deps,\n         model_settings=model_settings,\n+        usage_limits=usage_limits,\n     ) as agent_run:\n         try:\n             async for _ in agent_run:\n",
  "head_spans": [
   {
    "start_line": 70,
    "end_line": 155,
    "kind": "function",
    "qualified_name": "run_pass",
    "depth": 0
   },
   {
    "start_line": 158,
    "end_line": 171,
    "kind": "function",
    "qualified_name": "_preserve_attempt_trace",
    "depth": 0
   },
   {
    "start_line": 174,
    "end_line": 244,
    "kind": "function",
    "qualified_name": "_drive_agent",
    "depth": 0
   }
  ],
  "base_spans": [
   {
    "start_line": 56,
    "end_line": 146,
    "kind": "function",
    "qualified_name": "run_pass",
    "depth": 0
   },
   {
    "start_line": 149,
    "end_line": 168,
    "kind": "function",
    "qualified_name": "_write_cache_hit_marker",
    "depth": 0
   },
   {
    "start_line": 171,
    "end_line": 171,
    "kind": "constant",
    "qualified_name": "__all__",
    "depth": 0
   }
  ],
  "expect_runs": null
 },
 {
  "name": "semantic_code_review/augment/pipeline.py:@@ -389,25 +451,166",
  "moved": false,
  "old_start": 389,
  "old_count": 25,
  "new_start": 451,
  "new_count": 166,
  "header": "@@ -389,25 +451,166 @@ class _HunkStats:",
  "body": "     failed: int = 0\n \n \n-# Head-side lines of real source to show either side of a hunk. The diff\n-# body carries only git's few context lines; widening it in-process is\n-# what makes the modal \"read my own file around this change\" tool call\n-# unnecessary — that call cost a whole extra turn, and a turn re-reads\n-# the accumulated context.\n-HUNK_SURROUNDING_LINES = 25\n-\n-\n-def _surrounding_source(repo_tools: RepoTools, path: str, hunk: AnnotatedHunk) -> str:\n-    \"\"\"Head source around `hunk`, or \"\" when there is no head side.\"\"\"\n-    parsed = hunk.parsed\n-    if parsed.new_count <= 0:\n-        return \"\"\n-    hunk_end = parsed.new_start + parsed.new_count - 1\n-    return repo_tools.source_window(\n-        path,\n-        parsed.new_start - HUNK_SURROUNDING_LINES,\n-        hunk_end + HUNK_SURROUNDING_LINES,\n-    )\n+def _plan_batches(\n+    queued: list[tuple[int, int, int]],\n+    batch_size: int,\n+) -> list[list[tuple[int, int, int]]]:\n+    \"\"\"Group queued hunks into per-file batches of at most `batch_size`.\n+\n+    Batches never span files: the whole point is that a file's summary and\n+    outline are constant across the batch and can therefore live in the\n+    system prompt, which is the region the CLI backend caches. A batch\n+    spanning two files would have to push both outlines back into the user\n+    prompt, where nothing is cached.\n+\n+    `batch_size <= 1` yields one hunk per batch, i.e. the unbatched pass.\n+    \"\"\"\n+    by_file: dict[int, list[tuple[int, int, int]]] = {}\n+    for entry in queued:\n+        by_file.setdefault(entry[0], []).append(entry)\n+    size = max(1, batch_size)\n+    batches: list[list[tuple[int, int, int]]] = []\n+    for entries in by_file.values():\n+        batches += [entries[i : i + size] for i in range(0, len(entries), size)]\n+    return batches\n+\n+\n+async def _augment_one_batch(\n+    batch: list[tuple[int, int, int]],\n+    meter: ProgressMeter,\n+    sem: asyncio.Semaphore,\n+    client: Client,\n+    diff: AnnotatedDiff,\n+    overview_json: str,\n+    repo_tools: RepoTools | None,\n+    model: str,\n+    cache: CacheStore | None,\n+    trace_dir: Path,\n+    stats: _HunkStats,\n+    results: dict[tuple[int, int], HunkAnnotations],\n+    on_event: OnEvent | None,\n+    file_spans: dict[int, tuple[list, list]],\n+    file_outline: str,\n+    removed_symbols: str,\n+) -> None:\n+    \"\"\"Annotate one file's batch in a single call; retry the remainder singly.\n+\n+    Anything the batch doesn't answer for — a hunk it skipped, or every\n+    hunk if the call itself failed — falls back to the one-hunk-per-call\n+    path. The fallback runs *after* this batch's semaphore slot is\n+    released, since `_augment_one_hunk` acquires the same semaphore and\n+    would otherwise deadlock against a saturated pool.\n+    \"\"\"\n+    fi = batch[0][0]\n+    fp = diff.files[fi]\n+    file_summary = (fp.ann.summary or \"\").strip()\n+    hunks = [(hi, fp.hunks[hi]) for _fi, hi, _ord in batch]\n+    ord_by_hi = {hi: ord_idx for _fi, hi, ord_idx in batch}\n+    fallback: list[tuple[int, int, int]] = []\n+\n+    async with sem:\n+        for _fi, hi, ord_idx in batch:\n+            meter.start_hunk(ord_idx)\n+            _safe_emit(on_event, \"hunk-start\", {\"file_idx\": fi, \"hunk_idx\": hi})\n+        rt = repo_tools or RepoTools(\n+            head_worktree=Path(\"/dev/null\"),\n+            repo_git=Path(\"/dev/null\"),\n+            base_sha=\"\",\n+            head_sha=\"\",\n+        )\n+        try:\n+            submit = await run_batch_pass(\n+                client,\n+                fp=fp,\n+                hunks=hunks,\n+                overview_json=overview_json,\n+                file_summary=file_summary,\n+                repo_tools=rt,\n+                model=model,\n+                file_outline=file_outline,\n+                removed_symbols=removed_symbols,\n+                cache=cache,\n+                trace_dir=trace_dir,\n+            )\n+        except _BUG_ERRORS:\n+            raise\n+        except Exception as e:  # noqa: BLE001 — a failed batch degrades to single calls\n+            log.warning(\n+                \"batch %s [%s] failed: %s: %s — retrying its hunks individually\",\n+                fp.path,\n+                \", \".join(str(hi) for hi, _ in hunks),\n+                type(e).__name__,\n+                e,\n+            )\n+            fallback = list(batch)\n+        else:\n+            by_index, missing = split_batch_annotations(submit, [hi for hi, _ in hunks])\n+            # Applying a returned annotation can raise on a malformed\n+            # payload. Guard each hunk separately: uncontained, one bad\n+            # entry escapes `asyncio.gather` and loses the whole run,\n+            # where the unbatched path would have lost one hunk.\n+            unusable: list[int] = []\n+            for hi, hunk in hunks:\n+                if hi not in by_index:\n+                    continue\n+                spans = file_spans.get(fi, ([], []))\n+                try:\n+                    ann = build_hunk_annotations(hunk.parsed, by_index[hi])\n+                    block = build_hunk_viewer_block(\n+                        AnnotatedHunk(parsed=hunk.parsed, ann=ann), fi, hi, spans[0], spans[1]\n+                    )\n+                except _BUG_ERRORS:\n+                    raise\n+                except Exception as e:  # noqa: BLE001 — one hunk degrades to a single call\n+                    log.warning(\n+                        \"batched hunk %s @ %s unusable: %s: %s — retrying singly\",\n+                        fp.path,\n+                        hunk.parsed.header,\n+                        type(e).__name__,\n+                        e,\n+                    )\n+                    unusable.append(hi)\n+                    continue\n+                results[(fi, hi)] = ann\n+                stats.ok += 1\n+                meter.finish_hunk(ord_by_hi[hi], ok=True)\n+                log.info(\n+                    \"hunk %s @ %s (batched): intent=%r smells=%d segs=%d notes=%d\",\n+                    fp.path,\n+                    hunk.parsed.header,\n+                    (ann.intent or \"\")[:80],\n+                    len(ann.smells),\n+                    len(ann.segments),\n+                    len(ann.line_notes),\n+                )\n+                _safe_emit(\n+                    on_event,\n+                    \"hunk\",\n+                    {\"file_idx\": fi, \"hunk_idx\": hi, \"ok\": True, \"block\": block},\n+                )\n+            fallback = [entry for entry in batch if entry[1] in missing or entry[1] in unusable]\n+\n+    for _fi, hi, ord_idx in fallback:\n+        await _augment_one_hunk(\n+            ord_idx,\n+            meter,\n+            sem,\n+            client,\n+            diff,\n+            fi,\n+            hi,\n+            overview_json,\n+            repo_tools,\n+            model,\n+            cache,\n+            trace_dir,\n+            stats,\n+            results,\n+            on_event,\n+            file_spans.get(fi, ([], [])),\n+            file_outline,\n+            removed_symbols,\n+        )\n \n \n async def _augment_one_hunk(\n",
  "head_spans": [
   {
    "start_line": 76,
    "end_line": 445,
    "kind": "function",
    "qualified_name": "augment_run_dir",
    "depth": 0
   },
   {
    "start_line": 449,
    "end_line": 451,
    "kind": "class",
    "qualified_name": "_HunkStats",
    "depth": 0
   },
   {
    "start_line": 454,
    "end_line": 475,
    "kind": "function",
    "qualified_name": "_plan_batches",
    "depth": 0
   },
   {
    "start_line": 478,
    "end_line": 613,
    "kind": "function",
    "qualified_name": "_augment_one_batch",
    "depth": 0
   },
   {
    "start_line": 616,
    "end_line": 718,
    "kind": "function",
    "qualified_name": "_augment_one_hunk",
    "depth": 0
   }
  ],
  "base_spans": [
   {
    "start_line": 387,
    "end_line": 389,
    "kind": "class",
    "qualified_name": "_HunkStats",
    "depth": 0
   },
   {
    "start_line": 397,
    "end_line": 397,
    "kind": "constant",
    "qualified_name": "HUNK_SURROUNDING_LINES",
    "depth": 0
   },
   {
    "start_line": 400,
    "end_line": 410,
    "kind": "function",
    "qualified_name": "_surrounding_source",
    "depth": 0
   },
   {
    "start_line": 413,
    "end_line": 512,
    "kind": "function",
    "qualified_name": "_augment_one_hunk",
    "depth": 0
   },
   {
    "start_line": 515,
    "end_line": 570,
    "kind": "function",
    "qualified_name": "_overview_event_payload",
    "depth": 0
   },
   {
    "start_line": 573,
    "end_line": 589,
    "kind": "function",
    "qualified_name": "_merge_hunk_results",
    "depth": 0
   },
   {
    "start_line": 592,
    "end_line": 604,
    "kind": "function",
    "qualified_name": "_attach_file_log",
    "depth": 0
   },
   {
    "start_line": 607,
    "end_line": 607,
    "kind": "constant",
    "qualified_name": "__all__",
    "depth": 0
   }
  ],
  "expect_runs": null
 },
 {
  "name": "semantic_code_review/augment/prompts.py:@@ -4,16 +4,41",
  "moved": true,
  "old_start": 4,
  "old_count": 16,
  "new_start": 4,
  "new_count": 41,
  "header": "@@ -4,16 +4,41 @@",
  "body": " it so a bump forces a full re-run.\n \n The wire format the model emits is constrained by the Pydantic models\n-in `schemas.py` (`OverviewSubmission`, `HunkAnnotations`) via\n-pydantic-ai's `output_type=ToolOutput(...)`. The prompts describe\n-*what* fields to populate; the schema enforces *how* they're shaped.\n+in `schemas.py` (`OverviewSubmission`, `HunkSubmission`) via\n+pydantic-ai's `output_type` — `NativeOutput` where the model supports\n+it, `ToolOutput` otherwise. The prompts describe *what* fields to\n+populate; the schema enforces *how* they're shaped.\n \"\"\"\n \n from __future__ import annotations\n \n from .schemas import SMELL_TAGS_TEXT\n \n-PROMPT_VERSION = \"p13\"\n+PROMPT_VERSION = \"p23\"\n+\n+\n+# Field guidance shared by the single-hunk and batched forms of the\n+# per-hunk pass. Held in one constant so the two framings can't drift:\n+# they differ in how many hunks arrive per call, never in what a hunk's\n+# annotation should contain.\n+_ANNOTATION_FIELDS = (\n+    \"Populate the following fields for each hunk:\\n\"\n+    \"- `intent`: 1-2 sentences. MOTIVE, not mechanics. Name the exact change (what was \"\n+    \"X, is now Y), not 'probably'. Bad: 'one-line tweak to the compose file (likely an \"\n+    \"image bump)'. Good: 'bumps the postgres image tag from 15.3 to 15.5'.\\n\"\n+    \"- `segments`: when the hunk contains semantically distinct edits (e.g. a refactor \"\n+    \"plus an unrelated fix, or a changed if-branch alongside a new else-branch), split \"\n+    \"them. Each segment has POST-IMAGE `new_start`/`new_count` and its own intent. Omit \"\n+    \"segments if the hunk is single-intent.\\n\"\n+    \"- `smells`: list of {tag, note}. Tags are from the closed vocabulary: \"\n+    f\"{SMELL_TAGS_TEXT}. Attach each smell to a segment when it's segment-local, or to the \"\n+    \"hunk when it spans the whole change.\\n\"\n+    \"- `context`: cross-file dependencies the reviewer can't see from the diff.\\n\"\n+    \"- `refs`: {path, line, reason} for other files the reviewer should look at.\\n\"\n+    \"- `confidence`: 0-100 integer. Low is fine and honest.\\n\"\n+    \"- `line_notes`: {line, body} for notes too specific for intent. `line` is post-image.\\n\\n\"\n+    \"Tone: explanatory, not evaluative. Comprehension first.\"\n+)\n \n \n OVERVIEW_SYSTEM = (\n",
  "head_spans": [
   {
    "start_line": 17,
    "end_line": 17,
    "kind": "constant",
    "qualified_name": "PROMPT_VERSION",
    "depth": 0
   },
   {
    "start_line": 24,
    "end_line": 41,
    "kind": "constant",
    "qualified_name": "_ANNOTATION_FIELDS",
    "depth": 0
   },
   {
    "start_line": 44,
    "end_line": 80,
    "kind": "constant",
    "qualified_name": "OVERVIEW_SYSTEM",
    "depth": 0
   }
  ],
  "base_spans": [
   {
    "start_line": 16,
    "end_line": 16,
    "kind": "constant",
    "qualified_name": "PROMPT_VERSION",
    "depth": 0
   },
   {
    "start_line": 19,
    "end_line": 55,
    "kind": "constant",
    "qualified_name": "OVERVIEW_SYSTEM",
    "depth": 0
   }
  ],
  "expect_runs": [
   {
    "kind": "ins",
    "start_idx": 16,
    "texts": [
     "# Field guidance shared by the single-hunk and batched forms of the",
     "# per-hunk pass. Held in one constant so the two framings can't drift:",
     "# they differ in how many hunks arrive per call, never in what a hunk's",
     "# annotation should contain.",
     "_ANNOTATION_FIELDS = (",
     "    \"Populate the following fields for each hunk:\\n\"",
     "    \"- `intent`: 1-2 sentences. MOTIVE, not mechanics. Name the exact change (what was \"",
     "    \"X, is now Y), not 'probably'. Bad: 'one-line tweak to the compose file (likely an \"",
     "    \"image bump)'. Good: 'bumps the postgres image tag from 15.3 to 15.5'.\\n\"",
     "    \"- `segments`: when the hunk contains semantically distinct edits (e.g. a refactor \"",
     "    \"plus an unrelated fix, or a changed if-branch alongside a new else-branch), split \"",
     "    \"them. Each segment has POST-IMAGE `new_start`/`new_count` and its own intent. Omit \"",
     "    \"segments if the hunk is single-intent.\\n\"",
     "    \"- `smells`: list of {tag, note}. Tags are from the closed vocabulary: \"",
     "    f\"{SMELL_TAGS_TEXT}. Attach each smell to a segment when it's segment-local, or to the \"",
     "    \"hunk when it spans the whole change.\\n\"",
     "    \"- `context`: cross-file dependencies the reviewer can't see from the diff.\\n\"",
     "    \"- `refs`: {path, line, reason} for other files the reviewer should look at.\\n\"",
     "    \"- `confidence`: 0-100 integer. Low is fine and honest.\\n\"",
     "    \"- `line_notes`: {line, body} for notes too specific for intent. `line` is post-image.\\n\\n\"",
     "    \"Tone: explanatory, not evaluative. Comprehension first.\"",
     ")",
     "",
     ""
    ]
   }
  ]
 },
 {
  "name": "semantic_code_review/viewer/assets/comments.ts:@@ -631,20 +632,54",
  "moved": false,
  "old_start": 631,
  "old_count": 20,
  "new_start": 632,
  "new_count": 54,
  "header": "@@ -631,20 +632,54 @@ function _removeAnnotationByDerivedId(derivedId: string): void {",
  "body": "   ).forEach((el) => el.remove());\n }\n \n-function _removeReviewerCommentRowsAfter(anchorRowEl: HTMLElement): void {\n-  let n: ChildNode | null = anchorRowEl.nextSibling;\n-  while (n) {\n-    const next = n.nextSibling;\n-    const el = n as HTMLElement;\n-    const isReviewerCommentRow = n.nodeType === 1\n-      && el.classList.contains(\"row-annotation\")\n-      && el.classList.contains(\"annot-comment\")\n-      && !el.classList.contains(\"annot-editor\")\n-      && (el.dataset.threadId || el.dataset.commentId);\n-    if (!isReviewerCommentRow) break;\n-    Annotations.detach(el);\n-    n = next;\n-  }\n+/** Remove the comment rows already rendered for `anchor`.\n+ *\n+ *  Keyed rather than positional. Every annotation inserts itself\n+ *  directly after its anchor row, so an LLM line-note — or the\n+ *  `.row-placeholder` a side-by-side shadow anchor adds — can sit\n+ *  between the anchor and its comment rows. A sibling walk that\n+ *  stopped at the first non-comment row then removed nothing, and\n+ *  every later `renderAll()` appended another copy of every comment\n+ *  on that line. */\n+function _removeReviewerCommentRowsFor(anchor: Anchor): void {\n+  const key = _anchorKey(anchor);\n+  document\n+    .querySelectorAll<HTMLElement>(\n+      `.row-annotation.annot-comment[data-anchor-key=\"${_cssAttrEscape(key)}\"]`,\n+    )\n+    .forEach((el) => {\n+      if (el.classList.contains(\"annot-editor\")) return;\n+      Annotations.detach(el);\n+    });\n+}\n+\n+/** Rendered-mode blocks host every thread whose line falls in the\n+ *  block's range, so their sweep is by range rather than by one line.\n+ *  Same reason as the keyed single-anchor sweep: position is not a\n+ *  reliable handle on which rows belong to this block. */\n+function _removeReviewerCommentRowsInRange(\n+  file: string, side: \"old\" | \"new\", startLine: number, endLine: number,\n+): void {\n+  document\n+    .querySelectorAll<HTMLElement>(\".row-annotation.annot-comment[data-anchor-key]\")\n+    .forEach((el) => {\n+      if (el.classList.contains(\"annot-editor\")) return;\n+      const [f, sd, ln] = (el.dataset.anchorKey || \"\").split(\"|\");\n+      const n = Number(ln);\n+      if (f !== file || sd !== side || Number.isNaN(n)) return;\n+      if (n < startLine || n > endLine) return;\n+      Annotations.detach(el);\n+    });\n+}\n+\n+function _anchorKey(anchor: Anchor): string {\n+  return `${anchor.file}|${anchor.side}|${anchor.line}`;\n+}\n+\n+/** Escape a value for use inside a CSS attribute selector. Paths carry\n+ *  `/`, `.` and can carry quotes. */\n+function _cssAttrEscape(value: string): string {\n+  return value.replace(/[\"\\\\]/g, \"\\\\$&\");\n }\n \n export const Comments = {\n",
  "head_spans": [
   {
    "start_line": 626,
    "end_line": 633,
    "kind": "function",
    "qualified_name": "_removeAnnotationByDerivedId",
    "depth": 0
   },
   {
    "start_line": 644,
    "end_line": 654,
    "kind": "function",
    "qualified_name": "_removeReviewerCommentRowsFor",
    "depth": 0
   },
   {
    "start_line": 660,
    "end_line": 673,
    "kind": "function",
    "qualified_name": "_removeReviewerCommentRowsInRange",
    "depth": 0
   },
   {
    "start_line": 675,
    "end_line": 677,
    "kind": "function",
    "qualified_name": "_anchorKey",
    "depth": 0
   },
   {
    "start_line": 681,
    "end_line": 683,
    "kind": "function",
    "qualified_name": "_cssAttrEscape",
    "depth": 0
   }
  ],
  "base_spans": [
   {
    "start_line": 625,
    "end_line": 632,
    "kind": "function",
    "qualified_name": "_removeAnnotationByDerivedId",
    "depth": 0
   },
   {
    "start_line": 634,
    "end_line": 648,
    "kind": "function",
    "qualified_name": "_removeReviewerCommentRowsAfter",
    "depth": 0
   }
  ],
  "expect_runs": null
 },
 {
  "name": "tests/test_augment_pipeline.py:@@ -267,56 +265,6",
  "moved": false,
  "old_start": 267,
  "old_count": 56,
  "new_start": 265,
  "new_count": 6,
  "header": "@@ -267,56 +265,6 @@ async def test_hunk_prompt_omits_seeds_when_context_is_skipped(tmp_path: Path) -",
  "body": "     for tool, text in canned.prompts:\n         if tool == \"submit_annotations\":\n             assert \"# File outline\" not in text\n-            assert \"# Surrounding head source\" not in text\n-\n-\n-class _OrderRecordingModel(_CannedModel):\n-    \"\"\"Records call start/end order, yielding to the loop in between.\n-\n-    The yield is what makes fan-out observable: concurrent calls all\n-    append `start` before any appends `end`.\n-    \"\"\"\n-\n-    def __init__(self, *args, **kwargs) -> None:  # type: ignore[no-untyped-def]\n-        super().__init__(*args, **kwargs)\n-        self.events: list[str] = []\n-\n-    async def request(  # type: ignore[override]\n-        self,\n-        messages: list[ModelMessage],\n-        model_settings: ModelSettings | None,\n-        model_request_parameters: ModelRequestParameters,\n-    ) -> ModelResponse:\n-        is_hunk = bool(\n-            model_request_parameters.output_tools\n-            and model_request_parameters.output_tools[0].name == \"submit_annotations\"\n-        )\n-        if is_hunk:\n-            self.events.append(\"start\")\n-        await asyncio.sleep(0)\n-        response = await super().request(messages, model_settings, model_request_parameters)\n-        if is_hunk:\n-            self.events.append(\"end\")\n-        return response\n-\n-\n-async def test_first_hunk_runs_alone_to_warm_the_shared_prefix(tmp_path: Path) -> None:\n-    \"\"\"A cold fan-out has every in-flight call write its own prefix copy.\n-\n-    Cache writes bill at a premium and reads at a fraction, so the first\n-    call is run alone to establish the prefix the rest read.\n-    \"\"\"\n-    run = _make_run_dir(tmp_path)\n-    model = _OrderRecordingModel(\n-        {\"summary\": \"s\", \"themes\": [], \"files\": [{\"path\": \"f.py\", \"summary\": \"fs\"}]},\n-        [\n-            {\"intent\": \"a\", \"confidence\": 90, \"smells\": []},\n-            {\"intent\": \"b\", \"confidence\": 90, \"smells\": []},\n-        ],\n-    )\n-    await augment_run_dir(run, model=\"t\", concurrency=8, client=Client(model=model), cache=None)\n-\n-    assert model.events[:2] == [\"start\", \"end\"]\n \n \n class _RecordingSubprocModel(_CannedModel):\n",
  "head_spans": [
   {
    "start_line": 253,
    "end_line": 267,
    "kind": "function",
    "qualified_name": "test_hunk_prompt_omits_seeds_when_context_is_skipped",
    "depth": 0
   },
   {
    "start_line": 270,
    "end_line": 278,
    "kind": "class",
    "qualified_name": "_RecordingSubprocModel",
    "depth": 0
   },
   {
    "start_line": 273,
    "end_line": 275,
    "kind": "function",
    "qualified_name": "_RecordingSubprocModel.__init__",
    "depth": 1
   },
   {
    "start_line": 277,
    "end_line": 278,
    "kind": "function",
    "qualified_name": "_RecordingSubprocModel.set_mcp_endpoint",
    "depth": 1
   },
   {
    "start_line": 281,
    "end_line": 321,
    "kind": "function",
    "qualified_name": "test_augment_subprocess_backend_hosts_one_mcp_server",
    "depth": 0
   },
   {
    "start_line": 298,
    "end_line": 312,
    "kind": "class",
    "qualified_name": "test_augment_subprocess_backend_hosts_one_mcp_server._FakeHost",
    "depth": 1
   },
   {
    "start_line": 299,
    "end_line": 303,
    "kind": "function",
    "qualified_name": "test_augment_subprocess_backend_hosts_one_mcp_server._FakeHost.__init__",
    "depth": 2
   },
   {
    "start_line": 305,
    "end_line": 306,
    "kind": "function",
    "qualified_name": "test_augment_subprocess_backend_hosts_one_mcp_server._FakeHost.start",
    "depth": 2
   },
   {
    "start_line": 308,
    "end_line": 309,
    "kind": "function",
    "qualified_name": "test_augment_subprocess_backend_hosts_one_mcp_server._FakeHost.stop",
    "depth": 2
   },
   {
    "start_line": 311,
    "end_line": 312,
    "kind": "function",
    "qualified_name": "test_augment_subprocess_backend_hosts_one_mcp_server._FakeHost.mcp_config",
    "depth": 2
   },
   {
    "start_line": 324,
    "end_line": 340,
    "kind": "function",
    "qualified_name": "test_augment_only_files_filters",
    "depth": 0
   }
  ],
  "base_spans": [
   {
    "start_line": 255,
    "end_line": 270,
    "kind": "function",
    "qualified_name": "test_hunk_prompt_omits_seeds_when_context_is_skipped",
    "depth": 0
   },
   {
    "start_line": 273,
    "end_line": 300,
    "kind": "class",
    "qualified_name": "_OrderRecordingModel",
    "depth": 0
   },
   {
    "start_line": 280,
    "end_line": 282,
    "kind": "function",
    "qualified_name": "_OrderRecordingModel.__init__",
    "depth": 1
   },
   {
    "start_line": 284,
    "end_line": 300,
    "kind": "function",
    "qualified_name": "_OrderRecordingModel.request",
    "depth": 1
   },
   {
    "start_line": 303,
    "end_line": 319,
    "kind": "function",
    "qualified_name": "test_first_hunk_runs_alone_to_warm_the_shared_prefix",
    "depth": 0
   },
   {
    "start_line": 322,
    "end_line": 330,
    "kind": "class",
    "qualified_name": "_RecordingSubprocModel",
    "depth": 0
   },
   {
    "start_line": 325,
    "end_line": 327,
    "kind": "function",
    "qualified_name": "_RecordingSubprocModel.__init__",
    "depth": 1
   }
  ],
  "expect_runs": null
 }
]
